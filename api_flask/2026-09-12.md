# Flask — Basic API Exercises

Date: 2026-09-12 | Level: Beginner | Sources: Flask docs, Real Python, GeeksforGeeks (adapted)

## Exercise 1: GET Calculator with Query Parameters
- **Difficulty:** Easy
- **Description:** Create a `GET /add` endpoint that reads two query parameters (`a` and `b`), parses them as numbers, and returns their sum as JSON. Return a 400 error if the parameters are missing or not numeric.
- **Concepts:** query parameters (`request.args`), `jsonify`, error responses, type parsing
- **Starter code:**
```python
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route("/add")
def add():
    a = request.args.get("a")
    b = request.args.get("b")
    # TODO: validate and parse a, b; return {"result": a + b} or a 400
    return jsonify({"result": None})

if __name__ == "__main__":
    app.run(debug=True)
```

## Exercise 2: POST with Validation and Error Handling
- **Difficulty:** Easy
- **Description:** Create a `POST /register` endpoint that accepts a JSON body with `username` and `email`. Validate that both fields are non-empty strings and return 400 with an error message if validation fails, otherwise return 201 with the created user.
- **Concepts:** `request.get_json()`, input validation, status codes, error handling
- **Starter code:**
```python
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route("/register", methods=["POST"])
def register():
    data = request.get_json(silent=True) or {}
    username = data.get("username")
    email = data.get("email")
    # TODO: validate fields; return 400 on failure, 201 + user on success
    return jsonify({}), 201

if __name__ == "__main__":
    app.run(debug=True)
```

## Exercise 3: In-Memory Book CRUD (GET/POST/PUT/DELETE)
- **Difficulty:** Medium
- **Description:** Build a CRUD API for books stored in a Python list. Implement `GET /books`, `POST /books`, `PUT /books/<id>`, and `DELETE /books/<id>`, returning appropriate status codes (201 created, 404 not found, 204 no content).
- **Concepts:** all HTTP verbs, route parameters, list manipulation, status codes
- **Starter code:**
```python
from flask import Flask, jsonify, request

app = Flask(__name__)
books = []  # each book: {"id": int, "title": str}

@app.route("/books", methods=["GET"])
def get_books():
    return jsonify(books)

@app.route("/books", methods=["POST"])
def create_book():
    data = request.get_json()
    # TODO: append book with auto-incremented id, return 201

@app.route("/books/<int:book_id>", methods=["PUT"])
def update_book(book_id):
    # TODO: update matching book or return 404

@app.route("/books/<int:book_id>", methods=["DELETE"])
def delete_book(book_id):
    # TODO: remove matching book or return 404 (204 on success)

if __name__ == "__main__":
    app.run(debug=True)
```
