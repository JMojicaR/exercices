# Flask API Exercises — 2026-07-24

## Exercise 1: Basic CRUD API for a Book Collection
**Difficulty:** ⭐⭐ Basic  
**Concepts:** Flask app setup, routing, HTTP methods (GET/POST/PUT/DELETE), JSON

**Description:** Build a RESTful API for managing a book collection. Store books in memory (a list of dictionaries). Each book has `id`, `title`, `author`, and `year`. Implement all CRUD endpoints using proper HTTP methods and JSON responses.

**Starter Code:**
```python
from flask import Flask, request, jsonify

app = Flask(__name__)

# In-memory storage
books = []
next_id = 1

# GET /books — List all books
@app.route('/books', methods=['GET'])
def get_books():
    return jsonify(books)

# GET /books/<id> — Get a specific book
@app.route('/books/<int:book_id>', methods=['GET'])
def get_book(book_id):
    # Your code: find book by id or return 404
    pass

# POST /books — Create a new book
@app.route('/books', methods=['POST'])
def create_book():
    global next_id
    data = request.get_json()
    # Your code: validate required fields, create book, return 201
    pass

# PUT /books/<id> — Update a book
@app.route('/books/<int:book_id>', methods=['PUT'])
def update_book(book_id):
    # Your code: find and update book or return 404
    pass

# DELETE /books/<id> — Delete a book
@app.route('/books/<int:book_id>', methods=['DELETE'])
def delete_book(book_id):
    # Your code: remove book or return 404
    pass

if __name__ == '__main__':
    app.run(debug=True)
```

**Expected Learning:** Flask route decorators, `@app.route()`, `methods` parameter, `request.get_json()`, `jsonify()`, HTTP status codes (200, 201, 404), URL parameter parsing (`<int:id>`).

**Test with curl:**
```bash
curl -X POST http://localhost:5000/books -H "Content-Type: application/json" -d '{"title":"1984","author":"George Orwell","year":1949}'
curl http://localhost:5000/books
curl http://localhost:5000/books/1
curl -X PUT http://localhost:5000/books/1 -H "Content-Type: application/json" -d '{"title":"Nineteen Eighty-Four","author":"George Orwell","year":1949}'
curl -X DELETE http://localhost:5000/books/1
```

---

## Exercise 2: Request Validation and Error Handling Middleware
**Difficulty:** ⭐⭐ Basic  
**Concepts:** Request validation, error handling, before/after request hooks, custom error responses

**Description:** Extend the book API with proper validation and error handling. Create a custom `validate_book_data()` function that checks required fields and types. Add `@app.errorhandler` for 400 (bad request), 404 (not found), and 500 (server error). Add a `@app.before_request` hook that logs all incoming requests.

**Starter Code:**
```python
from flask import Flask, request, jsonify
from datetime import datetime

app = Flask(__name__)
books = []
next_id = 1

def validate_book_data(data):
    """Validate book data. Returns (is_valid, error_message)."""
    if not data:
        return False, "Request body is required"
    if 'title' not in data:
        return False, "Title is required"
    # Your code: check author, year (optional), validate types
    return True, None

@app.before_request
def log_request():
    """Log each incoming request."""
    # Your code: log method, path, timestamp
    pass

@app.errorhandler(400)
def bad_request(error):
    # Your code: return JSON error with 400 status
    pass

@app.errorhandler(404)
def not_found(error):
    # Your code: return JSON error with 404 status
    pass

@app.errorhandler(500)
def server_error(error):
    # Your code: return JSON error with 500 status
    pass

# ... (include your CRUD routes from Exercise 1, using validate_book_data)

@app.route('/books', methods=['POST'])
def create_book():
    global next_id
    data = request.get_json()
    is_valid, error = validate_book_data(data)
    if not is_valid:
        from flask import abort
        abort(400, description=error)
    # Your code: create book
    pass

if __name__ == '__main__':
    app.run(debug=True)
```

**Expected Learning:** `@app.before_request`, `@app.errorhandler()`, `abort()`, custom validation functions, structured JSON error responses, middleware patterns.

---

## Exercise 3: Search and Filter with Query Parameters
**Difficulty:** ⭐⭐⭐ Basic  
**Concepts:** Query parameters, filtering, pagination, sorting

**Description:** Enhance the book API with search and filter capabilities. The `GET /books` endpoint should support query parameters: `?author=` (filter by author), `?year=` (filter by year), `?q=` (search in title), `?sort_by=` (title/year), `?limit=` and `?offset=` (pagination). Return paginated results with metadata.

**Starter Code:**
```python
from flask import Flask, request, jsonify

app = Flask(__name__)
books = [
    {"id": 1, "title": "1984", "author": "George Orwell", "year": 1949},
    {"id": 2, "title": "Brave New World", "author": "Aldous Huxley", "year": 1932},
    {"id": 3, "title": "Fahrenheit 451", "author": "Ray Bradbury", "year": 1953},
    {"id": 4, "title": "Animal Farm", "author": "George Orwell", "year": 1945},
]

@app.route('/books', methods=['GET'])
def get_books():
    # Get query parameters
    author_filter = request.args.get('author')
    year_filter = request.args.get('year', type=int)
    search_query = request.args.get('q')
    sort_by = request.args.get('sort_by', 'id')
    limit = request.args.get('limit', default=10, type=int)
    offset = request.args.get('offset', default=0, type=int)

    result = books[:]  # Start with all books (copy)

    # Your filtering logic here
    # Apply author filter, year filter, search query

    # Your sorting logic here
    # Sort by sort_by field

    # Your pagination logic here
    # Slice result[offset:offset+limit]

    return jsonify({
        'total': len(books),      # Total before pagination
        'count': len(result),     # Number in this page
        'offset': offset,
        'limit': limit,
        'books': result
    })

if __name__ == '__main__':
    app.run(debug=True)
```

**Test URLs:**
```
GET /books?author=George+Orwell
GET /books?year=1949
GET /books?q=Fahrenheit
GET /books?sort_by=year&limit=2&offset=0
```

**Expected Learning:** `request.args.get()`, query parameter types and defaults, list filtering with multiple conditions, sorting with `sorted()` and `key=`, pagination with slicing, structured API responses with metadata.

---

## Source Notes
Exercises are based on universally recognized REST API patterns and Flask documentation commonly found on Flask's official docs, Real Python, and GeeksforGeeks.
