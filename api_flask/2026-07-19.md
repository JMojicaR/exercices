# Flask API Exercises
**Date:** 2026-07-19
**Level:** Basic
**Source References:** Flask Documentation, Real Python, GeeksforGeeks

---

## Exercise 1: Simple REST API for a To-Do List

**Title:** Build a To-Do List REST API with Flask

**Description:**  
Create a Flask API for managing a simple to-do list stored in memory (a Python list of dictionaries). Implement the following endpoints:
- `GET /todos` — return all to-do items
- `GET /todos/<id>` — return a specific to-do item
- `POST /todos` — create a new to-do (accepts JSON with `title` and `done` fields)
- `PUT /todos/<id>` — update a to-do item
- `DELETE /todos/<id>` — delete a to-do item

Each to-do should have: `id`, `title`, `done` (boolean), and `created_at` (ISO timestamp).

**Concepts Practiced:** Flask routing, HTTP methods, JSON request/response, status codes, in-memory storage

**Difficulty:** Basic

**Starter Code:**
```python
from flask import Flask, request, jsonify
from datetime import datetime, timezone

app = Flask(__name__)

# In-memory storage
todos = []
next_id = 1

@app.route('/todos', methods=['GET'])
def get_todos():
    # Return all todos as JSON
    pass

@app.route('/todos/<int:todo_id>', methods=['GET'])
def get_todo(todo_id):
    # Return a specific todo or 404
    pass

@app.route('/todos', methods=['POST'])
def create_todo():
    # Create a new todo from JSON body
    # Expected JSON: {"title": "...", "done": false}
    # Return the created todo with 201 status
    pass

@app.route('/todos/<int:todo_id>', methods=['PUT'])
def update_todo(todo_id):
    # Update a todo by id
    # Return updated todo or 404
    pass

@app.route('/todos/<int:todo_id>', methods=['DELETE'])
def delete_todo(todo_id):
    # Delete a todo by id
    # Return empty response with 204 or 404
    pass

if __name__ == '__main__':
    app.run(debug=True)

# Test with curl:
# curl -X POST http://localhost:5000/todos -H "Content-Type: application/json" -d '{"title":"Learn Flask","done":false}'
# curl http://localhost:5000/todos
```

---

## Exercise 2: Request Validation Middleware

**Title:** Add Input Validation and Error Handling

**Description:**  
Extend a Flask API with custom decorators for request validation. Create:
- A `@require_json` decorator that ensures the request has `Content-Type: application/json` and a valid JSON body
- A `@validate_fields(*fields)` decorator that checks required fields exist in the JSON body
- A `@handle_errors` decorator that catches exceptions and returns proper JSON error responses
- An `error_handler` for 404 and 500 errors that returns JSON instead of HTML

Apply these to a simple `User` resource API with `POST /users` and `GET /users/<id>`.

**Concepts Practiced:** Decorators, middleware, error handling, request validation, JSON responses, Flask `errorhandler`

**Difficulty:** Basic

**Starter Code:**
```python
from flask import Flask, request, jsonify
from functools import wraps

app = Flask(__name__)
users = {}  # In-memory user store

# --- Custom Decorators ---

def require_json(f):
    @wraps(f)
    def decorated(*args, **kwargs):
        # Check Content-Type header
        # Parse JSON body, return 400 if invalid
        pass
    return decorated

def validate_fields(*required_fields):
    def decorator(f):
        @wraps(f)
        def decorated(*args, **kwargs):
            # Check that all required_fields are in request.json
            # Return 422 with missing fields listed
            pass
        return decorated
    return decorator

def handle_errors(f):
    @wraps(f)
    def decorated(*args, **kwargs):
        # Wrap in try/except, return JSON error on exception
        pass
    return decorated

# --- Error Handlers ---

@app.errorhandler(404)
def not_found(error):
    # Return JSON {"error": "Not found", "status": 404}
    pass

@app.errorhandler(500)
def internal_error(error):
    # Return JSON {"error": "Internal server error", "status": 500}
    pass

# --- User API ---

@app.route('/users/<int:user_id>', methods=['GET'])
def get_user(user_id):
    # Return user or 404 as JSON
    pass

@app.route('/users', methods=['POST'])
@require_json
@validate_fields('name', 'email')
@handle_errors
def create_user():
    # Create user from request.json
    # Auto-generate ID
    # Return 201 with created user
    pass

if __name__ == '__main__':
    app.run(debug=True)
```

---

## Exercise 3: Basic Authentication API

**Title:** Build a Token-Based Auth API

**Description:**  
Create a Flask API with the following:
- `POST /register` — register a new user (accepts `username` and `password`, store hashed passwords)
- `POST /login` — authenticate and return a simple token (use `secrets.token_hex` or a basic UUID as a token)
- `GET /profile` — protected endpoint that requires a valid token in the `Authorization: Bearer <token>` header
- A decorator `@login_required` that checks the token and returns 401 if invalid/missing

Store users and tokens in memory. Hash passwords using `werkzeug.security.generate_password_hash` and `check_password_hash`.

**Concepts Practiced:** Authentication, decorators, token management, password hashing, authorization headers, Flask `g` object

**Difficulty:** Basic

**Starter Code:**
```python
from flask import Flask, request, jsonify, g
from werkzeug.security import generate_password_hash, check_password_hash
from functools import wraps
import secrets

app = Flask(__name__)

# In-memory stores
users = {}        # username -> {"password_hash": ..., "created_at": ...}
tokens = {}       # token -> username

def login_required(f):
    @wraps(f)
    def decorated(*args, **kwargs):
        # Extract token from Authorization header
        # Validate token exists in tokens dict
        # Set g.current_user = username
        # Return 401 if invalid
        pass
    return decorated

@app.route('/register', methods=['POST'])
def register():
    # Accept username and password from JSON
    # Validate: username not taken, both fields present
    # Hash password with generate_password_hash()
    # Store in users dict
    # Return 201
    pass

@app.route('/login', methods=['POST'])
def login():
    # Accept username and password from JSON
    # Validate user exists and password matches
    # Generate token with secrets.token_hex(16)
    # Store token -> username mapping
    # Return {"token": token, "username": username}
    # Return 401 if invalid credentials
    pass

@app.route('/profile', methods=['GET'])
@login_required
def profile():
    # Return {"username": g.current_user}
    pass

if __name__ == '__main__':
    app.run(debug=True)

# Test flow:
# 1. POST /register {"username":"alice","password":"secret123"}
# 2. POST /login {"username":"alice","password":"secret123"} → get token
# 3. GET /profile -H "Authorization: Bearer <token>"
```

---

*Generated by Hermes Agent — Cron Job: Programming Exercise Collection*
