# Flask — Basic API Exercises

Date: 2026-09-11 | Level: Beginner | Sources: Flask docs, Real Python, GeeksforGeeks (adapted)

## Exercise 1: Hello, World! GET Endpoint
- **Difficulty:** Easy
- **Description:** Create a minimal Flask app with a `GET /` route that returns a JSON greeting, and a `GET /hello/<name>` route that personalizes the greeting.
- **Concepts:** app instantiation, `@app.route`, returning JSON, URL path parameters
- **Starter code:**
```python
from flask import Flask, jsonify

app = Flask(__name__)

@app.route("/")
def home():
    return jsonify({"message": "Hello, World!"})

@app.route("/hello/<name>")
def hello(name):
    # TODO: return a personalized greeting
    return jsonify({})

if __name__ == "__main__":
    app.run(debug=True)
```

## Exercise 2: POST Request Handling
- **Difficulty:** Easy
- **Description:** Add a `POST /echo` route that reads a JSON body and echoes it back along with a `received` flag.
- **Concepts:** `request.get_json()`, `methods=["POST"]`, request/response handling
- **Starter code:**
```python
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route("/echo", methods=["POST"])
def echo():
    data = request.get_json()
    # TODO: return {"received": data} (handle missing body with a 400)
    return jsonify({})

if __name__ == "__main__":
    app.run(debug=True)
```

## Exercise 3: In-Memory CRUD (GET/POST/PUT/DELETE)
- **Difficulty:** Medium
- **Description:** Build a complete CRUD API for a list of items stored in a Python list. Implement `GET /items`, `POST /items`, `PUT /items/<id>`, and `DELETE /items/<id>`.
- **Concepts:** all HTTP verbs, route parameters, list manipulation, status codes
- **Starter code:**
```python
from flask import Flask, jsonify, request

app = Flask(__name__)
items = []  # each item: {"id": int, "name": str}

@app.route("/items", methods=["GET"])
def get_items():
    return jsonify(items)

@app.route("/items", methods=["POST"])
def create_item():
    # TODO: append a new item with an auto-incremented id
    return jsonify({}), 201

@app.route("/items/<int:item_id>", methods=["PUT"])
def update_item(item_id):
    # TODO: update the item with matching id, or 404
    return jsonify({})

@app.route("/items/<int:item_id>", methods=["DELETE"])
def delete_item(item_id):
    # TODO: remove the item with matching id, or 404
    return jsonify({}), 204

if __name__ == "__main__":
    app.run(debug=True)
```
