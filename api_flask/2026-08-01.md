# Flask API Coding Exercises - 2026-08-01

---

## Exercise 1: Simple Todo List API

**Difficulty:** Basic

### Description

Build a RESTful API for managing a simple todo list. The API should allow clients to create, read, update, and delete todo items stored in an in-memory data structure (a Python `list` of `dict` objects). Each todo item has an `id` (auto-generated integer), a `title` (string), and a `completed` status (boolean, defaults to `False`). Respond with proper HTTP status codes for success and error cases.

### Concepts Covered

- Flask app initialization (`Flask(__name__)`)
- Route decorators (`@app.route`)
- HTTP methods: `GET`, `POST`, `PUT`, `DELETE`
- JSON request parsing (`request.get_json()`)
- JSON responses (`jsonify()`)
- In-memory data store (Python `list`)
- Auto-incrementing IDs
- HTTP status codes: `200`, `201`, `400`, `404`
- Error handling for missing resources

### Starter Code

```python
# exercise1_todo_api.py
from flask import Flask, request, jsonify

app = Flask(__name__)

# In-memory data store
todos = []
next_id = 1


# TODO 1: GET /todos — Return all todo items as a JSON list.
# Response: 200 OK with the todos list.
@app.route('/todos', methods=['GET'])
def get_todos():
    # Your code here
    pass


# TODO 2: GET /todos/<int:todo_id> — Return a single todo by ID.
# If the todo is not found, return a 404 error with {"error": "Todo not found"}.
# Response: 200 OK with the todo dict, or 404 Not Found.
@app.route('/todos/<int:todo_id>', methods=['GET'])
def get_todo(todo_id):
    # Your code here
    pass


# TODO 3: POST /todos — Create a new todo item.
# Expects JSON body: {"title": "Buy groceries"}
# Auto-assign an id, set completed to False.
# Validate that "title" is present and non-empty; if missing, return 400 with {"error": "Title is required"}.
# Response: 201 Created with the new todo dict.
@app.route('/todos', methods=['POST'])
def create_todo():
    # Your code here
    pass


# TODO 4: PUT /todos/<int:todo_id> — Update an existing todo item.
# Expects JSON body with any of: {"title": "...", "completed": true/false}
# Only update the fields that are provided (partial update).
# If the todo is not found, return 404.
# Response: 200 OK with the updated todo dict.
@app.route('/todos/<int:todo_id>', methods=['PUT'])
def update_todo(todo_id):
    # Your code here
    pass


# TODO 5: DELETE /todos/<int:todo_id> — Delete a todo by ID.
# If the todo is not found, return 404.
# Response: 200 OK with {"message": "Todo deleted"}.
@app.route('/todos/<int:todo_id>', methods=['DELETE'])
def delete_todo(todo_id):
    # Your code here
    pass


if __name__ == '__main__':
    app.run(debug=True)
```

### Expected Behavior (Manual Test with `curl` or HTTPie)

```bash
# Create a todo
curl -X POST http://127.0.0.1:5000/todos \
  -H "Content-Type: application/json" \
  -d '{"title": "Learn Flask"}'

# Get all todos
curl http://127.0.0.1:5000/todos

# Get a single todo
curl http://127.0.0.1:5000/todos/1

# Update a todo
curl -X PUT http://127.0.0.1:5000/todos/1 \
  -H "Content-Type: application/json" \
  -d '{"completed": true}'

# Delete a todo
curl -X DELETE http://127.0.0.1:5000/todos/1

# Try getting a non-existent todo (should return 404)
curl http://127.0.0.1:5000/todos/999
```

---

## Exercise 2: Book Store API

**Difficulty:** Basic

### Description

Create a REST API for a small online book store. The API manages a catalog of books, each with an `id`, `title`, `author`, `price` (float), and `in_stock` (boolean). Use an in-memory list as the data store. Implement full CRUD operations and add a search endpoint that filters books by author name (case-insensitive substring match via a query parameter).

### Concepts Covered

- Route path design with query parameters (`request.args.get()`)
- Data validation (type checking, positive price)
- Partial updates with field-level granularity
- Case-insensitive string filtering
- Consistent error response format (`{"error": "..."}`)
- HTTP status codes: `200`, `201`, `400`, `404`

### Starter Code

```python
# exercise2_book_store.py
from flask import Flask, request, jsonify

app = Flask(__name__)

# In-memory data store: list of book dicts
books = []
next_id = 1


# TODO 1: GET /books — Return all books.
# Support an optional query parameter: ?author=<name>
# When ?author= is provided, return only books whose author contains the
# given string (case-insensitive).
# If no author filter is given, return all books.
# Response: 200 OK with the filtered list.
@app.route('/books', methods=['GET'])
def get_books():
    # Your code here
    pass


# TODO 2: GET /books/<int:book_id> — Return a single book by ID.
# If not found, return 404 with {"error": "Book not found"}.
# Response: 200 OK.
@app.route('/books/<int:book_id>', methods=['GET'])
def get_book(book_id):
    # Your code here
    pass


# TODO 3: POST /books — Add a new book to the store.
# Expects JSON: {"title": "...", "author": "...", "price": 9.99, "in_stock": true}
# Validate:
#   - "title" and "author" must be non-empty strings
#   - "price" must be a positive number (> 0)
#   - "in_stock" is optional and defaults to True
# On validation failure, return 400 with a descriptive error message.
# Auto-generate the id field.
# Response: 201 Created with the new book dict.
@app.route('/books', methods=['POST'])
def create_book():
    # Your code here
    pass


# TODO 4: PUT /books/<int:book_id> — Fully update a book.
# Expects a complete JSON body with all fields: title, author, price, in_stock.
# Apply the same validation rules as POST.
# If the book is not found, return 404.
# Response: 200 OK with the updated book dict.
@app.route('/books/<int:book_id>', methods=['PUT'])
def update_book(book_id):
    # Your code here
    pass


# TODO 5: DELETE /books/<int:book_id> — Remove a book from the store.
# If not found, return 404.
# Response: 200 OK with {"message": "Book deleted"}.
@app.route('/books/<int:book_id>', methods=['DELETE'])
def delete_book(book_id):
    # Your code here
    pass


if __name__ == '__main__':
    app.run(debug=True)
```

### Expected Behavior

```bash
# Add a book
curl -X POST http://127.0.0.1:5000/books \
  -H "Content-Type: application/json" \
  -d '{"title": "Clean Code", "author": "Robert Martin", "price": 34.99}'

# Add another book
curl -X POST http://127.0.0.1:5000/books \
  -H "Content-Type: application/json" \
  -d '{"title": "The Pragmatic Programmer", "author": "David Thomas", "price": 42.00}'

# Search books by author (case-insensitive)
curl "http://127.0.0.1:5000/books?author=martin"

# Get all books
curl http://127.0.0.1:5000/books

# Try creating a book with an invalid price (should return 400)
curl -X POST http://127.0.0.1:5000/books \
  -H "Content-Type: application/json" \
  -d '{"title": "Bad Book", "author": "Someone", "price": -5.00}'
```

---

## Exercise 3: User Management API

**Difficulty:** Basic

### Description

Build a user management REST API that stores user profiles in an in-memory dictionary (keyed by user ID). Each user has an `id`, `username` (unique string), `email` (string), and `role` (one of `"admin"`, `"editor"`, or `"viewer"`, defaulting to `"viewer"`). Implement CRUD operations plus a special `PATCH` endpoint for changing only the user's role. Enforce username uniqueness on creation and update, and return appropriate error responses.

### Concepts Covered

- In-memory data store using a `dict` (keyed by ID)
- Uniqueness constraint validation (username)
- Enum-like validation (allowed roles)
- `PATCH` method for targeted partial updates
- Distinction between `PUT` (full replace) and `PATCH` (partial)
- Helper functions to reduce code duplication
- HTTP status codes: `200`, `201`, `400`, `404`, `409` (Conflict)

### Starter Code

```python
# exercise3_user_management.py
from flask import Flask, request, jsonify

app = Flask(__name__)

# In-memory data store: dict mapping user_id -> user dict
users = {}
next_id = 1

# Allowed roles
ALLOWED_ROLES = {"admin", "editor", "viewer"}


# TODO 0 (Helper): Write a helper function find_user_by_username(username)
# that iterates over users.values() and returns the first user dict with a
# matching username (case-sensitive), or None if not found.
# Use this in create and update to enforce uniqueness.
def find_user_by_username(username):
    # Your code here
    pass


# TODO 1: GET /users — Return all users as a list.
# Response: 200 OK with a list of user dicts.
@app.route('/users', methods=['GET'])
def get_users():
    # Your code here
    pass


# TODO 2: GET /users/<int:user_id> — Return a single user by ID.
# If not found, return 404 with {"error": "User not found"}.
# Response: 200 OK.
@app.route('/users/<int:user_id>', methods=['GET'])
def get_user(user_id):
    # Your code here
    pass


# TODO 3: POST /users — Create a new user.
# Expects JSON: {"username": "alice", "email": "alice@example.com", "role": "editor"}
# Validation:
#   - "username" is required and must be non-empty
#   - "email" is required and must be non-empty (simple check, no regex needed)
#   - "role" is optional; if provided, must be one of: admin, editor, viewer
#   - If role is not provided, default to "viewer"
#   - Username must be UNIQUE — return 409 Conflict with
#     {"error": "Username already exists"} if taken
# Auto-generate the id.
# Response: 201 Created with the new user dict.
@app.route('/users', methods=['POST'])
def create_user():
    # Your code here
    pass


# TODO 4: PUT /users/<int:user_id> — Fully replace a user.
# Expects a complete JSON body with username, email, and role.
# Apply the same validation rules as POST (including uniqueness).
# If the user is not found, return 404.
# When checking username uniqueness, skip the current user (they're allowed
# to keep their own username).
# Response: 200 OK with the updated user dict.
@app.route('/users/<int:user_id>', methods=['PUT'])
def update_user(user_id):
    # Your code here
    pass


# TODO 5: PATCH /users/<int:user_id>/role — Change only the user's role.
# Expects JSON: {"role": "admin"}
# Validate that the role is one of the allowed values.
# If the user is not found, return 404.
# Response: 200 OK with the updated user dict.
@app.route('/users/<int:user_id>/role', methods=['PATCH'])
def patch_user_role(user_id):
    # Your code here
    pass


# TODO 6: DELETE /users/<int:user_id> — Delete a user by ID.
# If not found, return 404.
# Response: 200 OK with {"message": "User deleted"}.
@app.route('/users/<int:user_id>', methods=['DELETE'])
def delete_user(user_id):
    # Your code here
    pass


if __name__ == '__main__':
    app.run(debug=True)
```

### Expected Behavior

```bash
# Create a user
curl -X POST http://127.0.0.1:5000/users \
  -H "Content-Type: application/json" \
  -d '{"username": "alice", "email": "alice@example.com", "role": "editor"}'

# Create another user
curl -X POST http://127.0.0.1:5000/users \
  -H "Content-Type: application/json" \
  -d '{"username": "bob", "email": "bob@example.com"}'

# Try creating a user with a duplicate username (should return 409)
curl -X POST http://127.0.0.1:5000/users \
  -H "Content-Type: application/json" \
  -d '{"username": "alice", "email": "alice2@example.com"}'

# Change a user's role via PATCH
curl -X PATCH http://127.0.0.1:5000/users/1/role \
  -H "Content-Type: application/json" \
  -d '{"role": "admin"}'

# Try setting an invalid role (should return 400)
curl -X PATCH http://127.0.0.1:5000/users/1/role \
  -H "Content-Type: application/json" \
  -d '{"role": "superuser"}'

# Get all users
curl http://127.0.0.1:5000/users

# Delete a user
curl -X DELETE http://127.0.0.1:5000/users/2
```

---

## Summary of HTTP Status Codes Used

| Code  | Meaning               | When Used                                          |
|-------|-----------------------|----------------------------------------------------|
| 200   | OK                    | Successful GET, PUT, PATCH, DELETE                 |
| 201   | Created               | Successful POST (resource created)                 |
| 400   | Bad Request           | Invalid or missing fields in the request body      |
| 404   | Not Found             | Resource with the given ID does not exist          |
| 409   | Conflict              | Duplicate username (Exercise 3)                    |

---

*Generated on 2026-08-01*
