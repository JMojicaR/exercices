# Flask API Exercises — 2026-08-08

> **Level:** Beginner (Basic) — focused on API creation  
> **Sources:** Flask docs, Real Python, freeCodeCamp  
> **Topics:** Routes, HTTP Methods (GET/POST/PUT/DELETE), JSON, Request/Response, In-Memory CRUD

---

## Exercise 1: Hello World API — Basic Flask App & Routes

| Field | Detail |
|-------|--------|
| **Difficulty** | ★☆☆☆☆ |
| **Concepts** | `Flask(__name__)`, `@app.route()`, `jsonify()`, `app.run()` |

**Description:**  
Create a Flask app with two endpoints:
- `GET /` — returns `{"message": "Hello, World!"}`
- `GET /greet/<name>` — returns `{"message": "Hello, <name>!"}`

**Starter Code:**
```python
from flask import Flask, jsonify

app = Flask(__name__)

@app.route('/')
def home():
    # TODO: Return {"message": "Hello, World!"}
    pass

@app.route('/greet/<name>')
def greet(name):
    # TODO: Return {"message": f"Hello, {name}!"}
    pass

if __name__ == '__main__':
    app.run(debug=True)
```

**Test:**
```bash
curl http://localhost:5000/
curl http://localhost:5000/greet/Alice
```

---

## Exercise 2: Todo API — GET & POST Endpoints (Create & Read)

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `request.get_json()`, list as in-memory storage, `jsonify()`, status codes (201) |

**Description:**  
Build a simple Todo API with in-memory storage (`list` of dicts):
- `GET /todos` — returns all todos
- `POST /todos` — creates a new todo `{"id": int, "title": str, "done": bool}`, returns it with status 201

**Starter Code:**
```python
from flask import Flask, jsonify, request

app = Flask(__name__)
todos = []
next_id = 1

@app.route('/todos', methods=['GET', 'POST'])
def handle_todos():
    global next_id
    if request.method == 'GET':
        return jsonify(todos)
    elif request.method == 'POST':
        data = request.get_json()
        # TODO: Create new todo with id, append, return 201
        pass

if __name__ == '__main__':
    app.run(debug=True)
```

**Test:**
```bash
curl -X POST http://localhost:5000/todos -H "Content-Type: application/json" -d '{"title":"Learn Flask","done":false}'
curl http://localhost:5000/todos
```

---

## Exercise 3: Todo API — PUT & DELETE with Error Handling (Update & Delete)

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★★☆☆ |
| **Concepts** | `PUT`/`DELETE` methods, URL parameters (`<int:id>`), 404 handling, `abort()` |

**Description:**  
Extend Exercise 2 with:
- `GET /todos/<int:id>` — returns a single todo, or 404 if not found
- `PUT /todos/<int:id>` — updates title and/or done status, or 404
- `DELETE /todos/<int:id>` — removes the todo, or 404

**Starter Code:**
```python
from flask import Flask, jsonify, request, abort

app = Flask(__name__)
todos = []
next_id = 1

def find_todo(todo_id):
    """Helper: return (todo, index) or (None, -1)"""
    for i, t in enumerate(todos):
        if t['id'] == todo_id:
            return t, i
    return None, -1

@app.route('/todos/<int:todo_id>', methods=['GET', 'PUT', 'DELETE'])
def single_todo(todo_id):
    todo, idx = find_todo(todo_id)
    if todo is None:
        abort(404, description="Todo not found")

    if request.method == 'GET':
        return jsonify(todo)
    elif request.method == 'PUT':
        data = request.get_json()
        # TODO: Update fields, return updated todo
    elif request.method == 'DELETE':
        # TODO: Remove and return success message
        pass

if __name__ == '__main__':
    app.run(debug=True)
```

**Test:**
```bash
curl http://localhost:5000/todos/1
curl -X PUT http://localhost:5000/todos/1 -H "Content-Type: application/json" -d '{"title":"Master Flask","done":true}'
curl -X DELETE http://localhost:5000/todos/1
```
