# JavaScript — Basic Exercises

Date: 2026-09-18 | Level: Beginner | Sources: W3Schools, MDN, freeCodeCamp, Programiz (adapted)

## Exercise 1: Temperature Converter
- **Difficulty:** Easy
- **Description:** Write two functions: `celsiusToFahrenheit(c)` and `fahrenheitToCelsius(f)` using `F = C * 9/5 + 32`. Log a conversion table from 0°C to 40°C in steps of 10 using `console.log()` and a `for` loop.
- **Concepts:** functions, arithmetic, template literals, `for` loop
- **Starter code:**
```javascript
function celsiusToFahrenheit(c) {
  return c * 9 / 5 + 32;
}

function fahrenheitToCelsius(f) {
  // TODO: return (f - 32) * 5 / 9
  return 0;
}

for (let c = 0; c <= 40; c += 10) {
  // TODO: console.log(`${c}C = ${celsiusToFahrenheit(c)}F`);
}
```

## Exercise 2: Count Vowels
- **Difficulty:** Easy
- **Description:** Write a function `countVowels(str)` that returns how many vowels (a, e, i, o, u — case-insensitive) appear in a string. Test with `"Hello World"` (3) and `"JavaScript"` (3).
- **Concepts:** strings, `toLowerCase()`, `includes()`, `for...of` loop, conditionals
- **Starter code:**
```javascript
function countVowels(str) {
  const vowels = "aeiou";
  let count = 0;
  for (const ch of str.toLowerCase()) {
    // TODO: increment count if vowels.includes(ch)
  }
  return count;
}

console.log(countVowels("Hello World")); // 3
```

## Exercise 3: Fibonacci Sequence
- **Difficulty:** Easy
- **Description:** Print the first N terms of the Fibonacci sequence (0, 1, 1, 2, 3, 5, ...) using a loop. Then write a recursive `fib(n)` that returns the nth term (0-indexed).
- **Concepts:** loops, recursion, functions, console logging
- **Starter code:**
```javascript
function printFibonacci(n) {
  let a = 0, b = 1;
  for (let i = 0; i < n; i++) {
    console.log(a);
    // TODO: [a, b] = [b, a + b];
  }
}

function fib(n) {
  if (n <= 1) return n;
  // TODO: return fib(n - 1) + fib(n - 2)
  return 0;
}

printFibonacci(10);
console.log("10th term:", fib(10)); // 55
```

## Exercise 4: Largest Element in an Array
- **Difficulty:** Easy
- **Description:** Write a function `largest(arr)` that returns the biggest number in an array without using `Math.max`. Return `null` for an empty array. Test with `[3, 41, 12, 9, 74, 15]`.
- **Concepts:** arrays, `for` loops, comparison, `null`, edge cases
- **Starter code:**
```javascript
function largest(arr) {
  if (arr.length === 0) return null;
  let biggest = arr[0];
  for (const n of arr) {
    // TODO: update biggest if n is larger
  }
  return biggest;
}

console.log(largest([3, 41, 12, 9, 74, 15])); // 74
```

## Exercise 5: Simple Calculator
- **Difficulty:** Easy
- **Description:** Build a calculator using a `switch` statement that takes two numbers and an operator (`+`, `-`, `*`, `/`) and returns the result. For division, check for zero and return `"Cannot divide by zero"`.
- **Concepts:** `switch`, functions, division by zero handling, return values
- **Starter code:**
```javascript
function calculator(a, op, b) {
  switch (op) {
    case "+":
      return a + b;
    // TODO: add cases for '-', '*', '/'
    // TODO: for '/', check b === 0 and return an error message
    default:
      return "Invalid operator";
  }
}

console.log(calculator(10, "+", 5));  // 15
console.log(calculator(10, "/", 0));  // Cannot divide by zero
```
