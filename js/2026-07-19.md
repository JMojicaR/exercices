# JavaScript Basic-Level Coding Exercises
**Date:** 2026-07-19
**Level:** Basic
**Source References:** W3Schools, freeCodeCamp, MDN Web Docs, Exercism

---

## Exercise 1: Array Sum

**Title:** Sum All Numbers in an Array

**Description:**  
Write a function `sumArray(arr)` that takes an array of numbers and returns the sum of all elements. Handle an empty array by returning 0. Use both a loop-based approach and the `reduce()` method.

**Concepts Practiced:** Arrays, loops (`for`/`for...of`), `Array.reduce()`, arithmetic

**Difficulty:** Basic

**Starter Code:**
```javascript
function sumArray(arr) {
    // Return the sum of all numbers in the array
    // Handle empty arrays
}

// Test cases
console.log(sumArray([1, 2, 3, 4, 5]));  // Expected: 15
console.log(sumArray([]));                // Expected: 0
console.log(sumArray([-1, 1]));          // Expected: 0
console.log(sumArray([10, 20, 30]));     // Expected: 60
```

---

## Exercise 2: Reverse a String

**Title:** String Reverser

**Description:**  
Write a function `reverseString(str)` that returns the input string reversed. Solve it using at least two different approaches (e.g., using built-in methods and using a loop).

**Concepts Practiced:** String methods (`split()`, `join()`, `reverse()`), loops, character iteration

**Difficulty:** Basic

**Starter Code:**
```javascript
function reverseString(str) {
    // Return the reversed string
}

// Test cases
console.log(reverseString("hello"));      // Expected: "olleh"
console.log(reverseString("JavaScript")); // Expected: "tpircSavaJ"
console.log(reverseString(""));           // Expected: ""
console.log(reverseString("a"));          // Expected: "a"
```

---

## Exercise 3: Even or Odd Filter

**Title:** Filter Even and Odd Numbers

**Description:**  
Write a function `filterEvenOdd(arr)` that returns an object with two arrays: `even` containing all even numbers, and `odd` containing all odd numbers from the input array.

**Concepts Practiced:** Arrays, `Array.filter()`, objects, modulo operator, destructuring

**Difficulty:** Basic

**Starter Code:**
```javascript
function filterEvenOdd(arr) {
    // Return an object: { even: [...], odd: [...] }
}

// Test cases
console.log(filterEvenOdd([1, 2, 3, 4, 5, 6]));
// Expected: { even: [2, 4, 6], odd: [1, 3, 5] }
console.log(filterEvenOdd([7, 11, 13]));
// Expected: { even: [], odd: [7, 11, 13] }
console.log(filterEvenOdd([]));
// Expected: { even: [], odd: [] }
```

---

## Exercise 4: Find the Longest Word

**Title:** Longest Word Finder

**Description:**  
Write a function `longestWord(sentence)` that takes a string sentence and returns the longest word. If there is a tie, return the first occurring longest word. Ignore punctuation.

**Concepts Practiced:** String splitting, iteration, length comparison, conditionals

**Difficulty:** Basic

**Starter Code:**
```javascript
function longestWord(sentence) {
    // Return the longest word from the sentence
    // Remove punctuation before processing
}

// Test cases
console.log(longestWord("The quick brown fox jumps over the lazy dog"));
// Expected: "jumps" (or "quick" — both are 5 chars)
console.log(longestWord("Hello world"));     // Expected: "Hello"
console.log(longestWord("A cat ran fast."));  // Expected: "fast"
```

---

## Exercise 5: Countdown Timer (Closure)

**Title:** Create a Countdown with Closures

**Description:**  
Write a function `createCountdown(start)` that returns a function. Each call to the returned function decrements a counter by 1 and returns the current count. When the count reaches 0, subsequent calls return `"Time's up!"`.

**Concepts Practiced:** Closures, higher-order functions, lexical scope, state management

**Difficulty:** Basic

**Starter Code:**
```javascript
function createCountdown(start) {
    // Return a function that counts down each time it's called
}

// Test
const countdown = createCountdown(3);
console.log(countdown());  // Expected: 2
console.log(countdown());  // Expected: 1
console.log(countdown());  // Expected: "Time's up!"
console.log(countdown());  // Expected: "Time's up!"
```

---

*Generated by Hermes Agent — Cron Job: Programming Exercise Collection*
