# JavaScript — Basic Exercises

Date: 2026-09-11 | Level: Beginner | Sources: W3Schools, MDN, Codecademy, freeCodeCamp (adapted)

## Exercise 1: Hello, World! and Variables
- **Difficulty:** Easy
- **Description:** Log "Hello, World!" to the console, then declare variables using `let` and `const` and log their types and values.
- **Concepts:** `console.log()`, `let`/`const`, primitive types, `typeof`
- **Starter code:**
```javascript
console.log("Hello, World!");

const name = "Ada";
let age = 36;
let height = 1.75;
let isStudent = true;

// TODO: log each variable and its type, e.g. console.log(typeof name)
```

## Exercise 2: Even or Odd
- **Difficulty:** Easy
- **Description:** Write a function `isEven(n)` that returns `true` if `n` is even and `false` otherwise, then test it with several values.
- **Concepts:** functions, modulo operator, booleans
- **Starter code:**
```javascript
function isEven(n) {
  // TODO: return true if n is even
}

console.log(isEven(4)); // true
console.log(isEven(7)); // false
```

## Exercise 3: FizzBuzz
- **Difficulty:** Easy
- **Description:** Loop from 1 to 50 and log "Fizz", "Buzz", "FizzBuzz", or the number according to divisibility by 3 and 5.
- **Concepts:** `for` loops, `if`/`else if`, modulo, `console.log()`
- **Starter code:**
```javascript
for (let i = 1; i <= 50; i++) {
  // TODO: log "Fizz", "Buzz", "FizzBuzz", or i
}
```

## Exercise 4: Reverse a String
- **Difficulty:** Easy
- **Description:** Write a function `reverseString(str)` that returns the reversed string without using built-in `reverse()` on arrays — use a loop.
- **Concepts:** strings, loops, string concatenation, array methods
- **Starter code:**
```javascript
function reverseString(str) {
  let reversed = "";
  // TODO: build the reversed string
  return reversed;
}

console.log(reverseString("hello")); // "olleh"
```

## Exercise 5: Sum of Positive Numbers in an Array
- **Difficulty:** Easy
- **Description:** Write a function `sumPositive(arr)` that returns the sum of only the positive numbers in an array.
- **Concepts:** arrays, iteration, conditionals, accumulator pattern
- **Starter code:**
```javascript
function sumPositive(arr) {
  let sum = 0;
  // TODO: add each positive number to sum
  return sum;
}

console.log(sumPositive([1, -4, 7, 12, -3])); // 20
```
