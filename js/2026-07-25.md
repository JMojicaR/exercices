# JavaScript Basic Coding Exercises - 2026-07-25

_Sourced from Exercism, freeCodeCamp, W3Schools, and GeeksforGeeks beginner tracks._

---

## Exercise 1: Array Manipulation — Card Dealer

- **Description**: Write a function that simulates dealing a hand of cards. Given an array representing a deck of cards (e.g., `["A♠", "2♠", "3♠", ...]`), the function should return a new array of 5 randomly selected cards. Use `Math.random()` and array methods like `splice()` or destructuring. Handle the case where fewer than 5 cards remain in the deck.
- **Difficulty**: Beginner
- **Concepts**: Arrays, `Math.random()`, `Array.splice()`, destructuring, control flow (if/else), function basics
- **Example/Starter Code**:
```js
function dealHand(deck) {
  // Return 5 random cards from the deck
  // If deck has fewer than 5 cards, return all remaining cards
}

// Test
const deck = [
  "A♠", "2♠", "3♠", "4♠", "5♠", "6♠", "7♠", "8♠",
  "A♥", "2♥", "3♥", "4♥", "5♥", "6♥", "7♥", "8♥"
];
console.log(dealHand(deck));
console.log(deck.length); // should be 11 (16 - 5)
```

---

## Exercise 2: DOM Manipulation — To-Do List

- **Description**: Build a simple interactive to-do list in the browser. The user can type a task into an input field, click an "Add" button, and the task appears as a new `<li>` element in a `<ul>`. Clicking on a task toggles a "completed" style (line-through). Use `document.querySelector()`, `addEventListener()`, `createElement()`, and class toggling. No frameworks — vanilla JavaScript only.
- **Difficulty**: Beginner
- **Concepts**: DOM selection, event listeners, element creation, CSS class manipulation, `classList.toggle()`
- **Example/Starter Code**:
```html
<!DOCTYPE html>
<html>
<body>
  <h2>My To-Do List</h2>
  <input id="taskInput" type="text" placeholder="Enter a task..." />
  <button id="addBtn">Add</button>
  <ul id="taskList"></ul>

  <script>
    // Select DOM elements
    const input = document.querySelector("#taskInput");
    const btn = document.querySelector("#addBtn");
    const list = document.querySelector("#taskList");

    // Write your addEventListener for the button click
    // Create <li>, set textContent, append to <ul>
    // Add click listener to <li> to toggle .completed class
  </script>
</body>
</html>
```

---

## Exercise 3: Form Validation — Signup Form

- **Description**: Create a simple signup form with fields for email, password, and confirm password. Write a JavaScript function that validates the form on submission: email must contain `@` and a `.`, password must be at least 8 characters, and both password fields must match. Display error messages next to the relevant field without an alert dialog. Use `event.preventDefault()` to stop invalid submissions.
- **Difficulty**: Beginner
- **Concepts**: Form events (`submit`), `preventDefault()`, string methods (`includes()`, `length`), DOM traversal, conditional rendering
- **Example/Starter Code**:
```html
<form id="signupForm">
  <div>
    <label>Email:</label>
    <input type="text" id="email" />
    <span class="error" id="emailError"></span>
  </div>
  <div>
    <label>Password:</label>
    <input type="password" id="password" />
    <span class="error" id="passwordError"></span>
  </div>
  <div>
    <label>Confirm Password:</label>
    <input type="password" id="confirmPassword" />
    <span class="error" id="confirmError"></span>
  </div>
  <button type="submit">Sign Up</button>
</form>

<script>
  const form = document.querySelector("#signupForm");

  form.addEventListener("submit", function (event) {
    // Clear previous errors
    // Validate email: must include '@' and '.'
    // Validate password: at least 8 characters
    // Validate confirm: must match password
    // If any validation fails, set error text and event.preventDefault()
  });
</script>
```

---

## Exercise 4: Array Methods — Student Grade Analyzer

- **Description**: Write a function that takes an array of student scores (numbers 0–100) and returns an object with the following statistics: the highest score, the lowest score, the average score (rounded to 1 decimal), the number of passing scores (≥ 60), and a new array with each score converted to a letter grade (A: 90+, B: 80–89, C: 70–79, D: 60–69, F: < 60). Use `Math.max()`, `Math.min()`, `reduce()`, `filter()`, and `map()`.
- **Difficulty**: Beginner
- **Concepts**: Array methods (`map`, `filter`, `reduce`), `Math` object, objects, conditionals
- **Example/Starter Code**:
```js
function analyzeGrades(scores) {
  // Return an object with:
  //   highest: highest score
  //   lowest: lowest score
  //   average: average score (1 decimal)
  //   passing: count of scores >= 60
  //   grades: array of letter grades ["A", "B", "C", ...]
}

// Test
const scores = [88, 92, 74, 60, 45, 81, 95, 55, 72, 68];
console.log(analyzeGrades(scores));
// Expected: { highest: 95, lowest: 45, average: 73.0, passing: 8, grades: ["B", "A", "C", "D", "F", ...] }
```

---

## Exercise 5: Event Handling — Color Guessing Game

- **Description**: Build a color guessing game. Display a color name (e.g., "Red") but render it in a different color (e.g., the word "Red" shown in blue text). The user must click a button matching the *ink color* (not the word). Show 3–4 color buttons as choices. Track the score and display it. Use `addEventListener` on buttons, generate random colors from an array, and update the score display in the DOM. This combines event handling, DOM updates, and basic game logic.
- **Difficulty**: Beginner
- **Concepts**: Event delegation/listeners, arrays, `Math.random()`, DOM text updates, conditional logic, game state management
- **Example/Starter Code**:
```html
<!DOCTYPE html>
<html>
<body>
  <h1>Color Guessing Game</h1>
  <p>What color is this text?</p>
  <h2 id="colorWord" style="font-size: 48px;">Red</h2>
  <div id="choices"></div>
  <p>Score: <span id="score">0</span></p>

  <script>
    const colors = ["Red", "Blue", "Green", "Yellow", "Orange", "Purple"];
    let score = 0;

    function newRound() {
      // Pick random word color and ink color (must be different)
      // Update #colorWord text and style.color
      // Generate buttons in #choices for each option
      // Add click listeners to buttons
      // On correct guess: score++ and newRound()
      // On wrong guess: display "Try again!" or reset
    }

    newRound();
  </script>
</body>
</html>
```
