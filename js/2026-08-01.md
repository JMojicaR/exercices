# JavaScript Coding Exercises - 2026-08-01

---

## Exercise 1: Hello, World! & Console Greeting

**Description:**  
Write a program that logs "Hello, World!" to the browser console. Then, declare a variable with your name and log a personalized greeting: "Hello, [name]! Welcome to JavaScript!".

**Difficulty:** Basic

**Concepts:**  
- Console output (`console.log()`)
- Variable declaration (`let`, `const`)
- String concatenation or template literals

**Starter Code:**
```javascript
// Exercise 1: Hello, World! & Console Greeting

// TODO: Log "Hello, World!" to the console
console.log("Hello, World!");

// TODO: Declare a variable with your name
// let myName = "Your Name";

// TODO: Log a personalized greeting using the variable
// Example output: "Hello, Alice! Welcome to JavaScript!"
```

---

## Exercise 2: Grade Calculator

**Description:**  
Write a function called `getGrade(score)` that takes a numeric score (0–100) and returns the letter grade using if/else if/else conditions:
- 90–100: "A"
- 80–89: "B"
- 70–79: "C"
- 60–69: "D"
- Below 60: "F"

Test the function with a few sample scores and log the results.

**Difficulty:** Basic

**Concepts:**  
- Functions and parameters
- If/else if/else conditionals
- Comparison operators (`>=`, `<=`)
- Return statements

**Starter Code:**
```javascript
// Exercise 2: Grade Calculator

function getGrade(score) {
    // TODO: Use if/else if/else to determine the letter grade
    // Return the appropriate grade letter ("A", "B", "C", "D", or "F")
}

// TODO: Test the function with different scores
console.log(getGrade(95));  // Expected: "A"
console.log(getGrade(83));  // Expected: "B"
console.log(getGrade(75));  // Expected: "C"
console.log(getGrade(62));  // Expected: "D"
console.log(getGrade(45));  // Expected: "F"
```

---

## Exercise 3: FizzBuzz

**Description:**  
Write a program that prints the numbers from 1 to 20 using a `for` loop. For multiples of 3, print "Fizz" instead of the number. For multiples of 5, print "Buzz". For numbers that are multiples of both 3 and 5, print "FizzBuzz".

**Difficulty:** Basic

**Concepts:**  
- For loops
- Modulo operator (`%`)
- If/else if/else conditionals
- Logical AND operator (`&&`)

**Starter Code:**
```javascript
// Exercise 3: FizzBuzz

// TODO: Loop from 1 to 20
// For each number, check:
//   - Divisible by both 3 and 5 → "FizzBuzz"
//   - Divisible by 3 → "Fizz"
//   - Divisible by 5 → "Buzz"
//   - Otherwise → print the number

// Example output:
// 1, 2, Fizz, 4, Buzz, Fizz, 7, 8, Fizz, Buzz, 11, Fizz, 13, 14, FizzBuzz, ...
```

---

## Exercise 4: Temperature Converter

**Description:**  
Write a function called `celsiusToFahrenheit(celsius)` that converts a Celsius temperature to Fahrenheit using the formula: `F = (C × 9/5) + 32`. Also write a function called `fahrenheitToCelsius(fahrenheit)` that converts Fahrenheit to Celsius using: `C = (F − 32) × 5/9`. Test both functions with sample values and log the results.

**Difficulty:** Basic

**Concepts:**  
- Functions with parameters and return values
- Arithmetic expressions
- Template literals for output

**Starter Code:**
```javascript
// Exercise 4: Temperature Converter

function celsiusToFahrenheit(celsius) {
    // TODO: Convert Celsius to Fahrenheit
    // Formula: (C × 9/5) + 32
}

function fahrenheitToCelsius(fahrenheit) {
    // TODO: Convert Fahrenheit to Celsius
    // Formula: (F − 32) × 5/9
}

// TODO: Test both functions
console.log("0°C = " + celsiusToFahrenheit(0) + "°F");   // Expected: 32°F
console.log("100°C = " + celsiusToFahrenheit(100) + "°F"); // Expected: 212°F
console.log("32°F = " + fahrenheitToCelsius(32) + "°C");  // Expected: 0°C
console.log("212°F = " + fahrenheitToCelsius(212) + "°C"); // Expected: 100°C
```

---

## Exercise 5: Array Palindrome Checker

**Description:**  
Write a function called `isPalindrome(word)` that takes a string and returns `true` if the string is a palindrome (reads the same forwards and backwards) and `false` otherwise. The function should be case-insensitive. Use array methods like `split()`, `reverse()`, and `join()`.

**Difficulty:** Basic

**Concepts:**  
- String methods (`.toLowerCase()`)
- Array methods (`.split()`, `.reverse()`, `.join()`)
- Functions and return values
- Comparison operators

**Starter Code:**
```javascript
// Exercise 5: Array Palindrome Checker

function isPalindrome(word) {
    // TODO: Convert the word to lowercase
    // TODO: Reverse the string using split, reverse, and join
    // TODO: Compare the original (lowercase) with the reversed version
    // TODO: Return true if they match, false otherwise
}

// TODO: Test the function with several words
console.log(isPalindrome("racecar"));  // Expected: true
console.log(isPalindrome("hello"));    // Expected: false
console.log(isPalindrome("Madam"));    // Expected: true (case-insensitive)
console.log(isPalindrome("A"));        // Expected: true
```
