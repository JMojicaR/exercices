# JavaScript Basic Exercises — 2026-08-08

> **Level:** Beginner (Basic)  
> **Sources:** W3Schools, W3Resource, freeCodeCamp, Exercism  
> **Topics:** Variables, Functions, Strings, Arrays, Loops, Conditionals

---

## Exercise 1: Hello, Console! — Variables & Output

| Field | Detail |
|-------|--------|
| **Difficulty** | ★☆☆☆☆ |
| **Concepts** | `console.log()`, `let`, `const`, template literals |

**Description:**  
Write a script that creates variables for a person's name, age, and favorite language. Print a formatted sentence using template literals. Then test it in the browser console or Node.js.

**Starter Code:**
```javascript
const name = "Alice";
const age = 25;
const language = "JavaScript";

// TODO: Print: "Alice is 25 years old and loves JavaScript!"
```

---

## Exercise 2: String Reversal — Functions & Loops

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | Functions, `for` loop, string indexing, `split()`, `reverse()`, `join()` |

**Description:**  
Write a function `reverseString(str)` that returns the input string reversed.  
Bonus: Solve it both with a loop and with array methods.

**Starter Code:**
```javascript
function reverseString(str) {
  // TODO: return the reversed string
}

console.log(reverseString("hello"));  // Expected: "olleh"
console.log(reverseString("JavaScript"));  // Expected: "tpircSavaJ"
```

---

## Exercise 3: FizzBuzz — Loops & Conditionals

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `for` loop, `%` modulo, `if/else if/else` |

**Description:**  
Print numbers 1 to 100. For multiples of 3 print `"Fizz"`, for multiples of 5 print `"Buzz"`, and for multiples of both print `"FizzBuzz"`.

**Starter Code:**
```javascript
for (let i = 1; i <= 100; i++) {
  // TODO: Print the number, "Fizz", "Buzz", or "FizzBuzz"
}
```

---

## Exercise 4: Array Sum & Average — Arrays & Reduce

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | Arrays, `reduce()`, `for...of`, `length` |

**Description:**  
Write a function `arrayStats(arr)` that returns an object with `sum` and `average` of all numbers in the array.

**Starter Code:**
```javascript
function arrayStats(arr) {
  // TODO: Return { sum: ..., average: ... }
}

const numbers = [4, 8, 15, 16, 23, 42];
console.log(arrayStats(numbers));
// Expected: { sum: 108, average: 18 }
```

---

## Exercise 5: Palindrome Checker — Strings & Logic

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★★☆☆ |
| **Concepts** | Strings, `toLowerCase()`, `replace()` with regex, `split().reverse().join()` |

**Description:**  
Write a function `isPalindrome(str)` that returns `true` if the string is a palindrome (reads the same forwards and backwards), ignoring case and non-alphanumeric characters.

**Starter Code:**
```javascript
function isPalindrome(str) {
  // TODO: Normalize the string and check if it's a palindrome
}

console.log(isPalindrome("Racecar"));         // true
console.log(isPalindrome("A man, a plan, a canal: Panama")); // true
console.log(isPalindrome("hello"));            // false
```
