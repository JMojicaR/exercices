# Programming Exercises — Summary 2026-09-18

## Languages (5 exercises each, basic level)

| Language | Exercises | Theme / Topic |
|----------|-----------|---------------|
| Python | 5 | Temperature converter, count vowels, Fibonacci (loop + recursion), largest in list, simple calculator |
| JavaScript | 5 | Temperature converter, count vowels, Fibonacci (loop + recursion), largest in array, calculator (`switch`) |
| Java | 5 | Temperature converter (`static`), count vowels, Fibonacci, largest in array, calculator (`Scanner` + `switch`) |
| C | 5 | Temperature converter, count vowels (`ctype.h`), Fibonacci (`long long`), largest in array, calculator (`switch`) |
| C++ | 5 | Temperature converter, count vowels (`std::tolower`), Fibonacci, largest in vector, calculator (`switch`) |
| C# | 5 | Temperature converter, count vowels, Fibonacci, largest in array, calculator (`switch`) |
| Rust | 5 | Temperature converter, count vowels, Fibonacci, largest in slice (`Option`), calculator (`match` + `Result`) |
| Go | 5 | Temperature converter, count vowels, Fibonacci, largest in slice, calculator (`switch` + `error`) |
| Erlang | 5 | Temperature converter, count vowels (recursion), Fibonacci (tail recursion), largest in list, calculator (pattern matching) |

## API Frameworks (3 exercises each)

| Framework | Exercises | Theme / Topic |
|-----------|-----------|---------------|
| Flask | 3 | GET with query params, POST with validation + 400 error, in-memory notes CRUD |
| FastAPI | 3 | GET path/query params, POST with Pydantic validation, in-memory products CRUD with `HTTPException` |
| Node.js/Express | 3 | GET routes/query params, POST with `express.json()` + validation, in-memory users CRUD + error middleware |

## Issues / Missing Content
- None. All 9 languages and 3 API frameworks delivered at basic/beginner level.
- Themes rotated from the prior week (even/odd, palindrome, multiplication table, sum of digits) to avoid repeats: this week centers on temperature conversion, vowel counting, Fibonacci, array/list max, and a simple calculator.
- API exercises cover GET/POST/PUT/DELETE, request/response handling, middleware (`express.json()`), and error handling (`HTTPException`, 400/404 status codes), with in-memory CRUD (notes / products / users).

## Totals
- 9 languages × 5 exercises = **45 exercises**
- 3 frameworks × 3 exercises = **9 exercises**
- **Total: 54 exercises**
