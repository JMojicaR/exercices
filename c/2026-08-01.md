# C Coding Exercises - 2026-08-01

---

## Exercise 1: Hello, World!

**Description:** Write a program that prints "Hello, World!" followed by a newline to the console. This is the classic first program every C programmer writes. It introduces you to the structure of a C program, the `main` function, and the `printf` function from the standard library.

**Difficulty:** Basic

**Concepts:** `printf`, `main` function, `#include`, `return` statement

**Starter Code:**

```c
#include <stdio.h>

int main() {
    // TODO: Print "Hello, World!" to the console

    return 0;
}
```

---

## Exercise 2: Simple Calculator

**Description:** Write a program that reads two integers from the user and prints their sum, difference, product, and quotient. Use `scanf` to read input and `printf` to display results. Handle the case where the second number is zero for division by displaying "Cannot divide by zero."

**Difficulty:** Basic

**Concepts:** `scanf`, `printf`, variables, arithmetic operators, `if/else`

**Starter Code:**

```c
#include <stdio.h>

int main() {
    int num1, num2;

    printf("Enter first number: ");
    scanf("%d", &num1);
    printf("Enter second number: ");
    scanf("%d", &num2);

    // TODO: Calculate and print sum, difference, product, and quotient
    // TODO: Handle division by zero

    return 0;
}
```

---

## Exercise 3: Factorial Calculator

**Description:** Write a program that reads a non-negative integer from the user and calculates its factorial using a loop. The factorial of n (written n!) is the product of all positive integers from 1 to n. By definition, 0! = 1. Print the result to the console.

**Difficulty:** Basic

**Concepts:** `for` loops, `scanf`, `printf`, integer multiplication, conditionals

**Starter Code:**

```c
#include <stdio.h>

int main() {
    int n;
    unsigned long long factorial = 1;

    printf("Enter a non-negative integer: ");
    scanf("%d", &n);

    // TODO: Check if n is negative and print an error
    // TODO: Calculate factorial using a loop
    // TODO: Print the result

    return 0;
}
```

---

## Exercise 4: Array Sum and Average

**Description:** Write a program that creates an array of 5 integers, fills it with values entered by the user, and then calculates and prints the sum and average of the array elements. Use a loop to read the values and another loop to compute the sum.

**Difficulty:** Basic

**Concepts:** Arrays, `for` loops, `scanf`, `printf`, type casting (for average)

**Starter Code:**

```c
#include <stdio.h>

int main() {
    int numbers[5];
    int sum = 0;
    float average;

    // TODO: Read 5 integers from the user into the array
    // TODO: Calculate the sum of all elements
    // TODO: Calculate the average (cast to float)
    // TODO: Print the sum and average

    return 0;
}
```

---

## Exercise 5: Swap Two Numbers Using Pointers

**Description:** Write a program with a function `swap` that takes two integer pointers as parameters and swaps the values they point to. In `main`, declare two integers, print their values before and after calling `swap`, and verify the swap worked correctly. This exercise introduces pointer parameters and call-by-reference in C.

**Difficulty:** Basic

**Concepts:** Functions, pointers, dereference operator (`*`), address-of operator (`&`), call-by-reference

**Starter Code:**

```c
#include <stdio.h>

// TODO: Write the swap function that takes two int pointers

int main() {
    int a = 10;
    int b = 20;

    printf("Before swap: a = %d, b = %d\n", a, b);

    // TODO: Call swap with the addresses of a and b

    printf("After swap: a = %d, b = %d\n", a, b);

    return 0;
}
```
