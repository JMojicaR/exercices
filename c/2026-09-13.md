# C — Basic Exercises

Date: 2026-09-13 | Level: Beginner | Sources: GeeksforGeeks, Programiz, W3Schools (adapted)

## Exercise 1: Even or Odd
- **Difficulty:** Easy
- **Description:** Read an integer with `scanf` and print whether it is "Even" or "Odd" using the modulo operator `%`.
- **Concepts:** `scanf`, `printf`, `if`/`else`, modulo `%`
- **Starter code:**
```c
#include <stdio.h>

int main(void) {
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);
    // TODO: print "Even" or "Odd"
    return 0;
}
```

## Exercise 2: Factorial
- **Difficulty:** Easy
- **Description:** Compute the factorial of `n` using a `for` loop and a `long long` accumulator.
- **Concepts:** `for` loop, `scanf`/`printf`, `long long` type
- **Starter code:**
```c
#include <stdio.h>

int main(void) {
    int n;
    long long fact = 1;
    printf("Enter n: ");
    scanf("%d", &n);
    // TODO: loop from 1 to n and multiply fact *= i
    printf("%lld\n", fact);
    return 0;
}
```

## Exercise 3: Palindrome Check
- **Difficulty:** Easy
- **Description:** Write `is_palindrome(char s[])` that returns 1 if a string reads the same forwards and backwards, 0 otherwise. Compare `s[i]` with `s[len-1-i]` in a loop.
- **Concepts:** char arrays, `strlen()`, `for` loop, indexing
- **Starter code:**
```c
#include <stdio.h>
#include <string.h>

int is_palindrome(char s[]) {
    int len = strlen(s);
    // TODO: compare s[i] with s[len-1-i] for i = 0..len/2
    return 1;
}

int main(void) {
    char s[100];
    printf("Enter a string: ");
    scanf("%s", s);
    printf("%s\n", is_palindrome(s) ? "Palindrome" : "Not a palindrome");
    return 0;
}
```

## Exercise 4: Sum of Digits
- **Difficulty:** Easy
- **Description:** Sum the digits of an integer using a `while` loop with `% 10` and `/ 10` (e.g. `1234` → `10`).
- **Concepts:** `while` loop, modulo `%`, integer division `/`
- **Starter code:**
```c
#include <stdio.h>

int main(void) {
    int num, sum = 0;
    printf("Enter an integer: ");
    scanf("%d", &num);
    while (num > 0) {
        // TODO: sum += num % 10; num /= 10;
    }
    printf("Sum: %d\n", sum);
    return 0;
}
```

## Exercise 5: Multiplication Table
- **Difficulty:** Easy
- **Description:** Print the multiplication table for a number from 1 to 10 using a `for` loop.
- **Concepts:** `for` loop, `printf` formatting, `scanf`
- **Starter code:**
```c
#include <stdio.h>

int main(void) {
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    for (int i = 1; i <= 10; i++) {
        // TODO: printf("%d x %d = %d\n", n, i, n * i);
    }
    return 0;
}
```
