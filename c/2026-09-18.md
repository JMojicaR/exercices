# C — Basic Exercises

Date: 2026-09-18 | Level: Beginner | Sources: W3Schools, GeeksforGeeks, Programiz (adapted)

## Exercise 1: Temperature Converter
- **Difficulty:** Easy
- **Description:** Write a function `double celsius_to_fahrenheit(double c)` and its inverse `fahrenheit_to_celsius`. In `main`, print a table from 0°C to 40°C in steps of 10 using `printf`.
- **Concepts:** functions, `double` arithmetic, `printf` formatting, `for` loop
- **Starter code:**
```c
#include <stdio.h>

double celsius_to_fahrenheit(double c) {
    return c * 9.0 / 5.0 + 32.0;
}

double fahrenheit_to_celsius(double f) {
    // TODO: return (f - 32.0) * 5.0 / 9.0;
    return 0;
}

int main(void) {
    for (int c = 0; c <= 40; c += 10) {
        // TODO: printf("%dC = %.1fF\n", c, celsius_to_fahrenheit(c));
    }
    return 0;
}
```

## Exercise 2: Count Vowels
- **Difficulty:** Easy
- **Description:** Write a function `int count_vowels(const char *s)` that counts vowels (a, e, i, o, u) case-insensitively in a null-terminated string. Handle both uppercase and lowercase letters.
- **Concepts:** strings as `char*`, loops, `tolower()` from `<ctype.h>`, character comparison
- **Starter code:**
```c
#include <stdio.h>
#include <ctype.h>

int count_vowels(const char *s) {
    int count = 0;
    for (int i = 0; s[i] != '\0'; i++) {
        char ch = tolower(s[i]);
        // TODO: increment count if ch is a vowel
    }
    return count;
}

int main(void) {
    printf("%d\n", count_vowels("Hello World")); // 3
    return 0;
}
```

## Exercise 3: Fibonacci Sequence
- **Difficulty:** Easy
- **Description:** Print the first N terms of the Fibonacci sequence using a loop (0, 1, 1, 2, 3, 5, ...). Then write a recursive `long fib(int n)` that returns the nth term. Use `long long` to avoid overflow for larger N.
- **Concepts:** loops, recursion, `long long`, functions
- **Starter code:**
```c
#include <stdio.h>

long long fib(int n) {
    if (n <= 1) return n;
    // TODO: return fib(n - 1) + fib(n - 2);
    return 0;
}

int main(void) {
    int n = 10;
    long long a = 0, b = 1;
    for (int i = 0; i < n; i++) {
        printf("%lld ", a);
        // TODO: update a and b
    }
    printf("\n10th term: %lld\n", fib(10)); // 55
    return 0;
}
```

## Exercise 4: Largest Element in an Array
- **Difficulty:** Easy
- **Description:** Write a function `int largest(const int *arr, int size)` that returns the biggest value in an integer array. Guard against `size == 0`.
- **Concepts:** arrays, pointers, loops, comparison, edge cases
- **Starter code:**
```c
#include <stdio.h>

int largest(const int *arr, int size) {
    if (size == 0) return 0;
    int biggest = arr[0];
    for (int i = 1; i < size; i++) {
        // TODO: update biggest if arr[i] is larger
    }
    return biggest;
}

int main(void) {
    int nums[] = {3, 41, 12, 9, 74, 15};
    int size = sizeof(nums) / sizeof(nums[0]);
    printf("%d\n", largest(nums, size)); // 74
    return 0;
}
```

## Exercise 5: Simple Calculator
- **Difficulty:** Easy
- **Description:** Build a calculator that reads two `double` values and an operator character (`+`, `-`, `*`, `/`) and prints the result using a `switch` statement. Check for division by zero.
- **Concepts:** `scanf`, `switch`, `double` arithmetic, division by zero handling
- **Starter code:**
```c
#include <stdio.h>

int main(void) {
    double a, b;
    char op;

    printf("Enter expression (e.g. 10 + 5): ");
    scanf("%lf %c %lf", &a, &op, &b);

    switch (op) {
        case '+':
            printf("%.2f\n", a + b);
            break;
        // TODO: add cases for '-', '*', '/'
        // TODO: for '/', check b == 0 and print an error
        default:
            printf("Invalid operator\n");
    }
    return 0;
}
```
