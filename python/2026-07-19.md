# Python Basic-Level Coding Exercises
**Date:** 2026-07-19
**Level:** Basic
**Source References:** W3Schools, Programiz, GeeksforGeeks, Exercism

---

## Exercise 1: Palindrome Checker

**Title:** String Palindrome Checker

**Description:**  
Write a function that checks whether a given string is a palindrome. A palindrome reads the same forward and backward, ignoring case and non-alphanumeric characters. Return `True` if it is a palindrome, `False` otherwise.

**Concepts Practiced:** String manipulation, loops, conditionals, string methods (`lower()`, `isalnum()`)

**Difficulty:** Basic

**Starter Code:**
```python
def is_palindrome(s: str) -> bool:
    # Remove non-alphanumeric characters and convert to lowercase
    # Then check if it reads the same forward and backward
    pass

# Test cases
print(is_palindrome("racecar"))        # Expected: True
print(is_palindrome("A man, a plan, a canal: Panama"))  # Expected: True
print(is_palindrome("hello"))          # Expected: False
print(is_palindrome(""))               # Expected: True
```

---

## Exercise 2: FizzBuzz

**Title:** Classic FizzBuzz

**Description:**  
Write a program that prints the numbers from 1 to 100. For multiples of 3, print `"Fizz"` instead of the number. For multiples of 5, print `"Buzz"`. For numbers that are multiples of both 3 and 5, print `"FizzBuzz"`.

**Concepts Practiced:** Loops, conditionals (`if/elif/else`), modulo operator, string output

**Difficulty:** Basic

**Starter Code:**
```python
def fizzbuzz(n: int = 100) -> None:
    # Loop from 1 to n and print according to FizzBuzz rules
    pass

# Run the function
fizzbuzz(30)  # Print first 30 for testing
```

---

## Exercise 3: List Deduplication

**Title:** Remove Duplicates from a List

**Description:**  
Write a function that takes a list and returns a new list with all duplicate elements removed, preserving the original order of first occurrences.

**Concepts Practiced:** Lists, sets, iteration, list comprehensions, order preservation

**Difficulty:** Basic

**Starter Code:**
```python
def remove_duplicates(items: list) -> list:
    # Return a new list with duplicates removed, preserving order
    pass

# Test cases
print(remove_duplicates([1, 2, 2, 3, 4, 4, 5]))  # Expected: [1, 2, 3, 4, 5]
print(remove_duplicates(["a", "b", "a", "c", "b"]))  # Expected: ["a", "b", "c"]
print(remove_duplicates([]))  # Expected: []
print(remove_duplicates([1, 1, 1, 1]))  # Expected: [1]
```

---

## Exercise 4: Word Frequency Counter

**Title:** Count Word Occurrences

**Description:**  
Write a function that takes a string and returns a dictionary where keys are words and values are the number of times each word appears. Ignore case and punctuation, and split on whitespace.

**Concepts Practiced:** Dictionaries, string methods (`split()`, `lower()`), iteration, counting

**Difficulty:** Basic

**Starter Code:**
```python
import string

def word_frequency(text: str) -> dict:
    # Return a dict mapping each word to its count
    pass

# Test case
sample = "The cat and the dog and the bird"
print(word_frequency(sample))
# Expected: {"the": 3, "cat": 1, "and": 2, "dog": 1, "bird": 1}
```

---

## Exercise 5: Temperature Converter

**Title:** Celsius ↔ Fahrenheit Converter

**Description:**  
Write a program with two functions: `celsius_to_fahrenheit(c)` and `fahrenheit_to_celsius(f)`. Both should take a float and return the converted value rounded to 2 decimal places. Include input validation — reject values below absolute zero (-273.15°C or -459.67°F).

**Concepts Practiced:** Functions, arithmetic, input validation, `round()`, error handling

**Difficulty:** Basic

**Starter Code:**
```python
def celsius_to_fahrenheit(celsius: float) -> float:
    # Convert Celsius to Fahrenheit
    # Formula: F = (C * 9/5) + 32
    # Validate: Celsius must be >= -273.15
    pass

def fahrenheit_to_celsius(fahrenheit: float) -> float:
    # Convert Fahrenheit to Celsius
    # Formula: C = (F - 32) * 5/9
    # Validate: Fahrenheit must be >= -459.67
    pass

# Test cases
print(celsius_to_fahrenheit(0))      # Expected: 32.0
print(celsius_to_fahrenheit(100))    # Expected: 212.0
print(fahrenheit_to_celsius(32))     # Expected: 0.0
print(fahrenheit_to_celsius(212))    # Expected: 100.0
```

---

*Generated by Hermes Agent — Cron Job: Programming Exercise Collection*
