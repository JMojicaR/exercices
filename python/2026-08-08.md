# Python Basic Exercises — 2026-08-08

> **Level:** Beginner (Basic)  
> **Sources:** W3Schools, PYnative, Programiz, GeeksforGeeks  
> **Topics:** Variables, IO, Conditionals, Loops, Functions, Lists

---

## Exercise 1: Hello, You! — Variables & Input

| Field | Detail |
|-------|--------|
| **Difficulty** | ★☆☆☆☆ |
| **Concepts** | `print()`, `input()`, variables, string concatenation, f-strings |

**Description:**  
Write a program that asks the user for their name and age, then prints a personalized greeting.  
Use f-strings to format the output nicely (e.g., `f"Hello {name}, you are {age} years old!"`).

**Starter Code:**
```python
def main():
    name = input("Enter your name: ")
    age = input("Enter your age: ")
    # TODO: Print a greeting using f-strings

if __name__ == "__main__":
    main()
```

**Expected Output (example):**
```
Enter your name: Alice
Enter your age: 25
Hello Alice, you are 25 years old!
```

---

## Exercise 2: Simple Calculator — Arithmetic & Conditionals

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `int()`/`float()` casting, `if/elif/else`, arithmetic operators (`+`, `-`, `*`, `/`) |

**Description:**  
Build a calculator that takes two numbers and an operator (+, -, *, /) and prints the result.  
Handle division by zero with an appropriate error message.

**Starter Code:**
```python
def calculator():
    a = float(input("Enter first number: "))
    op = input("Enter operator (+, -, *, /): ")
    b = float(input("Enter second number: "))

    # TODO: Compute and print the result
    # TODO: Handle division by zero

calculator()
```

---

## Exercise 3: Even or Odd — Conditionals & Modulo

| Field | Detail |
|-------|--------|
| **Difficulty** | ★☆☆☆☆ |
| **Concepts** | `%` (modulo), `if/else`, integer input |

**Description:**  
Write a program that reads an integer and prints whether it is **even** or **odd**.

**Starter Code:**
```python
number = int(input("Enter an integer: "))
# TODO: Check if number is even or odd and print the result
```

---

## Exercise 4: Number Guessing Game — Loops & Random

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `random.randint()`, `while` loop, `break`, user feedback (`too high`/`too low`) |

**Description:**  
The program picks a random number between 1 and 100. The user guesses until they get it right.  
Give hints ("Too high" / "Too low") after each guess and count the number of attempts.

**Starter Code:**
```python
import random

secret = random.randint(1, 100)
attempts = 0

# TODO: Write the guessing loop

print(f"You guessed it in {attempts} attempts!")
```

---

## Exercise 5: List Analyzer — Lists, Loops & Functions

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★★☆☆ |
| **Concepts** | `list`, `for` loop, `min()`, `max()`, `sum()`, `len()`, custom functions |

**Description:**  
Write a function that takes a list of numbers and returns a dictionary with:
- `min` — smallest number
- `max` — largest number
- `average` — mean value
- `sum` — total sum

**Starter Code:**
```python
def analyze_list(numbers):
    # TODO: Return a dict with min, max, average, sum
    pass

# Test
data = [4, 7, 1, 9, 3, 8, 2]
result = analyze_list(data)
print(result)
# Expected: {'min': 1, 'max': 9, 'average': 4.857..., 'sum': 34}
```
