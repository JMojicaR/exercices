# Python — Basic Exercises

Date: 2026-09-12 | Level: Beginner | Sources: W3Schools, GeeksforGeeks, Programiz, PyNative (adapted)

## Exercise 1: Temperature Converter
- **Difficulty:** Easy
- **Description:** Write two functions: `c_to_f(c)` and `f_to_c(f)` that convert between Celsius and Fahrenheit. Formulas: `F = C * 9/5 + 32` and `C = (F - 32) * 5/9`. Print a small conversion table for 0, 20, 40 °C.
- **Concepts:** functions, arithmetic, string formatting
- **Starter code:**
```python
def c_to_f(c):
    return c * 9 / 5 + 32

def f_to_c(f):
    # TODO: implement the reverse conversion
    return 0

print(f"0 C = {c_to_f(0)} F")
print(f"32 F = {f_to_c(32)} C")  # should print 0.0
```

## Exercise 2: Largest of Three Numbers
- **Difficulty:** Easy
- **Description:** Ask the user for three numbers and print the largest one. Solve it using `if`/`elif`/`else`, then try the built-in `max()` as a second approach.
- **Concepts:** `input()`, type conversion, conditionals, `max()`
- **Starter code:**
```python
a = int(input("First number: "))
b = int(input("Second number: "))
c = int(input("Third number: "))

# TODO: print the largest of a, b, c
```

## Exercise 3: Count Vowels
- **Difficulty:** Easy
- **Description:** Write a function `count_vowels(s)` that returns how many vowels (a, e, i, o, u) appear in a string, case-insensitively.
- **Concepts:** strings, `for` loops, `in` membership, `lower()`
- **Starter code:**
```python
def count_vowels(s):
    s = s.lower()
    count = 0
    # TODO: loop over s and count vowels
    return count

print(count_vowels("Hello World"))  # should print 3
```

## Exercise 4: Fibonacci Sequence
- **Difficulty:** Easy
- **Description:** Print the first N terms of the Fibonacci sequence (0, 1, 1, 2, 3, 5, ...), where N is read from the user.
- **Concepts:** loops, multiple assignment, variables
- **Starter code:**
```python
n = int(input("How many terms? "))

a, b = 0, 1
for _ in range(n):
    print(a)
    # TODO: update a and b to the next pair
```

## Exercise 5: List Statistics
- **Difficulty:** Easy
- **Description:** Given a list of integers, compute and print the sum, minimum, maximum, and average. Do it once with built-in functions and once with a manual loop.
- **Concepts:** lists, `sum()`/`min()`/`max()`/`len()`, loops
- **Starter code:**
```python
numbers = [12, 5, 8, 20, 3, 15]

total = sum(numbers)
print(f"sum: {total}")

# TODO: print min, max, and average (total / len(numbers))
```
