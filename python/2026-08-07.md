# Python Basic Coding Exercises — 2026-08-07

> **Level:** Basic / Beginner  
> **Sources:** PYnative, W3Schools, w3resource, Programiz  
> **Focus:** Variables, data types, conditionals, loops, functions, strings, lists

---

## Exercise 1: Hello, Variables!
**Difficulty:** ★☆☆☆☆  
**Concepts:** Variables, `print()`, string formatting, `input()`

### Description
Write a program that asks the user for their name and age, then prints a greeting message that includes both. Format the output as: `"Hello, <name>! You are <age> years old."`

### Starter Code
```python
name = input("Enter your name: ")
age = input("Enter your age: ")
# Your code here — print the greeting
```

---

## Exercise 2: Temperature Converter
**Difficulty:** ★☆☆☆☆  
**Concepts:** Arithmetic operators, float conversion, functions

### Description
Write a function `celsius_to_fahrenheit(celsius)` that converts a Celsius temperature to Fahrenheit using the formula:  
`F = (C × 9/5) + 32`. Also write `fahrenheit_to_celsius(fahrenheit)`. Test both with sample values.

### Starter Code
```python
def celsius_to_fahrenheit(celsius):
    # Your code here
    pass

def fahrenheit_to_celsius(fahrenheit):
    # Your code here
    pass

print(celsius_to_fahrenheit(0))      # Expected: 32.0
print(fahrenheit_to_celsius(32))     # Expected: 0.0
```

---

## Exercise 3: Even or Odd
**Difficulty:** ★☆☆☆☆  
**Concepts:** Conditionals (`if`/`else`), modulo operator, user input

### Description
Ask the user for a number and determine whether it is even or odd. Print the result. Extend the program to also check if the number is positive, negative, or zero.

### Starter Code
```python
num = int(input("Enter a number: "))

# Check if even or odd
if num % 2 == 0:
    print(f"{num} is even")
else:
    print(f"{num} is odd")

# TODO: Also check if positive, negative, or zero
```

---

## Exercise 4: List Statistics
**Difficulty:** ★★☆☆☆  
**Concepts:** Lists, `len()`, `sum()`, `min()`, `max()`, loops

### Description
Given a list of numbers, compute and print: the sum, the average (mean), the minimum, and the maximum. **Do not use built-in `sum()`, `min()`, or `max()`** — implement them manually using a loop.

### Starter Code
```python
numbers = [12, 5, 8, 21, 3, 17, 9]

total = 0
minimum = numbers[0]
maximum = numbers[0]

for n in numbers:
    # Your code here — update total, minimum, maximum
    pass

# Print: sum, average = total/len(numbers), minimum, maximum
```

---

## Exercise 5: Palindrome Checker
**Difficulty:** ★★☆☆☆  
**Concepts:** Strings, slicing, string methods, functions

### Description
Write a function `is_palindrome(text)` that returns `True` if the given string is a palindrome (reads the same forward and backward), ignoring case, spaces, and punctuation. Test it with: `"racecar"`, `"A man a plan a canal Panama"`, and `"hello"`.

### Starter Code
```python
import re

def is_palindrome(text):
    # Clean the string: lower case, remove non-alphanumeric chars
    cleaned = re.sub(r'[^a-zA-Z0-9]', '', text).lower()
    # Your code here — check if cleaned is a palindrome
    pass

print(is_palindrome("racecar"))              # Expected: True
print(is_palindrome("A man a plan a canal Panama"))  # Expected: True
print(is_palindrome("hello"))                # Expected: False
```

---

> **Tip:** Practice these exercises in order. Write your own solutions before looking up answers. Use `python3` to run your code.
