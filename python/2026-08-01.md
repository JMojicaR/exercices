# Python Coding Exercises - 2026-08-01

---

## Exercise 1: Hello, World! & Personal Greeting

**Description:**  
Write a program that prints "Hello, World!" to the console. Then, ask the user for their name using `input()` and print a personalized greeting: "Hello, [name]! Welcome to Python!".

**Difficulty:** Basic

**Concepts:**  
- Print to console (`print()`)
- User input (`input()`)
- Variables and string concatenation / f-strings

**Starter Code:**
```python
# Exercise 1: Hello, World! & Personal Greeting

# TODO: Print "Hello, World!"
print("Hello, World!")

# TODO: Ask the user for their name and store it in a variable
name = input("What is your name? ")

# TODO: Print a personalized greeting using the name
# Example output: "Hello, Alice! Welcome to Python!"
```

---

## Exercise 2: Even or Odd Checker

**Description:**  
Write a program that asks the user to enter an integer. Use an `if/else` statement to check whether the number is even or odd using the modulo operator (`%`). Print the result.

**Difficulty:** Basic

**Concepts:**  
- User input and type casting (`int()`)
- If/else conditionals
- Modulo operator (`%`)
- Comparison operators (`==`)

**Starter Code:**
```python
# Exercise 2: Even or Odd Checker

# TODO: Ask the user for a number and convert it to an integer
number = int(input("Enter an integer: "))

# TODO: Check if the number is even or odd using the modulo operator
# If number % 2 == 0, it's even; otherwise, it's odd

# TODO: Print the result
# Example output: "7 is odd."
```

---

## Exercise 3: Multiplication Table (For Loop)

**Description:**  
Write a program that asks the user for a number, then prints the multiplication table for that number from 1 to 10 using a `for` loop. Each line should show: `n x i = result`.

**Difficulty:** Basic

**Concepts:**  
- For loops with `range()`
- String formatting / f-strings
- Arithmetic operations

**Starter Code:**
```python
# Exercise 3: Multiplication Table

# TODO: Ask the user for a number
n = int(input("Enter a number: "))

# TODO: Use a for loop to iterate from 1 to 10
# Print each line in the format: "5 x 3 = 15"

# Example output for n=5:
# 5 x 1 = 5
# 5 x 2 = 10
# ...
# 5 x 10 = 50
```

---

## Exercise 4: Sum of Numbers (While Loop)

**Description:**  
Write a program that continuously asks the user to enter a number. Keep a running total of all numbers entered. If the user enters `0`, stop the loop and print the total sum. Use a `while` loop.

**Difficulty:** Basic

**Concepts:**  
- While loops
- Running totals / accumulators
- Break condition
- Conditionals inside loops

**Starter Code:**
```python
# Exercise 4: Sum of Numbers

# TODO: Initialize a total variable to 0
total = 0

# TODO: Use a while loop to repeatedly ask for numbers
# If the user enters 0, break out of the loop
# Otherwise, add the number to the total

# TODO: After the loop, print the total sum
# Example output: "The total sum is: 42"
```

---

## Exercise 5: List Statistics

**Description:**  
Write a function called `list_stats(numbers)` that takes a list of integers and returns the minimum, maximum, and average (mean) of the list. Test your function with a sample list and print the results.

**Difficulty:** Basic

**Concepts:**  
- Defining and calling functions
- Lists
- Built-in functions (`min()`, `max()`, `len()`)
- Return values

**Starter Code:**
```python
# Exercise 5: List Statistics

def list_stats(numbers):
    """
    TODO: Complete this function.
    It should take a list of numbers and return three values:
    - minimum value
    - maximum value
    - average (sum of all numbers / count of numbers)
    """
    # TODO: Calculate min, max, and average
    pass

# TODO: Test the function with this sample list
sample = [5, 12, 3, 8, 15, 7]

# TODO: Call list_stats and print the results
# Example output: "Min: 3, Max: 15, Average: 8.33..."
```
