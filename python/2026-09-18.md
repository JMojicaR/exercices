# Python — Basic Exercises

Date: 2026-09-18 | Level: Beginner | Sources: W3Schools, GeeksforGeeks, Programiz, PyNative (adapted)

## Exercise 1: Temperature Converter
- **Difficulty:** Easy
- **Description:** Write a function `celsius_to_fahrenheit(c)` that converts Celsius to Fahrenheit using the formula `F = C * 9/5 + 32`. Also write the reverse function `fahrenheit_to_celsius(f)`. Print a small conversion table from 0°C to 40°C in steps of 10.
- **Concepts:** functions, arithmetic, f-strings, `for` loop with `range()`
- **Starter code:**
```python
def celsius_to_fahrenheit(c):
    return c * 9 / 5 + 32

def fahrenheit_to_celsius(f):
    # TODO: return (f - 32) * 5 / 9
    return 0

for c in range(0, 41, 10):
    # TODO: print f"{c}C = {celsius_to_fahrenheit(c)}F"
    pass
```

## Exercise 2: Count Vowels
- **Difficulty:** Easy
- **Description:** Write a function `count_vowels(s)` that returns the number of vowels (a, e, i, o, u) in a string, counting both uppercase and lowercase. Test with `"Hello World"` (should be 3) and `"Programming"` (should be 3).
- **Concepts:** strings, `for` loop, `in` operator, `lower()`, conditionals
- **Starter code:**
```python
def count_vowels(s):
    vowels = "aeiou"
    count = 0
    for ch in s.lower():
        # TODO: increment count if ch is a vowel
        pass
    return count

print(count_vowels("Hello World"))  # 3
```

## Exercise 3: Fibonacci Sequence
- **Difficulty:** Easy
- **Description:** Print the first N terms of the Fibonacci sequence (0, 1, 1, 2, 3, 5, 8, ...). Ask the user for N. Implement it first with a loop, then write a recursive `fib(n)` function for the nth term.
- **Concepts:** `for`/`while` loops, list/sequence building, recursion, functions
- **Starter code:**
```python
n = int(input("How many Fibonacci terms? "))

a, b = 0, 1
for _ in range(n):
    print(a, end=" ")
    # TODO: update a and b (a, b = b, a + b)

def fib(n):
    if n <= 1:
        return n
    # TODO: return fib(n - 1) + fib(n - 2)
    return 0

print("\n10th term:", fib(10))  # 55
```

## Exercise 4: Largest Element in a List
- **Difficulty:** Easy
- **Description:** Write a function `largest(numbers)` that returns the largest number in a list without using the built-in `max()`. Handle the empty-list case by returning `None`. Test with `[3, 41, 12, 9, 74, 15]`.
- **Concepts:** lists, `for` loop, comparison, `None`, edge cases
- **Starter code:**
```python
def largest(numbers):
    if not numbers:
        return None
    biggest = numbers[0]
    for n in numbers:
        # TODO: update biggest if n is larger
        pass
    return biggest

print(largest([3, 41, 12, 9, 74, 15]))  # 74
```

## Exercise 5: Simple Calculator
- **Difficulty:** Easy
- **Description:** Build a calculator that reads two numbers and an operator (`+`, `-`, `*`, `/`) and prints the result. Use `if`/`elif` to select the operation. Handle division by zero with a friendly error message.
- **Concepts:** `input()`, `float()` conversion, conditionals, division by zero handling
- **Starter code:**
```python
a = float(input("First number: "))
op = input("Operator (+, -, *, /): ")
b = float(input("Second number: "))

if op == "+":
    print(a + b)
# TODO: handle '-', '*', '/'
# TODO: for '/', check b != 0 and print an error otherwise
```
