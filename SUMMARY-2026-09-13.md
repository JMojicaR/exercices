# Programming Exercises — Summary 2026-09-13

## Languages (5 exercises each, basic level)

| Language | Exercises | Theme / Topic |
|----------|-----------|---------------|
| Python | 5 | Even/odd, multiplication table, palindrome check, factorial (loop + recursion), sum of digits |
| JavaScript | 5 | FizzBuzz, multiplication table, palindrome check, filter even numbers + `reduce()`, string reverse |
| Java | 5 | Even/odd (`Scanner`), factorial, palindrome (`StringBuilder`), sum of digits, multiplication table |
| C | 5 | Even/odd (`scanf`), factorial (`long long`), palindrome (char array), sum of digits, multiplication table |
| C++ | 5 | Even/odd, factorial, palindrome (`std::string`/`std::reverse`), sum of digits, multiplication table |
| C# | 5 | Even/odd, factorial, palindrome (`Array.Reverse`), sum of digits, multiplication table |
| Rust | 5 | FizzBuzz, factorial, palindrome (iterators), sum of digits, multiplication table |
| Go | 5 | FizzBuzz, factorial, palindrome (`[]rune`), sum of digits, multiplication table |
| Erlang | 5 | Even/odd (guards), factorial (recursion), palindrome (`lists:reverse`), sum of digits (recursion), FizzBuzz (guards) |

## API Frameworks (3 exercises each)

| Framework | Exercises | Theme / Topic |
|-----------|-----------|---------------|
| Flask | 3 | GET endpoints (JSON + path params), POST with validation + 400 error, in-memory book CRUD |
| FastAPI | 3 | GET path/query params, POST with Pydantic model + validation, in-memory task CRUD with `HTTPException` |
| Node.js/Express | 3 | GET routes/query params, POST with `express.json()` middleware + validation, in-memory todo CRUD |

## Issues / Missing Content
- None. All 9 languages and 3 API frameworks delivered at basic/beginner level.
- API exercises cover GET/POST/PUT/DELETE, request/response handling, middleware (`express.json()`), error handling (`HTTPException`, 400/404/422), and basic in-memory CRUD.

## Totals
- 9 languages × 5 exercises = **45 exercises**
- 3 frameworks × 3 exercises = **9 exercises**
- **Total: 54 exercises**
