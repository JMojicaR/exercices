# C# Coding Exercises - 2026-08-01

---

## Exercise 1: Hello, World!

**Description:** Write a program that prints "Hello, World!" to the console. This introduces the structure of a C# program, the `Main` method, and the `Console.WriteLine` method. Optionally, use the top-level statements feature of modern C# to simplify the boilerplate.

**Difficulty:** Basic

**Concepts:** `Console.WriteLine`, `Main` method, namespace, `using System`, top-level statements

**Starter Code:**

```csharp
using System;

class Program
{
    static void Main()
    {
        // TODO: Print "Hello, World!" to the console

    }
}
```

---

## Exercise 2: Simple Calculator

**Description:** Write a program that reads two numbers from the user and prints their sum, difference, product, and quotient. Use `Console.ReadLine` and `double.Parse` (or `Convert.ToDouble`) for input. If the second number is zero, print "Cannot divide by zero." instead of attempting division.

**Difficulty:** Basic

**Concepts:** `Console.ReadLine`, `Console.WriteLine`, `double.Parse`, arithmetic operators, `if/else`

**Starter Code:**

```csharp
using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter first number: ");
        double num1 = double.Parse(Console.ReadLine());

        Console.Write("Enter second number: ");
        double num2 = double.Parse(Console.ReadLine());

        // TODO: Calculate and print sum, difference, product, and quotient
        // TODO: Handle division by zero

    }
}
```

---

## Exercise 3: Factorial Calculator

**Description:** Write a program that reads a non-negative integer from the user and calculates its factorial using a method. The factorial of n (n!) is the product of all positive integers from 1 to n. By definition, 0! = 1. Use recursion or a loop. If the user enters a negative number, print an error message.

**Difficulty:** Basic

**Concepts:** Methods, loops/recursion, `Console.ReadLine`, `int.Parse`, conditionals, `long` type

**Starter Code:**

```csharp
using System;

class Program
{
    // TODO: Write a method that calculates and returns the factorial of n

    static void Main()
    {
        Console.Write("Enter a non-negative integer: ");
        int n = int.Parse(Console.ReadLine());

        // TODO: Validate input (check for negative)
        // TODO: Call the factorial method and print the result

    }
}
```

---

## Exercise 4: Array Statistics with LINQ

**Description:** Write a program that creates an array of 10 integers (you can hardcode the values), then uses LINQ to calculate and print the sum, average, minimum, and maximum values. This introduces the `System.Linq` namespace, extension methods like `.Sum()`, `.Average()`, `.Min()`, and `.Max()`, and working with integer arrays.

**Difficulty:** Basic

**Concepts:** Arrays, `System.Linq`, `.Sum()`, `.Average()`, `.Min()`, `.Max()`, `Console.WriteLine`

**Starter Code:**

```csharp
using System;
using System.Linq;

class Program
{
    static void Main()
    {
        int[] numbers = { 15, 8, 23, 42, 7, 19, 31, 4, 12, 55 };

        // TODO: Use LINQ to calculate sum, average, min, and max
        // TODO: Print all results to the console

    }
}
```

---

## Exercise 5: Simple Person Class with Properties

**Description:** Define a `Person` class with auto-implemented properties for `Name` (string) and `Age` (int). Add a method `Introduce()` that prints a greeting like "Hi, I'm [Name] and I'm [Age] years old." In `Main`, create two `Person` objects, call `Introduce()` on each, then modify one person's age and call `Introduce()` again to show the update. This introduces classes, properties, and methods in C#.

**Difficulty:** Basic

**Concepts:** Classes, auto-implemented properties, methods, `Console.WriteLine`, object instantiation, string interpolation

**Starter Code:**

```csharp
using System;

// TODO: Define the Person class with Name and Age properties
// TODO: Add an Introduce() method that prints a greeting

class Program
{
    static void Main()
    {
        // TODO: Create two Person objects
        // TODO: Call Introduce() on each
        // TODO: Change one person's Age property
        // TODO: Call Introduce() again to show the updated age

    }
}
```
