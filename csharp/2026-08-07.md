# C# Basic Coding Exercises — 2026-08-07

> **Level:** Basic / Beginner  
> **Sources:** W3Schools, w3resource, PYnative, Exercism  
> **Focus:** Console I/O, variables, conditionals, loops, arrays, lists, classes, methods

---

## Exercise 1: Console Greeter
**Difficulty:** ★☆☆☆☆  
**Concepts:** `Console.WriteLine()`, `Console.ReadLine()`, string interpolation

### Description
Write a C# console application that asks the user for their first name, last name, and birth year. Calculate their approximate age and print a personalized greeting using **string interpolation** (`$"..."`).

### Starter Code
```csharp
using System;

class Program {
    static void Main() {
        Console.Write("Enter your first name: ");
        string firstName = Console.ReadLine();

        Console.Write("Enter your last name: ");
        string lastName = Console.ReadLine();

        Console.Write("Enter your birth year: ");
        int birthYear = int.Parse(Console.ReadLine());

        int currentYear = DateTime.Now.Year;
        // Your code here — compute age and print greeting
    }
}
```

---

## Exercise 2: Number Guessing Game
**Difficulty:** ★★☆☆☆  
**Concepts:** `Random`, `while` loop, conditionals, user input

### Description
Write a number guessing game. The program generates a random number between 1 and 100. The user guesses, and the program responds with "Too high", "Too low", or "Correct!". Track and display the number of attempts.

### Starter Code
```csharp
using System;

class Program {
    static void Main() {
        Random random = new Random();
        int target = random.Next(1, 101);
        int guess = 0;
        int attempts = 0;

        Console.WriteLine("I'm thinking of a number between 1 and 100.");

        // Your code here — game loop
    }
}
```

---

## Exercise 3: List Operations
**Difficulty:** ★★☆☆☆  
**Concepts:** `List<T>`, `foreach`, LINQ basics (optional)

### Description
Create a `List<int>` with at least 10 numbers. Write methods to:
1. Find and return all even numbers.
2. Compute the sum of all numbers.
3. Double each number and return the new list.

### Starter Code
```csharp
using System;
using System.Collections.Generic;

class Program {
    static List<int> GetEvens(List<int> numbers) {
        // Your code here
        return new List<int>();
    }

    static int GetSum(List<int> numbers) {
        // Your code here
        return 0;
    }

    static List<int> DoubleAll(List<int> numbers) {
        // Your code here
        return new List<int>();
    }

    static void Main() {
        List<int> nums = new List<int> { 3, 8, 15, 22, 7, 10, 33, 6, 41, 18 };
        Console.WriteLine("Evens: " + string.Join(", ", GetEvens(nums)));
        Console.WriteLine("Sum: " + GetSum(nums));
        Console.WriteLine("Doubled: " + string.Join(", ", DoubleAll(nums)));
    }
}
```

---

## Exercise 4: Palindrome Sentence Checker
**Difficulty:** ★★☆☆☆  
**Concepts:** `string` methods, `StringBuilder`, `char` operations

### Description
Write a function `bool IsPalindrome(string text)` that checks if a string is a palindrome, ignoring case, spaces, and punctuation. Use `char.IsLetterOrDigit()` to filter characters.

### Starter Code
```csharp
using System;
using System.Text;

class Program {
    static bool IsPalindrome(string text) {
        // Your code here — clean and check
        return false;
    }

    static void Main() {
        Console.WriteLine(IsPalindrome("racecar"));                       // True
        Console.WriteLine(IsPalindrome("A man, a plan, a canal: Panama")); // True
        Console.WriteLine(IsPalindrome("hello"));                         // False
    }
}
```

---

## Exercise 5: Bank Account Class
**Difficulty:** ★★☆☆☆  
**Concepts:** Classes, properties, methods, encapsulation, constructors

### Description
Create a `BankAccount` class with:
- Properties: `AccountNumber` (string, read-only), `HolderName` (string), `Balance` (decimal, read-only)
- Methods: `Deposit(decimal amount)`, `Withdraw(decimal amount)` (check sufficient funds)
- Constructor that sets account number and holder name, initial balance 0
- `ToString()` override that formats account info

### Starter Code
```csharp
using System;

class BankAccount {
    public string AccountNumber { get; }
    public string HolderName { get; set; }
    public decimal Balance { get; private set; }

    public BankAccount(string accNum, string holder) {
        AccountNumber = accNum;
        HolderName = holder;
        Balance = 0;
    }

    // Your code here — Deposit, Withdraw, ToString
}

class Program {
    static void Main() {
        BankAccount acc = new BankAccount("ACC12345", "John Doe");
        acc.Deposit(500);
        acc.Withdraw(200);
        Console.WriteLine(acc);
    }
}
```

---

> **Tip:** Use `dotnet new console` to create a project, or compile with `mcs program.cs && mono program.exe` on Mono. Use `dotnet run` with .NET SDK.
