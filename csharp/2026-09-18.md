# C# — Basic Exercises

Date: 2026-09-18 | Level: Beginner | Sources: W3Schools, Microsoft Learn, GeeksforGeeks, Programiz (adapted)

## Exercise 1: Temperature Converter
- **Difficulty:** Easy
- **Description:** Write two `static` methods `CelsiusToFahrenheit(double c)` and `FahrenheitToCelsius(double f)` using `F = C * 9.0/5.0 + 32`. In `Main`, print a table from 0°C to 40°C in steps of 10.
- **Concepts:** classes, `static` methods, `double` arithmetic, `for` loop, string interpolation
- **Starter code:**
```csharp
using System;

class TemperatureConverter
{
    static double CelsiusToFahrenheit(double c) => c * 9.0 / 5.0 + 32.0;

    static double FahrenheitToCelsius(double f)
    {
        // TODO: return (f - 32.0) * 5.0 / 9.0;
        return 0;
    }

    static void Main()
    {
        for (int c = 0; c <= 40; c += 10)
        {
            // TODO: Console.WriteLine($"{c}C = {CelsiusToFahrenheit(c)}F");
        }
    }
}
```

## Exercise 2: Count Vowels
- **Difficulty:** Easy
- **Description:** Write a `static` method `CountVowels(string s)` that counts vowels (a, e, i, o, u) case-insensitively using `ToLower()` and a `foreach` loop. Test with `"Hello World"` (3) and `"CSharp"` (1).
- **Concepts:** `string`, `ToLower()`, `foreach`, `Contains()` or `switch`, conditionals
- **Starter code:**
```csharp
using System;

class VowelCounter
{
    static int CountVowels(string s)
    {
        string vowels = "aeiou";
        int count = 0;
        foreach (char ch in s.ToLower())
        {
            // TODO: increment count if vowels.Contains(ch)
        }
        return count;
    }

    static void Main()
    {
        Console.WriteLine(CountVowels("Hello World")); // 3
    }
}
```

## Exercise 3: Fibonacci Sequence
- **Difficulty:** Easy
- **Description:** Print the first N terms of the Fibonacci sequence using a loop (0, 1, 1, 2, 3, 5, ...). Then write a recursive `Fib(int n)` that returns the nth term. Test `Fib(10)` → 55.
- **Concepts:** `for` loop, recursion, methods, `long` type
- **Starter code:**
```csharp
using System;

class Fibonacci
{
    static long Fib(int n)
    {
        if (n <= 1) return n;
        // TODO: return Fib(n - 1) + Fib(n - 2);
        return 0;
    }

    static void Main()
    {
        int n = 10;
        long a = 0, b = 1;
        for (int i = 0; i < n; i++)
        {
            Console.Write(a + " ");
            // TODO: update a and b
        }
        Console.WriteLine("\n10th term: " + Fib(10)); // 55
    }
}
```

## Exercise 4: Largest Element in an Array
- **Difficulty:** Easy
- **Description:** Write a `static` method `Largest(int[] arr)` that returns the biggest value in an integer array without using LINQ's `Max()`. Return `int.MinValue` for an empty array.
- **Concepts:** arrays, `foreach` loop, comparison, edge cases
- **Starter code:**
```csharp
using System;

class LargestElement
{
    static int Largest(int[] arr)
    {
        if (arr.Length == 0) return int.MinValue;
        int biggest = arr[0];
        foreach (int n in arr)
        {
            // TODO: update biggest if n is larger
        }
        return biggest;
    }

    static void Main()
    {
        int[] nums = { 3, 41, 12, 9, 74, 15 };
        Console.WriteLine(Largest(nums)); // 74
    }
}
```

## Exercise 5: Simple Calculator
- **Difficulty:** Easy
- **Description:** Build a calculator that reads two `double` values and an operator (`+`, `-`, `*`, `/`) and prints the result using a `switch` statement. Check for division by zero.
- **Concepts:** `Console.ReadLine`, `double.Parse`, `switch`, division by zero handling
- **Starter code:**
```csharp
using System;

class Calculator
{
    static void Main()
    {
        Console.Write("First number: ");
        double a = double.Parse(Console.ReadLine());
        Console.Write("Operator (+, -, *, /): ");
        string op = Console.ReadLine();
        Console.Write("Second number: ");
        double b = double.Parse(Console.ReadLine());

        switch (op)
        {
            case "+":
                Console.WriteLine(a + b);
                break;
            // TODO: add cases for '-', '*', '/'
            // TODO: for '/', check b == 0 and print an error
            default:
                Console.WriteLine("Invalid operator");
                break;
        }
    }
}
```
