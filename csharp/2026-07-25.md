# C# Basic Coding Exercises - 2026-07-25

---

## Exercise 1: Hello, World! & String Interpolation
- **Description**: Write a program that asks the user for their name and age, then prints a personalized greeting using string interpolation. The program should read input via `Console.ReadLine()`, convert the age string to an integer with `int.Parse()`, and print a message like `"Hello Alice! You are 25 years old."`. This exercise introduces basic console I/O, type conversion, and string interpolation.
- **Difficulty**: Beginner
- **Concepts**: `Console.WriteLine`, `Console.ReadLine`, `int.Parse`, string interpolation (`$""`), variables
- **Example/Starter Code**:
```csharp
using System;

class Program
{
    static void Main()
    {
        // TODO: Ask the user for their name
        Console.Write("Enter your name: ");
        string name = Console.ReadLine();

        // TODO: Ask for their age and convert to an integer
        Console.Write("Enter your age: ");

        // TODO: Print a greeting using string interpolation
        Console.WriteLine($"Hello {name}! You are ...");
    }
}
```

---

## Exercise 2: Even or Odd Checker (if-else)
- **Description**: Create a program that reads an integer from the user and determines whether it is even or odd. Use the modulo operator `%` and an `if-else` statement to check if the number is divisible by 2. Print a message like `"The number 7 is odd."` or `"The number 8 is even."`. Handle the edge case of zero, which is technically even.
- **Difficulty**: Beginner
- **Concepts**: `if-else`, modulo operator (`%`), `int.Parse`, `Console.WriteLine`, conditionals
- **Example/Starter Code**:
```csharp
using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter a number: ");
        int number = int.Parse(Console.ReadLine());

        // TODO: Check if the number is even or odd using %
        // Hint: number % 2 == 0 means even
    }
}
```

---

## Exercise 3: Multiplication Table (for loop)
- **Description**: Write a program that prints the multiplication table (1 through 10) for a number entered by the user. Use a `for` loop that runs from 1 to 10, multiplying the loop variable by the user's number each iteration. Format each line as `"7 x 1 = 7"`, `"7 x 2 = 14"`, etc. This builds comfort with `for` loops and formatted output.
- **Difficulty**: Beginner
- **Concepts**: `for` loop, multiplication, string formatting, `Console.ReadLine`, `int.Parse`
- **Example/Starter Code**:
```csharp
using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter a number: ");
        int n = int.Parse(Console.ReadLine());

        // TODO: Use a for loop from 1 to 10
        // TODO: Print "n x i = (n * i)" for each iteration
    }
}
```

---

## Exercise 4: Find the Maximum in an Array
- **Description**: Define an integer array with at least 5 numbers (e.g., `{ 23, 45, 12, 89, 34 }`). Write code that loops through the array to find and print the largest value. Then extend your solution to also find the minimum value. Do NOT use built-in methods like `Array.Max()` — implement the logic yourself with a `foreach` loop and an `if` condition.
- **Difficulty**: Beginner
- **Concepts**: Arrays, `foreach` loop, `if` conditionals, comparison operators, algorithm basics
- **Example/Starter Code**:
```csharp
using System;

class Program
{
    static void Main()
    {
        int[] numbers = { 23, 45, 12, 89, 34 };

        // TODO: Initialize a variable to track the max (hint: use numbers[0])
        // TODO: Loop through the array with foreach
        // TODO: If current element > max, update max
        // TODO: Print the result

        // BONUS: Also find the minimum value
    }
}
```

---

## Exercise 5: Simple To-Do List with `List<T>`
- **Description**: Build a minimal to-do list manager that uses `List<string>`. The program should present a menu with three options: (1) Add a task, (2) List all tasks, (3) Exit. Use a `while` loop so the menu repeats until the user chooses exit. Implement each option: adding pushes a string onto the list, listing prints each item with an index using a `for` loop, and exit breaks out of the loop.
- **Difficulty**: Beginner
- **Concepts**: `List<T>`, `while` loop, `switch` or `if-else`, `Console.ReadLine`, menu-driven programs, `Add()`, `Count`, `for` loop with indexing
- **Example/Starter Code**:
```csharp
using System;
using System.Collections.Generic;

class Program
{
    static void Main()
    {
        List<string> tasks = new List<string>();
        bool running = true;

        while (running)
        {
            Console.WriteLine("\n--- To-Do Menu ---");
            Console.WriteLine("1. Add task");
            Console.WriteLine("2. List tasks");
            Console.WriteLine("3. Exit");
            Console.Write("Choose an option: ");

            string choice = Console.ReadLine();

            // TODO: Use if-else or switch to handle each option
            // Option 1: Ask for a task description, then tasks.Add(...)
            // Option 2: Loop through tasks and print each with its index
            // Option 3: Set running = false (or use break)
        }
    }
}
```

---

*Exercises sourced from standard C# beginner curriculum concepts. For interactive practice, see:*
- [W3Schools C# Exercises](https://www.w3schools.com/cs/cs_exercises.php)
- [Microsoft Learn — C# for Beginners](https://learn.microsoft.com/en-us/dotnet/csharp/)
- [Exercism C# Track](https://exercism.org/tracks/csharp)
- [GeeksforGeeks — C# Basic Programs](https://www.geeksforgeeks.org/c-sharp-basic-programs/)
