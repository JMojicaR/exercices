# C# Basic Exercises — 2026-08-09

> **Level:** Beginner (Basic)  
> **Sources:** W3Resource, W3Schools, PYnative, Exercism  
> **Topics:** Console I/O, Variables, Arithmetic, Conditionals, Loops, Arrays

---

## Exercise 1: Hello, You!

### Problem
Write a C# console program that asks the user for their name and then prints a personalized greeting: `"Hello, <name>! Welcome to C#."`

### Concepts
- `Console.WriteLine()`, `Console.ReadLine()`
- String variables
- String concatenation / interpolation

### Example/Starter Code
```csharp
using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter your name: ");
        string name = Console.ReadLine();

        // TODO: Print a greeting using the name

    }
}
```

---

## Exercise 2: Sum and Average of Three Numbers

### Problem
Write a C# program that reads three integers from the user, computes their sum and average (as a `double`), and prints both results.

### Concepts
- Reading multiple inputs
- Arithmetic operators (`+`, `/`)
- Type casting (`double`)
- Formatted output

### Example/Starter Code
```csharp
using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter first number: ");
        int a = int.Parse(Console.ReadLine());

        Console.Write("Enter second number: ");
        int b = int.Parse(Console.ReadLine());

        Console.Write("Enter third number: ");
        int c = int.Parse(Console.ReadLine());

        // TODO: Calculate sum and average, then print results

    }
}
```

---

## Exercise 3: Positive, Negative, or Zero

### Problem
Write a C# program that reads a number and prints whether it is positive, negative, or zero using `if-else if-else` statements.

### Concepts
- `if` / `else if` / `else`
- Comparison operators (`>`, `<`, `==`)
- Conditional logic

### Example/Starter Code
```csharp
using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter a number: ");
        double num = double.Parse(Console.ReadLine());

        // TODO: Check if num is positive, negative, or zero and print the result

    }
}
```

---

## Exercise 4: Multiplication Table (1 to 10)

### Problem
Write a C# program that prints the multiplication table of a given number from 1 to 10 using a `for` loop.

### Concepts
- `for` loops
- Loop control variables
- Formatted output with `Console.WriteLine()`

### Example/Starter Code
```csharp
using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter a number: ");
        int n = int.Parse(Console.ReadLine());

        // TODO: Print multiplication table from n x 1 to n x 10

    }
}
```

---

## Exercise 5: Sum of Array Elements

### Problem
Write a C# program that creates an integer array with 5 predefined values, calculates the sum of all elements using a `foreach` loop, and prints the result.

### Concepts
- Arrays (`int[]`)
- `foreach` loop
- Accumulator pattern

### Example/Starter Code
```csharp
using System;

class Program
{
    static void Main()
    {
        int[] numbers = { 10, 20, 30, 40, 50 };
        int sum = 0;

        // TODO: Use a foreach loop to compute the sum of the array

        Console.WriteLine($"Sum: {sum}");
    }
}
```
