# C# Basic Exercises — 2026-08-08

> **Level:** Beginner (Basic)  
> **Sources:** W3Schools, W3Resource, PYnative, Exercism  
> **Topics:** Console I/O, Conditionals, Loops, Arrays, Classes, Properties

---

## Exercise 1: Hello, C#! — Console.WriteLine & ReadLine

| Field | Detail |
|-------|--------|
| **Difficulty** | ★☆☆☆☆ |
| **Concepts** | `Console.WriteLine()`, `Console.ReadLine()`, `string`, string interpolation (`$""`) |

**Description:**  
Write a C# console program that asks for the user's name and prints a welcome message using string interpolation.

**Starter Code:**
```csharp
using System;

class Program {
    static void Main() {
        Console.Write("Enter your name: ");
        string name = Console.ReadLine();

        // TODO: Print $"Hello, {name}! Welcome to C#."

    }
}
```

---

## Exercise 2: Calculator with Switch — Arithmetic & Switch

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `switch`, `double.Parse()` / `Convert.ToDouble()`, arithmetic operators |

**Description:**  
Create a calculator that reads two numbers and an operator, then prints the result. Use a `switch` statement and handle division by zero.

**Starter Code:**
```csharp
using System;

class Calculator {
    static void Main() {
        Console.Write("First number: ");
        double a = double.Parse(Console.ReadLine());

        Console.Write("Operator (+, -, *, /): ");
        char op = Console.ReadLine()[0];

        Console.Write("Second number: ");
        double b = double.Parse(Console.ReadLine());

        // TODO: switch on op, compute & print result, handle / 0
    }
}
```

---

## Exercise 3: Number Guessing Game — Random & Loops

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `Random`, `while` loop, `int.Parse()`, conditional hints |

**Description:**  
Generate a random number between 1 and 100. Let the user guess with hints ("Too high" / "Too low") until correct. Count the attempts.

**Starter Code:**
```csharp
using System;

class GuessingGame {
    static void Main() {
        Random rnd = new Random();
        int secret = rnd.Next(1, 101);
        int guess = 0;
        int attempts = 0;

        Console.WriteLine("Guess a number between 1 and 100!");

        // TODO: Loop until correct, give hints, count attempts

        Console.WriteLine($"Correct! It took you {attempts} attempts.");
    }
}
```

---

## Exercise 4: Array Sorting & Searching — Arrays & LINQ

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `int[]`, `Array.Sort()`, `Array.Reverse()`, `for`/`foreach`, `Array.IndexOf()` |

**Description:**  
Given an integer array, sort it in ascending order, then in descending order. Also find and print the index of a specific value.

**Starter Code:**
```csharp
using System;

class ArrayDemo {
    static void Main() {
        int[] numbers = { 42, 12, 8, 99, 23, 5, 67 };

        // TODO: Sort ascending, print
        // TODO: Reverse (descending), print
        // TODO: Find index of value 23, print it
    }
}
```

---

## Exercise 5: Bank Account Class — OOP & Properties

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★★☆☆ |
| **Concepts** | `class`, properties (`{ get; private set; }`), constructor, methods, validation |

**Description:**  
Create a `BankAccount` class with:
- Auto-property `Owner` (string, get only)
- Auto-property `Balance` (decimal, get; private set)
- Constructor
- Methods: `Deposit(decimal amount)`, `Withdraw(decimal amount)` (check for sufficient funds)

**Starter Code:**
```csharp
using System;

class BankAccount {
    public string Owner { get; }
    public decimal Balance { get; private set; }

    public BankAccount(string owner, decimal initial) {
        // TODO
    }

    public void Deposit(decimal amount) {
        // TODO
    }

    public bool Withdraw(decimal amount) {
        // TODO: Return false if insufficient funds
    }
}

class Program {
    static void Main() {
        var acc = new BankAccount("Alice", 1000m);
        acc.Deposit(500);
        Console.WriteLine(acc.Withdraw(2000) ? "Withdrew" : "Insufficient funds");
        Console.WriteLine($"{acc.Owner}: ${acc.Balance}");
    }
}
```
