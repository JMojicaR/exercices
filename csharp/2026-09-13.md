# C# — Basic Exercises

Date: 2026-09-13 | Level: Beginner | Sources: W3Schools, GeeksforGeeks, Programiz (adapted)

## Exercise 1: Even or Odd
- **Difficulty:** Easy
- **Description:** Read an integer with `Console.ReadLine()` and print whether it is "Even" or "Odd" using the modulo operator `%`.
- **Concepts:** `Console.WriteLine`, `int.Parse`, `if`/`else`, modulo `%`
- **Starter code:**
```csharp
using System;

class EvenOdd {
    static void Main() {
        Console.Write("Enter a number: ");
        int num = int.Parse(Console.ReadLine());
        // TODO: print "Even" or "Odd"
    }
}
```

## Exercise 2: Factorial
- **Difficulty:** Easy
- **Description:** Compute the factorial of `n` using a `for` loop and a `long` accumulator.
- **Concepts:** `for` loop, `long`, `Console.WriteLine`
- **Starter code:**
```csharp
using System;

class Factorial {
    static void Main() {
        Console.Write("Enter n: ");
        int n = int.Parse(Console.ReadLine());
        long fact = 1;
        for (int i = 1; i <= n; i++) {
            // TODO: fact *= i;
        }
        Console.WriteLine(fact);
    }
}
```

## Exercise 3: Palindrome Check
- **Difficulty:** Easy
- **Description:** Write `IsPalindrome(string s)` that returns `true` if a string reads the same forwards and backwards, ignoring case and spaces. Reverse the string with a `char[]` and `Array.Reverse`, or a manual loop.
- **Concepts:** `string`, `char[]`, `Array.Reverse`, `ToLower()`, `Replace()`
- **Starter code:**
```csharp
using System;

class Palindrome {
    static bool IsPalindrome(string s) {
        string cleaned = s.ToLower().Replace(" ", "");
        // TODO: compare cleaned with its reverse
        return false;
    }

    static void Main() {
        Console.WriteLine(IsPalindrome("racecar")); // True
        Console.WriteLine(IsPalindrome("hello"));   // False
    }
}
```

## Exercise 4: Sum of Digits
- **Difficulty:** Easy
- **Description:** Sum the digits of an integer using a `while` loop with `% 10` and `/ 10` (e.g. `1234` → `10`).
- **Concepts:** `while` loop, modulo `%`, integer division `/`
- **Starter code:**
```csharp
using System;

class SumDigits {
    static void Main() {
        int num = int.Parse(Console.ReadLine());
        int sum = 0;
        while (num > 0) {
            // TODO: sum += num % 10; num /= 10;
        }
        Console.WriteLine(sum);
    }
}
```

## Exercise 5: Multiplication Table
- **Difficulty:** Easy
- **Description:** Print the multiplication table for a number from 1 to 10 using a `for` loop and string interpolation.
- **Concepts:** `for` loop, string interpolation `$"..."`, `Console.WriteLine`
- **Starter code:**
```csharp
using System;

class Table {
    static void Main() {
        int n = int.Parse(Console.ReadLine());
        for (int i = 1; i <= 10; i++) {
            // TODO: Console.WriteLine($"{n} x {i} = {n * i}");
        }
    }
}
```
