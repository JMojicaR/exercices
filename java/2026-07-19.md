# Java Basic-Level Coding Exercises
**Date:** 2026-07-19
**Level:** Basic
**Source References:** W3Schools, Programiz, GeeksforGeeks, Exercism

---

## Exercise 1: Factorial Calculator

**Title:** Calculate Factorial of a Number

**Description:**  
Write a method `factorial(int n)` that returns the factorial of a given non-negative integer `n`. Handle the edge cases: factorial of 0 is 1, and throw an `IllegalArgumentException` for negative inputs. Use both iterative and recursive approaches.

**Concepts Practiced:** Methods, loops, recursion, exception handling, conditionals

**Difficulty:** Basic

**Starter Code:**
```java
public class Factorial {
    public static long factorial(int n) {
        // Return n! (n factorial)
        // Throw IllegalArgumentException for n < 0
        return 0;
    }

    public static void main(String[] args) {
        System.out.println(factorial(5));   // Expected: 120
        System.out.println(factorial(0));   // Expected: 1
        System.out.println(factorial(10));  // Expected: 3628800
        // System.out.println(factorial(-1)); // Should throw exception
    }
}
```

---

## Exercise 2: Prime Number Checker

**Title:** Check if a Number is Prime

**Description:**  
Write a method `isPrime(int n)` that returns `true` if the number is a prime number, and `false` otherwise. A prime number is greater than 1 and divisible only by 1 and itself. Optimize by only checking up to `sqrt(n)`.

**Concepts Practiced:** Loops, conditionals, `Math.sqrt()`, optimization, boolean logic

**Difficulty:** Basic

**Starter Code:**
```java
public class PrimeChecker {
    public static boolean isPrime(int n) {
        // Return true if n is prime, false otherwise
        // Numbers <= 1 are not prime
        return false;
    }

    public static void main(String[] args) {
        System.out.println(isPrime(2));    // Expected: true
        System.out.println(isPrime(17));   // Expected: true
        System.out.println(isPrime(4));    // Expected: false
        System.out.println(isPrime(1));    // Expected: false
        System.out.println(isPrime(97));   // Expected: true
    }
}
```

---

## Exercise 3: ArrayList Operations

**Title:** Basic ArrayList Manipulation

**Description:**  
Write a program that demonstrates basic `ArrayList` operations: add elements, get elements by index, remove elements, check if an element exists, and iterate using both a traditional `for` loop and an enhanced `for-each` loop. Print the list state after each operation.

**Concepts Practiced:** `ArrayList<T>`, generics, iteration, collections framework, method calls

**Difficulty:** Basic

**Starter Code:**
```java
import java.util.ArrayList;

public class ArrayListDemo {
    public static void main(String[] args) {
        // Create an ArrayList<String> of fruits
        // Add "Apple", "Banana", "Orange", "Mango", "Grape"
        // Remove "Banana" by value
        // Print element at index 1
        // Check if "Mango" is in the list
        // Print all elements using both for-loop and for-each
        // Print the final size
    }
}
```

---

## Exercise 4: Simple Calculator with Switch

**Title:** Command-Line Calculator

**Description:**  
Write a simple calculator that takes two numbers and an operator (`+`, `-`, `*`, `/`) and returns the result. Use a `switch` statement to handle each operation. Handle division by zero and invalid operators gracefully.

**Concepts Practiced:** `switch` statements, user input (`Scanner`), arithmetic, error handling

**Difficulty:** Basic

**Starter Code:**
```java
import java.util.Scanner;

public class SimpleCalculator {
    public static double calculate(double a, double b, char operator) {
        // Use switch on operator and return the result
        // Handle division by zero
        return 0;
    }

    public static void main(String[] args) {
        System.out.println(calculate(10, 5, '+'));  // Expected: 15.0
        System.out.println(calculate(10, 5, '-'));  // Expected: 5.0
        System.out.println(calculate(10, 5, '*'));  // Expected: 50.0
        System.out.println(calculate(10, 5, '/'));  // Expected: 2.0
        System.out.println(calculate(10, 0, '/'));  // Handle gracefully
    }
}
```

---

## Exercise 5: Anagram Detector

**Title:** Check if Two Strings Are Anagrams

**Description:**  
Write a method `areAnagrams(String s1, String s2)` that returns `true` if the two strings are anagrams of each other (contain the same characters in any order), ignoring case and whitespace. Return `false` otherwise.

**Concepts Practiced:** String methods (`toLowerCase()`, `toCharArray()`), `Arrays.sort()`, character arrays, comparison

**Difficulty:** Basic

**Starter Code:**
```java
import java.util.Arrays;

public class AnagramDetector {
    public static boolean areAnagrams(String s1, String s2) {
        // Return true if s1 and s2 are anagrams (case-insensitive, ignore spaces)
        return false;
    }

    public static void main(String[] args) {
        System.out.println(areAnagrams("listen", "silent"));        // Expected: true
        System.out.println(areAnagrams("hello", "world"));          // Expected: false
        System.out.println(areAnagrams("Dormitory", "Dirty Room")); // Expected: true
        System.out.println(areAnagrams("Astronomer", "Moon starer"));// Expected: true
        System.out.println(areAnagrams("abc", "ab"));               // Expected: false
    }
}
```

---

*Generated by Hermes Agent — Cron Job: Programming Exercise Collection*
