# Java Basic Exercises — 2026-08-08

> **Level:** Beginner (Basic)  
> **Sources:** GeeksforGeeks, W3Resource, W3Schools, Exercism  
> **Topics:** Classes, Scanner, Conditionals, Loops, Arrays, OOP

---

## Exercise 1: Hello, Java! — Print & Scanner Input

| Field | Detail |
|-------|--------|
| **Difficulty** | ★☆☆☆☆ |
| **Concepts** | `System.out.println()`, `Scanner`, `public static void main`, class structure |

**Description:**  
Write a Java program that asks for the user's name and prints `"Hello, <name>! Welcome to Java."`

**Starter Code:**
```java
import java.util.Scanner;

public class Greeting {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter your name: ");
        String name = scanner.nextLine();

        // TODO: Print the greeting

        scanner.close();
    }
}
```

---

## Exercise 2: Basic Calculator — Switch & Arithmetic

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `switch` statement, `Scanner`, `double`, arithmetic operators |

**Description:**  
Build a calculator that reads two numbers and an operator (`+`, `-`, `*`, `/`) and prints the result.  
Use a `switch` statement. Handle division by zero.

**Starter Code:**
```java
import java.util.Scanner;

public class Calculator {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter first number: ");
        double a = sc.nextDouble();

        System.out.print("Enter operator (+, -, *, /): ");
        char op = sc.next().charAt(0);

        System.out.print("Enter second number: ");
        double b = sc.nextDouble();

        // TODO: Use switch to compute and print result; handle / by zero

        sc.close();
    }
}
```

---

## Exercise 3: Factorial Calculator — Loops & Recursion

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `for` loop, `long`, recursion, methods |

**Description:**  
Write a Java class with two methods to compute factorial: one using a loop and one using recursion.  
Test with input `n = 5` (expected: 120).

**Starter Code:**
```java
public class Factorial {
    // TODO: factorialLoop(int n) — using a for loop
    // TODO: factorialRecursive(int n) — using recursion

    public static void main(String[] args) {
        int n = 5;
        System.out.println("Loop: " + factorialLoop(n));       // 120
        System.out.println("Recursive: " + factorialRecursive(n)); // 120
    }
}
```

---

## Exercise 4: Array Operations — Arrays & Loops

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | Arrays, `for`/`for-each` loops, `int[]` |

**Description:**  
Given an array of integers, find and print:
- The sum of all elements
- The maximum value
- The minimum value

**Starter Code:**
```java
public class ArrayOps {
    public static void main(String[] args) {
        int[] numbers = {12, 5, 8, 21, 3, 15, 7};

        // TODO: Compute and print sum, max, min
    }
}
```

---

## Exercise 5: Student Class — Basic OOP

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★★☆☆ |
| **Concepts** | Classes, constructors, getters, `ArrayList`, `toString()` |

**Description:**  
Create a `Student` class with fields `name` (String) and `grade` (double). Add a constructor, getters, and a `toString()` method.  
In `main()`, create a list of 3 students and print them.

**Starter Code:**
```java
import java.util.ArrayList;

class Student {
    private String name;
    private double grade;

    // TODO: Constructor
    // TODO: Getters
    // TODO: toString() → "Student{name='Alice', grade=92.5}"
}

public class StudentDemo {
    public static void main(String[] args) {
        ArrayList<Student> students = new ArrayList<>();
        // TODO: Add 3 students and print them
    }
}
```
