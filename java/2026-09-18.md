# Java — Basic Exercises

Date: 2026-09-18 | Level: Beginner | Sources: W3Schools, GeeksforGeeks, Programiz (adapted)

## Exercise 1: Temperature Converter
- **Difficulty:** Easy
- **Description:** Write a class with two `static` methods `celsiusToFahrenheit(double c)` and `fahrenheitToCelsius(double f)` using `F = C * 9/5 + 32`. In `main`, print a table from 0°C to 40°C in steps of 10.
- **Concepts:** classes, `static` methods, arithmetic, `for` loop, `System.out.printf`
- **Starter code:**
```java
public class TemperatureConverter {
    public static double celsiusToFahrenheit(double c) {
        return c * 9 / 5 + 32;
    }

    public static double fahrenheitToCelsius(double f) {
        // TODO: return (f - 32) * 5 / 9
        return 0;
    }

    public static void main(String[] args) {
        for (int c = 0; c <= 40; c += 10) {
            // TODO: print c + "C = " + celsiusToFahrenheit(c) + "F"
        }
    }
}
```

## Exercise 2: Count Vowels
- **Difficulty:** Easy
- **Description:** Write a `static` method `countVowels(String s)` that counts vowels (a, e, i, o, u) case-insensitively. Use `String.toLowerCase()` and a loop. Test with `"Hello World"` (3) and `"Java"` (2).
- **Concepts:** `String` methods, `toLowerCase()`, `charAt()`, loops, `switch` or `if`
- **Starter code:**
```java
public class VowelCounter {
    public static int countVowels(String s) {
        String lower = s.toLowerCase();
        int count = 0;
        for (int i = 0; i < lower.length(); i++) {
            char ch = lower.charAt(i);
            // TODO: increment count if ch is a vowel
        }
        return count;
    }

    public static void main(String[] args) {
        System.out.println(countVowels("Hello World")); // 3
    }
}
```

## Exercise 3: Fibonacci Sequence
- **Difficulty:** Easy
- **Description:** Print the first N terms of the Fibonacci sequence using a loop (0, 1, 1, 2, 3, 5, ...). Then write a recursive `fib(int n)` that returns the nth term. Test `fib(10)` → 55.
- **Concepts:** `for` loop, recursion, methods, `long` for larger values
- **Starter code:**
```java
public class Fibonacci {
    public static long fib(int n) {
        if (n <= 1) return n;
        // TODO: return fib(n - 1) + fib(n - 2)
        return 0;
    }

    public static void main(String[] args) {
        int n = 10;
        long a = 0, b = 1;
        for (int i = 0; i < n; i++) {
            System.out.print(a + " ");
            // TODO: update a and b
        }
        System.out.println("\n10th term: " + fib(10));
    }
}
```

## Exercise 4: Largest Element in an Array
- **Difficulty:** Easy
- **Description:** Write a `static` method `largest(int[] arr)` that returns the biggest value in an integer array without using `Arrays.stream`. Handle an empty array by returning `Integer.MIN_VALUE`.
- **Concepts:** arrays, `for`/`for-each` loops, comparison, edge cases
- **Starter code:**
```java
public class LargestElement {
    public static int largest(int[] arr) {
        if (arr.length == 0) return Integer.MIN_VALUE;
        int biggest = arr[0];
        for (int n : arr) {
            // TODO: update biggest if n is larger
        }
        return biggest;
    }

    public static void main(String[] args) {
        int[] nums = {3, 41, 12, 9, 74, 15};
        System.out.println(largest(nums)); // 74
    }
}
```

## Exercise 5: Simple Calculator
- **Difficulty:** Easy
- **Description:** Build a calculator that reads two numbers and an operator (`+`, `-`, `*`, `/`) from `Scanner` and prints the result using a `switch` statement. For division, check for zero and print an error.
- **Concepts:** `Scanner`, `switch`, `double` arithmetic, division by zero handling
- **Starter code:**
```java
import java.util.Scanner;

public class Calculator {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("First number: ");
        double a = sc.nextDouble();
        System.out.print("Operator (+, -, *, /): ");
        String op = sc.next();
        System.out.print("Second number: ");
        double b = sc.nextDouble();

        switch (op) {
            case "+":
                System.out.println(a + b);
                break;
            // TODO: add cases for '-', '*', '/'
            // TODO: for '/', check b == 0 and print an error
            default:
                System.out.println("Invalid operator");
        }
        sc.close();
    }
}
```
