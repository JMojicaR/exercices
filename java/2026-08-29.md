# Java Basic Exercises — 2026-08-29

> **Level:** Beginner (Basic)
> **Sources:** [PYnative](https://pynative.com/java-exercises-for-beginners/), [w3resource](https://www.w3resource.com/java-exercises/basic/index.php), [GeeksforGeeks](https://www.geeksforgeeks.org/java/java-exercises/)
> **Topics:** Loops, Conditionals, Strings, Arithmetic

---

## Exercise 1: Multiplication Table

**Difficulty:** Basic

### Problem
Write a program that prints the multiplication table of a number read from the user, from 1 to 10, using a `for` loop.

**Given Input:** `5`

**Expected Output:**
```
5 x 1 = 5
5 x 2 = 10
5 x 3 = 15
5 x 4 = 20
5 x 5 = 25
5 x 6 = 30
5 x 7 = 35
5 x 8 = 40
5 x 9 = 45
5 x 10 = 50
```

### Concepts
- `Scanner` for input
- `for` loop
- `System.out.println`

### Example / Starter Code

```java
import java.util.Scanner;

public class MultiplicationTable {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter a number: ");
        int n = sc.nextInt();

        for (int i = 1; i <= 10; i++) {
            System.out.println(n + " x " + i + " = " + (n * i));
        }
        sc.close();
    }
}
```

---

## Exercise 2: Largest of Three Numbers

**Difficulty:** Basic

### Problem
Write a program that reads three integers and prints the largest using `if`/`else if`/`else`.

**Given Input:** `10 25 15`

**Expected Output:**
```
Largest: 25
```

### Concepts
- `if`/`else if`/`else`
- Comparison operators (`>=`, `&&`)
- `Scanner.nextInt()`

### Example / Starter Code

```java
import java.util.Scanner;

public class LargestOfThree {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter three numbers: ");
        int a = sc.nextInt(), b = sc.nextInt(), c = sc.nextInt();

        int largest;
        if (a >= b && a >= c) {
            largest = a;
        } else if (b >= a && b >= c) {
            largest = b;
        } else {
            largest = c;
        }

        System.out.println("Largest: " + largest);
        sc.close();
    }
}
```

---

## Exercise 3: Reverse a String

**Difficulty:** Basic

### Problem
Write a program that reads a string and prints it reversed using `StringBuilder`.

**Given Input:** `hello`

**Expected Output:**
```
Reversed: olleh
```

### Concepts
- `Scanner.nextLine()`
- `StringBuilder.reverse()`
- `toString()`

### Example / Starter Code

```java
import java.util.Scanner;

public class ReverseString {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter a string: ");
        String text = sc.nextLine();

        String reversed = new StringBuilder(text).reverse().toString();
        System.out.println("Reversed: " + reversed);
        sc.close();
    }
}
```

---

## Exercise 4: Leap Year Checker

**Difficulty:** Basic

### Problem
Write a program that reads a year and prints whether it is a leap year using boolean logic.

**Given Input:** `2024`

**Expected Output:**
```
2024 is a leap year
```

### Concepts
- Modulo operator `%`
- Boolean logic with `&&` and `||`
- Ternary operator (`? :`)

### Example / Starter Code

```java
import java.util.Scanner;

public class LeapYear {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter a year: ");
        int year = sc.nextInt();

        boolean isLeap = (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
        System.out.println(year + (isLeap ? " is a leap year" : " is not a leap year"));
        sc.close();
    }
}
```

---

## Exercise 5: Simple Calculator

**Difficulty:** Basic

### Problem
Write a program that reads two numbers and an operator (`+`, `-`, `*`, `/`) and prints the result using a `switch` statement. Handle division by zero and invalid operators.

**Given Input:** `8 2 /`

**Expected Output:**
```
Result: 4.0
```

### Concepts
- `switch` statement
- `double` arithmetic
- Division-by-zero handling

### Example / Starter Code

```java
import java.util.Scanner;

public class Calculator {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter two numbers: ");
        double a = sc.nextDouble(), b = sc.nextDouble();
        System.out.print("Enter operator (+, -, *, /): ");
        char op = sc.next().charAt(0);

        double result;
        switch (op) {
            case '+': result = a + b; break;
            case '-': result = a - b; break;
            case '*': result = a * b; break;
            case '/': result = b != 0 ? a / b : Double.NaN; break;
            default:
                System.out.println("Invalid operator");
                sc.close();
                return;
        }

        System.out.println("Result: " + result);
        sc.close();
    }
}
```
