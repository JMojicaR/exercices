# Java — Basic Exercises

Date: 2026-09-13 | Level: Beginner | Sources: W3Schools, GeeksforGeeks, Programiz (adapted)

## Exercise 1: Even or Odd
- **Difficulty:** Easy
- **Description:** Read an integer using `Scanner` and print whether it is `"Even"` or `"Odd"` using the modulo operator `%`.
- **Concepts:** `Scanner`, `System.in`, `if`/`else`, modulo `%`
- **Starter code:**
```java
import java.util.Scanner;

public class EvenOdd {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter a number: ");
        int num = sc.nextInt();
        // TODO: print "Even" or "Odd"
        sc.close();
    }
}
```

## Exercise 2: Factorial
- **Difficulty:** Easy
- **Description:** Compute the factorial of a number `n` using a `for` loop and a `long` accumulator.
- **Concepts:** `for` loop, `Scanner`, integer/long multiplication, variables
- **Starter code:**
```java
import java.util.Scanner;

public class Factorial {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter n: ");
        int n = sc.nextInt();
        long fact = 1;
        // TODO: loop from 1 to n and multiply fact *= i
        System.out.println(fact);
        sc.close();
    }
}
```

## Exercise 3: Palindrome Check
- **Difficulty:** Easy
- **Description:** Write `isPalindrome(String s)` that returns `true` if a string reads the same forwards and backwards, ignoring case and spaces. Use `StringBuilder.reverse()` or a manual loop.
- **Concepts:** `String`, `StringBuilder`, `charAt()`, `equals()`, `toLowerCase()`, `replace()`
- **Starter code:**
```java
public class Palindrome {
    public static boolean isPalindrome(String s) {
        String cleaned = s.toLowerCase().replace(" ", "");
        // TODO: compare cleaned with its reverse and return the result
        return false;
    }

    public static void main(String[] args) {
        System.out.println(isPalindrome("racecar")); // true
        System.out.println(isPalindrome("hello"));   // false
    }
}
```

## Exercise 4: Sum of Digits
- **Difficulty:** Easy
- **Description:** Sum the digits of an integer using a `while` loop with `% 10` and `/ 10` (e.g. `1234` → `10`).
- **Concepts:** `while` loop, modulo `%`, integer division `/`, `Scanner`
- **Starter code:**
```java
import java.util.Scanner;

public class SumDigits {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int num = sc.nextInt();
        int sum = 0;
        while (num > 0) {
            // TODO: sum += num % 10; then num /= 10;
        }
        System.out.println(sum);
        sc.close();
    }
}
```

## Exercise 5: Multiplication Table
- **Difficulty:** Easy
- **Description:** Print the multiplication table for a number from 1 to 10 using a `for` loop and formatted output.
- **Concepts:** `for` loop, `System.out.printf()`, `Scanner`
- **Starter code:**
```java
import java.util.Scanner;

public class Table {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        for (int i = 1; i <= 10; i++) {
            // TODO: print "n x i = n*i" (try System.out.printf)
        }
        sc.close();
    }
}
```
