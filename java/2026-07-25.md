# Java Basic Coding Exercises - 2026-07-25

> Sources: [W3Schools Java Exercises](https://www.w3schools.com/java/java_exercises.asp), [Programiz Java Examples](https://www.programiz.com/java-programming/examples), [Exercism Java Track](https://exercism.org/tracks/java/exercises)

---

## Exercise 1: Hello, World! & Basic Output
- **Description**: Write a Java program that prints "Hello, World!" to the console. Then extend it to print your name, age (as an integer variable), and a hobby on separate lines using `System.out.println()`. Practice declaring variables of different types (`String`, `int`, `double`) and printing them alongside literal text.
- **Difficulty**: Beginner
- **Concepts**: `main` method, `System.out.println`, variables (`String`, `int`, `double`), string concatenation
- **Example/Starter Code**:
```java
public class HelloWorld {
    public static void main(String[] args) {
        // TODO: Print "Hello, World!"
        // TODO: Declare variables for name (String), age (int), gpa (double)
        // TODO: Print them on separate lines like: "My name is Alice"
    }
}
```

---

## Exercise 2: Simple Calculator with User Input
- **Description**: Build a console-based calculator that reads two numbers and an operator (`+`, `-`, `*`, `/`) from the user using the `Scanner` class. Perform the requested operation and display the result. Handle the case where the user tries to divide by zero with an appropriate error message. Use a `switch` statement to select the operation.
- **Difficulty**: Beginner
- **Concepts**: `Scanner` input, `switch` statement, arithmetic operators, `if-else` (for division-by-zero check), `double` type
- **Example/Starter Code**:
```java
import java.util.Scanner;

public class Calculator {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // TODO: Prompt for and read first number
        // TODO: Prompt for and read operator (+, -, *, /)
        // TODO: Prompt for and read second number
        // TODO: Use switch to perform the operation
        // TODO: Check for division by zero
        // TODO: Print the result

        scanner.close();
    }
}
```

---

## Exercise 3: Student Class (OOP Basics)
- **Description**: Create a `Student` class with private fields: `name` (String), `rollNumber` (int), and `marks` (double). Add a parameterized constructor, getter and setter methods for each field, and a `displayInfo()` method that prints all details. In the `main` method of a separate `StudentDemo` class, create two `Student` objects, set their data, and call `displayInfo()` on each. This exercise practices the core OOP concepts of encapsulation, constructors, and methods.
- **Difficulty**: Beginner
- **Concepts**: Classes and objects, constructors, encapsulation (private fields + public getters/setters), methods, `this` keyword
- **Example/Starter Code**:
```java
class Student {
    // TODO: Declare private fields: name (String), rollNumber (int), marks (double)

    // TODO: Write a parameterized constructor

    // TODO: Write getters and setters for each field

    // TODO: Write displayInfo() method that prints all student details
}

public class StudentDemo {
    public static void main(String[] args) {
        // TODO: Create two Student objects
        // TODO: Use setters or constructor to assign data
        // TODO: Call displayInfo() on each student
    }
}
```

---

## Exercise 4: Array Operations — Sum, Average, and Maximum
- **Description**: Write a program that declares an integer array of 8 elements. Populate it with numbers (either hardcoded or from user input). Write three separate methods: `calculateSum()` that returns the sum of all elements, `calculateAverage()` that returns the average as a `double`, and `findMaximum()` that returns the largest element. Call all three from `main()` and display the results. Use a `for` loop (or enhanced for-each loop) to traverse the array.
- **Difficulty**: Beginner
- **Concepts**: Arrays (declaration, initialization, indexing), `for` loops and enhanced `for-each` loops, methods with return values, type casting (`int` → `double`)
- **Example/Starter Code**:
```java
public class ArrayOperations {
    public static void main(String[] args) {
        int[] numbers = {12, 45, 7, 23, 56, 89, 34, 18};

        // TODO: Call calculateSum(numbers) and print result
        // TODO: Call calculateAverage(numbers) and print result
        // TODO: Call findMaximum(numbers) and print result
    }

    // TODO: Write calculateSum(int[] arr) — returns int

    // TODO: Write calculateAverage(int[] arr) — returns double

    // TODO: Write findMaximum(int[] arr) — returns int
}
```

---

## Exercise 5: String Palindrome Checker
- **Description**: Write a program that reads a string from the user and checks whether it is a palindrome (reads the same forwards and backwards, ignoring case). The check should ignore spaces, punctuation, and letter casing. For example, "A man, a plan, a canal: Panama" and "racecar" should both be recognized as palindromes. Use `StringBuilder` or a character-by-character loop to reverse the string, and use `String` methods like `toLowerCase()`, `replaceAll()` for cleaning the input.
- **Difficulty**: Beginner
- **Concepts**: `String` manipulation (`toLowerCase`, `replaceAll`, `charAt`), `StringBuilder` `.reverse()`, loops, `equals()` comparison, regex basics for cleaning input
- **Example/Starter Code**:
```java
import java.util.Scanner;

public class PalindromeChecker {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // TODO: Read a string from user input
        // TODO: Clean the string: remove non-alphanumeric chars, convert to lowercase
        // TODO: Check if the cleaned string equals its reverse
        // TODO: Print whether it's a palindrome or not

        scanner.close();
    }
}
```
