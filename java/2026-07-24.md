# Java Basic Programming Exercises — 2026-07-24

## Exercise 1: Prime Number Checker
**Difficulty:** ⭐ Basic  
**Concepts:** Loops, conditionals, methods, boolean logic

**Description:** Write a Java method `isPrime(int n)` that returns `true` if the number is prime and `false` otherwise. A prime number is only divisible by 1 and itself. Test with numbers from 1 to 20.

**Starter Code:**
```java
public class PrimeChecker {
    public static boolean isPrime(int n) {
        // Your code here
        // Hint: check divisibility from 2 to sqrt(n)
    }

    public static void main(String[] args) {
        for (int i = 1; i <= 20; i++) {
            System.out.println(i + " is prime? " + isPrime(i));
        }
    }
}
```

**Expected Learning:** `for` loop, `if/else`, method definition, `boolean` return type, optimization with `Math.sqrt()`.

---

## Exercise 2: Factorial Calculator
**Difficulty:** ⭐ Basic  
**Concepts:** Loops, methods, data types (long), recursion (optional)

**Description:** Write a Java method `factorial(int n)` that returns the factorial of `n` (n!). Implement it using both an iterative approach (loop) and a recursive approach. The factorial of n is `n * (n-1) * ... * 2 * 1`.

**Starter Code:**
```java
public class Factorial {
    public static long factorialIterative(int n) {
        // Your loop here
    }

    public static long factorialRecursive(int n) {
        // Your recursion here
    }

    public static void main(String[] args) {
        System.out.println("5! = " + factorialIterative(5));  // 120
        System.out.println("5! = " + factorialRecursive(5));  // 120
        System.out.println("10! = " + factorialIterative(10)); // 3628800
    }
}
```

**Expected Learning:** `for`/`while` loops, method overloading concepts, recursion with base case, `long` data type for large results.

---

## Exercise 3: Count Vowels and Consonants
**Difficulty:** ⭐ Basic  
**Concepts:** String methods, character iteration, counters

**Description:** Write a Java method `countVowelsAndConsonants(String str)` that takes a string, counts how many vowels (`a, e, i, o, u`) and consonants it contains (case-insensitive), and prints the results. Ignore spaces and non-letter characters.

**Starter Code:**
```java
public class VowelCounter {
    public static void countVowelsAndConsonants(String str) {
        int vowels = 0;
        int consonants = 0;

        str = str.toLowerCase();
        // Your code here

        System.out.println("Vowels: " + vowels);
        System.out.println("Consonants: " + consonants);
    }

    public static void main(String[] args) {
        countVowelsAndConsonants("Hello World");
        countVowelsAndConsonants("Java Programming");
    }
}
```

**Expected Learning:** `.toLowerCase()`, `.charAt()`, `for` loop over string length, character comparisons, counter pattern.

---

## Exercise 4: Array Operations — Min, Max, Average
**Difficulty:** ⭐⭐ Basic  
**Concepts:** Arrays, loops, math operations

**Description:** Write a program that takes an array of integers and prints the minimum value, maximum value, and the average (as a `double`). Use a single pass through the array to find min and max. Handle the edge case of an empty array.

**Starter Code:**
```java
public class ArrayStats {
    public static void printStats(int[] arr) {
        if (arr.length == 0) {
            System.out.println("Array is empty");
            return;
        }
        // Your code here
    }

    public static void main(String[] args) {
        int[] numbers = {15, 3, 22, 8, 42, 7, 19};
        printStats(numbers);
        // Expected:
        // Min: 3
        // Max: 42
        // Average: 16.571...
    }
}
```

**Expected Learning:** Array declaration, `arr.length`, loop traversal, min/max tracking pattern, `double` casting for average, edge case handling.

---

## Exercise 5: Simple Banking System (OOP)
**Difficulty:** ⭐⭐ Basic  
**Concepts:** Classes, objects, constructors, methods, encapsulation

**Description:** Create a `BankAccount` class with fields `accountNumber` (String), `accountHolder` (String), and `balance` (double). Include methods: `deposit(double amount)`, `withdraw(double amount)` (prevent overdraft), and `displayBalance()`. Create two accounts and perform operations on them.

**Starter Code:**
```java
public class BankAccount {
    private String accountNumber;
    private String accountHolder;
    private double balance;

    // Constructor
    public BankAccount(String accountNumber, String accountHolder, double initialBalance) {
        // Your code here
    }

    public void deposit(double amount) {
        // Your code here
    }

    public void withdraw(double amount) {
        // Your code here (check if sufficient funds)
    }

    public void displayBalance() {
        // Your code here
    }

    public static void main(String[] args) {
        BankAccount acc1 = new BankAccount("ACC001", "Alice", 1000.0);
        BankAccount acc2 = new BankAccount("ACC002", "Bob", 500.0);

        acc1.deposit(250.0);
        acc1.withdraw(100.0);
        acc1.displayBalance();

        acc2.withdraw(600.0); // Should show error
        acc2.displayBalance();
    }
}
```

**Expected Learning:** Class definition, `private` fields, constructors, method calls, `this` keyword, basic OOP encapsulation.

---

## Source Notes
Exercises are based on universally recognized beginner programming patterns commonly found on W3Schools, Programiz, GeeksforGeeks, and Exercism.
