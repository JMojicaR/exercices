# Java Basic Exercises — 2026-08-09

> Source: [w3resource — Java Basic Programming Exercises](https://www.w3resource.com/java-exercises/basic/index.php)

---

## Exercise 1: Hello and Name Printer

### Problem
Write a Java program to print `'Hello'` on screen and your name on a separate line.

**Expected Output:**
```
Hello
Alexandra Abramov
```

### Concepts
- Class definition and `main` method
- `System.out.println()` for console output
- Basic Java program structure

### Example / Starter Code

```java
public class HelloName {
    public static void main(String[] args) {
        System.out.println("Hello");
        System.out.println("Alexandra Abramov");
    }
}
```

---

## Exercise 2: Sum of Two Numbers

### Problem
Write a Java program to print the sum of two numbers.

**Test Data:**
```
74 + 36
```

**Expected Output:**
```
110
```

### Concepts
- Variable declaration (`int`)
- Arithmetic addition operator (`+`)
- `System.out.println()` with expressions

### Example / Starter Code

```java
public class SumNumbers {
    public static void main(String[] args) {
        int num1 = 74;
        int num2 = 36;
        int sum = num1 + num2;

        System.out.println(sum);
    }
}
```

---

## Exercise 3: Divide Two Numbers

### Problem
Write a Java program to divide two numbers and print them on the screen.

**Test Data:**
```
50 / 3
```

**Expected Output:**
```
16
```

### Concepts
- Integer division vs. floating-point division
- Variable types (`int`, `double`)
- Division operator (`/`)

### Example / Starter Code

```java
public class DivideNumbers {
    public static void main(String[] args) {
        int num1 = 50;
        int num2 = 3;

        // Integer division
        int result = num1 / num2;

        System.out.println(result);

        // For decimal result, use double:
        // double resultDouble = (double) num1 / num2;
        // System.out.println(resultDouble);  // 16.666...
    }
}
```

---

## Exercise 4: Print Results of Operations

### Problem
Write a Java program to print the results of the following operations.

**Test Data:**
- a. `-5 + 8 * 6`
- b. `(55 + 9) % 9`
- c. `20 + -3 * 5 / 8`
- d. `5 + 15 / 3 * 2 - 8 % 3`

**Expected Output:**
```
43
1
19
13
```

### Concepts
- Operator precedence in Java
- Arithmetic operators: `+`, `-`, `*`, `/`, `%`
- Understanding PEMDAS/BODMAS rules
- Parentheses for grouping

### Example / Starter Code

```java
public class Operations {
    public static void main(String[] args) {
        // Operator precedence: * / % before + -
        System.out.println(-5 + 8 * 6);           // 43
        System.out.println((55 + 9) % 9);          // 1
        System.out.println(20 + -3 * 5 / 8);       // 19
        System.out.println(5 + 15 / 3 * 2 - 8 % 3); // 13
    }
}
```

---

## Exercise 5: Product of Two Numbers (User Input)

### Problem
Write a Java program that takes two numbers as input from the user and displays the product of the two numbers.

**Test Data:**
```
Input first number: 25
Input second number: 5
```

**Expected Output:**
```
25 x 5 = 125
```

### Concepts
- `Scanner` class for user input
- Importing Java packages (`java.util.Scanner`)
- Reading integers with `nextInt()`
- Multiplication operator (`*`)

### Example / Starter Code

```java
import java.util.Scanner;

public class ProductInput {
    public static void main(String[] args) {
        // Create a Scanner object to read input
        Scanner scanner = new Scanner(System.in);

        System.out.print("Input first number: ");
        int num1 = scanner.nextInt();

        System.out.print("Input second number: ");
        int num2 = scanner.nextInt();

        int product = num1 * num2;
        System.out.println(num1 + " x " + num2 + " = " + product);

        scanner.close();
    }
}
```
