# Java — Basic Exercises

Date: 2026-09-12 | Level: Beginner | Sources: GeeksforGeeks, W3Schools, Programiz (adapted)

## Exercise 1: Simple Calculator
- **Difficulty:** Easy
- **Description:** Read two numbers and an operator (+, -, *, /) from the user and print the result. Use a `switch` statement on the operator. Guard against division by zero.
- **Concepts:** `Scanner`, `switch`, arithmetic, division-by-zero check
- **Starter code:**
```java
import java.util.Scanner;

public class Calculator {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter a: ");
        double a = sc.nextDouble();
        System.out.print("Enter operator (+, -, *, /): ");
        char op = sc.next().charAt(0);
        System.out.print("Enter b: ");
        double b = sc.nextDouble();

        // TODO: switch on op and print the result
    }
}
```

## Exercise 2: Largest of Three Numbers
- **Difficulty:** Easy
- **Description:** Read three integers and print the largest. Solve with `if`/`else` and also with `Math.max`.
- **Concepts:** `Scanner`, conditionals, `Math.max`
- **Starter code:**
```java
import java.util.Scanner;

public class LargestOfThree {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt(), b = sc.nextInt(), c = sc.nextInt();

        // TODO: print the largest of a, b, c
    }
}
```

## Exercise 3: Count Vowels
- **Difficulty:** Easy
- **Description:** Write a method `countVowels(String s)` that returns the number of vowels in a string, case-insensitively.
- **Concepts:** `String.charAt()`, `toLowerCase()`, loops, methods
- **Starter code:**
```java
public class Vowels {
    static int countVowels(String s) {
        s = s.toLowerCase();
        int count = 0;
        // TODO: loop and count vowels (a, e, i, o, u)
        return count;
    }

    public static void main(String[] args) {
        System.out.println(countVowels("Hello World")); // 3
    }
}
```

## Exercise 4: Fibonacci Sequence
- **Difficulty:** Easy
- **Description:** Print the first N Fibonacci numbers (0, 1, 1, 2, 3, 5, ...) where N is read from the user.
- **Concepts:** loops, variables, `Scanner`
- **Starter code:**
```java
import java.util.Scanner;

public class Fibonacci {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();

        int a = 0, b = 1;
        for (int i = 0; i < n; i++) {
            System.out.println(a);
            // TODO: update a and b
        }
    }
}
```

## Exercise 5: Reverse a String
- **Difficulty:** Easy
- **Description:** Reverse a string two ways: once with `StringBuilder.reverse()` and once with a manual loop.
- **Concepts:** `StringBuilder`, loops, `charAt()`
- **Starter code:**
```java
public class ReverseString {
    public static void main(String[] args) {
        String s = "hello";

        String reversed = new StringBuilder(s).reverse().toString();
        System.out.println(reversed); // "olleh"

        // TODO: also reverse s with a manual loop
    }
}
```
