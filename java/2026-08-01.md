# Java Coding Exercises - 2026-08-01

---

## Exercise 1: Hello, World!

**Description:**  
Write a Java program that prints "Hello, World!" to the console. This classic first program introduces you to the basic structure of a Java class and the `main` method.

**Difficulty:** Basic

**Concepts:**  
- Class declaration
- `main` method signature
- `System.out.println()` for console output

**Starter Code:**
```java
// Exercise 1: Hello, World!

public class HelloWorld {
    public static void main(String[] args) {
        // TODO: Print "Hello, World!" to the console
        System.out.println("Hello, World!");
    }
}
```

---

## Exercise 2: User Input & Greeting

**Description:**  
Write a Java program that asks the user for their name using `Scanner`, then prints a personalized greeting: "Hello, [name]! Welcome to Java!". Also ask for the user's age and print: "You are [age] years old."

**Difficulty:** Basic

**Concepts:**  
- Importing `java.util.Scanner`
- Reading user input (`nextLine()`, `nextInt()`)
- Variables and string concatenation
- Closing the Scanner

**Starter Code:**
```java
// Exercise 2: User Input & Greeting

import java.util.Scanner;

public class Greeting {
    public static void main(String[] args) {
        // TODO: Create a Scanner object for reading input
        Scanner scanner = new Scanner(System.in);

        // TODO: Ask the user for their name and store it
        System.out.print("What is your name? ");

        // TODO: Ask the user for their age and store it

        // TODO: Print a personalized greeting
        // Example: "Hello, Alice! Welcome to Java!"

        // TODO: Print the age
        // Example: "You are 25 years old."

        // TODO: Close the scanner
    }
}
```

---

## Exercise 3: Factorial Calculator

**Description:**  
Write a Java program that calculates the factorial of a number using a `for` loop. The factorial of a number `n` (written as `n!`) is the product of all positive integers from 1 to `n`. For example, `5! = 5 × 4 × 3 × 2 × 1 = 120`. Ask the user for a non-negative integer and print the result.

**Difficulty:** Basic

**Concepts:**  
- For loops
- Accumulator pattern
- `Scanner` for user input
- Long data type for larger results

**Starter Code:**
```java
// Exercise 3: Factorial Calculator

import java.util.Scanner;

public class Factorial {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // TODO: Ask the user for a non-negative integer
        System.out.print("Enter a non-negative integer: ");

        // TODO: Read the number and store it

        // TODO: Calculate factorial using a for loop
        // Initialize a variable (e.g., long factorial = 1)
        // Loop from 1 to n, multiplying the result each time

        // TODO: Print the result
        // Example: "5! = 120"

        scanner.close();
    }
}
```

---

## Exercise 4: Even Numbers in an Array

**Description:**  
Write a Java program that creates an array of integers. Loop through the array using a `for` loop and print only the even numbers. Also calculate and print the sum of those even numbers.

**Difficulty:** Basic

**Concepts:**  
- Array declaration and initialization
- For loops with array indexing
- If conditionals with modulo operator (`%`)
- Accumulator pattern

**Starter Code:**
```java
// Exercise 4: Even Numbers in an Array

public class EvenNumbers {
    public static void main(String[] args) {
        // TODO: Create an array with these values
        int[] numbers = {3, 8, 11, 14, 7, 20, 5, 12};

        // TODO: Initialize a variable to hold the sum of even numbers

        // TODO: Loop through the array
        // Check if each number is even (number % 2 == 0)
        // If even, print it and add it to the sum

        // TODO: Print the total sum of even numbers
        // Example output:
        // Even numbers: 8, 14, 20, 12
        // Sum of even numbers: 54
    }
}
```

---

## Exercise 5: Simple Calculator with Switch

**Description:**  
Write a Java program that acts as a simple calculator. Ask the user for two numbers and an operator (`+`, `-`, `*`, `/`). Use a `switch` statement to perform the appropriate operation and print the result. Handle division by zero with an error message.

**Difficulty:** Basic

**Concepts:**  
- `Scanner` for input
- Switch statements
- Arithmetic operations
- Conditional check for division by zero

**Starter Code:**
```java
// Exercise 5: Simple Calculator with Switch

import java.util.Scanner;

public class Calculator {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // TODO: Ask the user for the first number
        System.out.print("Enter first number: ");

        // TODO: Ask the user for the operator (+, -, *, /)
        System.out.print("Enter operator (+, -, *, /): ");

        // TODO: Ask the user for the second number
        System.out.print("Enter second number: ");

        // TODO: Use a switch statement to perform the operation
        // Handle each operator case (+, -, *, /)
        // For division, check if the second number is zero before dividing

        // TODO: Print the result
        // Example: "10.0 + 5.0 = 15.0"

        scanner.close();
    }
}
```
