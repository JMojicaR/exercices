# Java Basic Coding Exercises — 2026-08-07

> **Level:** Basic / Beginner  
> **Sources:** W3Schools, w3resource, GeeksforGeeks, Exercism  
> **Focus:** Classes, methods, variables, loops, arrays, conditionals, Scanner input

---

## Exercise 1: Hello, User!
**Difficulty:** ★☆☆☆☆  
**Concepts:** Class definition, `main` method, `Scanner`, `System.out.println`

### Description
Write a Java program that prompts the user for their name and age, then prints a personalized greeting. The program must use the `Scanner` class for input.

### Starter Code
```java
import java.util.Scanner;

public class HelloUser {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter your name: ");
        String name = scanner.nextLine();

        System.out.print("Enter your age: ");
        int age = scanner.nextInt();

        // Your code here — print the greeting

        scanner.close();
    }
}
```

---

## Exercise 2: Multiplication Table
**Difficulty:** ★☆☆☆☆  
**Concepts:** `for` loops, formatted output, user input

### Description
Ask the user for a number and print its multiplication table from 1 to 10. Format the output neatly:  
`5 x 1 = 5`, `5 x 2 = 10`, …

### Starter Code
```java
import java.util.Scanner;

public class MultiplicationTable {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter a number: ");
        int num = scanner.nextInt();

        // Your code here — print multiplication table

        scanner.close();
    }
}
```

---

## Exercise 3: Array Reverser
**Difficulty:** ★★☆☆☆  
**Concepts:** Arrays, loops, swapping

### Description
Write a method `reverseArray(int[] arr)` that reverses an integer array **in place** (without creating a new array). Print the array before and after reversing.

### Starter Code
```java
import java.util.Arrays;

public class ArrayReverser {
    public static void reverseArray(int[] arr) {
        // Your code here
    }

    public static void main(String[] args) {
        int[] numbers = {1, 2, 3, 4, 5, 6, 7};
        System.out.println("Before: " + Arrays.toString(numbers));
        reverseArray(numbers);
        System.out.println("After:  " + Arrays.toString(numbers));
        // Expected: [7, 6, 5, 4, 3, 2, 1]
    }
}
```

---

## Exercise 4: Prime Number Checker
**Difficulty:** ★★☆☆☆  
**Concepts:** Methods, conditionals, `for` loops, `Math.sqrt()`

### Description
Write a method `isPrime(int n)` that returns `true` if `n` is a prime number and `false` otherwise. A prime number is only divisible by 1 and itself. Handle edge cases (numbers less than 2 are not prime).

### Starter Code
```java
public class PrimeChecker {
    public static boolean isPrime(int n) {
        // Your code here
        return false;
    }

    public static void main(String[] args) {
        System.out.println(isPrime(2));   // Expected: true
        System.out.println(isPrime(17));  // Expected: true
        System.out.println(isPrime(15));  // Expected: false
        System.out.println(isPrime(1));   // Expected: false
    }
}
```

---

## Exercise 5: Student Grade Calculator
**Difficulty:** ★★☆☆☆  
**Concepts:** Arrays, loops, conditionals, methods

### Description
Create a class `Student` with fields `name` (String) and `grades` (int array). Add a method `getAverage()` that returns the average of the grades, and a method `getLetterGrade()` that returns the letter grade (A: 90+, B: 80-89, C: 70-79, D: 60-69, F: below 60). Test with sample data.

### Starter Code
```java
public class Student {
    private String name;
    private int[] grades;

    public Student(String name, int[] grades) {
        this.name = name;
        this.grades = grades;
    }

    public double getAverage() {
        // Your code here
        return 0.0;
    }

    public char getLetterGrade() {
        // Your code here
        return 'F';
    }

    public static void main(String[] args) {
        int[] scores = {85, 92, 78, 90, 88};
        Student student = new Student("Alice", scores);
        System.out.println("Average: " + student.getAverage());
        System.out.println("Grade: " + student.getLetterGrade());
    }
}
```

---

> **Tip:** Compile with `javac FileName.java` and run with `java FileName`. Practice writing the full class with a `main` method each time.
