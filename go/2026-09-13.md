# Go — Basic Exercises

Date: 2026-09-13 | Level: Beginner | Sources: Go by Example, GeeksforGeeks, Exercism (adapted)

## Exercise 1: FizzBuzz
- **Difficulty:** Easy
- **Description:** Print the numbers 1 to 20. For multiples of 3 print `"Fizz"`, for multiples of 5 print `"Buzz"`, for multiples of both print `"FizzBuzz"`, otherwise print the number.
- **Concepts:** `for` loop, `if`/`else if`/`else`, modulo `%`, `fmt.Println`
- **Starter code:**
```go
package main

import "fmt"

func main() {
    for i := 1; i <= 20; i++ {
        // TODO: apply the FizzBuzz rules and print the correct value
        fmt.Println(i)
    }
}
```

## Exercise 2: Factorial
- **Difficulty:** Easy
- **Description:** Write `factorial(n int) int` using a `for` loop and an accumulator. Factorial of 0 is 1.
- **Concepts:** functions, `for` loop, `int`, return values, `:=` short declaration
- **Starter code:**
```go
package main

import "fmt"

func factorial(n int) int {
    result := 1
    for i := 1; i <= n; i++ {
        // TODO: result *= i
    }
    return result
}

func main() {
    fmt.Println(factorial(5)) // 120
}
```

## Exercise 3: Palindrome Check
- **Difficulty:** Easy
- **Description:** Write `isPalindrome(s string) bool` that returns `true` if a string reads the same forwards and backwards, ignoring case and spaces. Convert to a `[]rune` to handle multi-byte characters safely.
- **Concepts:** `string`, `strings.ToLower`, `strings.ReplaceAll`, `[]rune`, indexing
- **Starter code:**
```go
package main

import (
    "fmt"
    "strings"
)

func isPalindrome(s string) bool {
    s = strings.ToLower(strings.ReplaceAll(s, " ", ""))
    runes := []rune(s)
    for i := 0; i < len(runes)/2; i++ {
        // TODO: compare runes[i] with runes[len(runes)-1-i]
    }
    return true
}

func main() {
    fmt.Println(isPalindrome("racecar")) // true
    fmt.Println(isPalindrome("hello"))   // false
}
```

## Exercise 4: Sum of Digits
- **Difficulty:** Easy
- **Description:** Write `sumDigits(n int) int` that returns the sum of the digits (e.g. `1234` → `10`) using a `for` loop with `% 10` and `/ 10`.
- **Concepts:** `for` loop, modulo `%`, integer division `/`
- **Starter code:**
```go
package main

import "fmt"

func sumDigits(n int) int {
    sum := 0
    for n > 0 {
        // TODO: sum += n % 10; n /= 10;
    }
    return sum
}

func main() {
    fmt.Println(sumDigits(1234)) // 10
}
```

## Exercise 5: Multiplication Table
- **Difficulty:** Easy
- **Description:** Print the multiplication table for a number from 1 to 10 using a `for` loop.
- **Concepts:** `for` loop, `fmt.Printf` formatting
- **Starter code:**
```go
package main

import "fmt"

func main() {
    n := 7
    for i := 1; i <= 10; i++ {
        // TODO: fmt.Printf("%d x %d = %d\n", n, i, n*i)
    }
}
```
