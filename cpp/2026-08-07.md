# C++ Basic Coding Exercises — 2026-08-07

> **Level:** Basic / Beginner  
> **Sources:** W3Schools, w3resource, PYnative, GeeksforGeeks  
> **Focus:** iostream, cin/cout, variables, conditionals, loops, vectors, strings, functions, references

---

## Exercise 1: Simple Calculator
**Difficulty:** ★☆☆☆☆  
**Concepts:** `cin`/`cout`, `switch` statement, arithmetic operators

### Description
Write a program that reads two numbers and an operator (`+`, `-`, `*`, `/`) from the user and performs the corresponding arithmetic operation. Handle division by zero with an error message.

### Starter Code
```cpp
#include <iostream>
using namespace std;

int main() {
    double a, b;
    char op;

    cout << "Enter two numbers and an operator (e.g., 10 5 +): ";
    cin >> a >> b >> op;

    // Your code here — use a switch statement

    return 0;
}
```

---

## Exercise 2: Fibonacci Sequence
**Difficulty:** ★★☆☆☆  
**Concepts:** Loops (`for`), variables, sequence generation

### Description
Print the first N numbers of the Fibonacci sequence, where N is provided by the user. The Fibonacci sequence starts with 0, 1, and each subsequent number is the sum of the two preceding ones: `0, 1, 1, 2, 3, 5, 8, 13, …`

### Starter Code
```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "How many Fibonacci numbers? ";
    cin >> n;

    long long a = 0, b = 1;
    // Your code here — print the first n Fibonacci numbers

    return 0;
}
```

---

## Exercise 3: Vector Statistics
**Difficulty:** ★★☆☆☆  
**Concepts:** `std::vector`, range-based `for` loops, algorithms

### Description
Create a `std::vector<int>` with at least 8 numbers. Compute and print: the sum, average, minimum, and maximum. Use a **range-based for loop** (`for (int x : vec)`) for iteration.

### Starter Code
```cpp
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> numbers = {15, 3, 42, 8, 23, 7, 19, 11};

    int sum = 0;
    int min_val = numbers[0];
    int max_val = numbers[0];

    for (int n : numbers) {
        // Your code here
    }

    cout << "Sum: " << sum << endl;
    cout << "Average: " << (double)sum / numbers.size() << endl;
    cout << "Min: " << min_val << endl;
    cout << "Max: " << max_val << endl;

    return 0;
}
```

---

## Exercise 4: Palindrome String Check
**Difficulty:** ★★☆☆☆  
**Concepts:** `std::string`, references, `for` loop, string indexing

### Description
Write a function `bool isPalindrome(const string &str)` that returns `true` if the input string is a palindrome (case-sensitive, no ignoring of spaces). Test with: `"racecar"` → `true`, `"hello"` → `false`, `"madam"` → `true`.

### Starter Code
```cpp
#include <iostream>
#include <string>
using namespace std;

bool isPalindrome(const string &str) {
    // Your code here
    return false;
}

int main() {
    cout << boolalpha;
    cout << isPalindrome("racecar") << endl;  // Expected: true
    cout << isPalindrome("hello") << endl;    // Expected: false
    cout << isPalindrome("madam") << endl;    // Expected: true
    return 0;
}
```

---

## Exercise 5: Student Class with Getters/Setters
**Difficulty:** ★★☆☆☆  
**Concepts:** Classes, constructors, getters/setters, encapsulation, `std::vector`

### Description
Create a `Student` class with private fields: `name` (string), `id` (int), and `grades` (vector of doubles). Include a constructor, getters/setters for each field, and a method `getGPA()` that returns the average grade. Test in `main()`.

### Starter Code
```cpp
#include <iostream>
#include <vector>
#include <string>
using namespace std;

class Student {
private:
    string name;
    int id;
    vector<double> grades;

public:
    Student(string n, int i) : name(n), id(i) {}

    // Your code here — add getters, setters, getGPA()
};

int main() {
    Student s("Bob", 101);
    s.addGrade(85.0);
    s.addGrade(92.5);
    s.addGrade(78.0);
    cout << "Student: " << s.getName() << endl;
    cout << "GPA: " << s.getGPA() << endl;
    return 0;
}
```

---

> **Tip:** Compile with `g++ -std=c++17 -Wall -o program program.cpp && ./program`. Use `g++` not `gcc` for C++ code.
