# C++ — Basic Exercises

Date: 2026-09-18 | Level: Beginner | Sources: W3Schools, GeeksforGeeks, Programiz, cppreference (adapted)

## Exercise 1: Temperature Converter
- **Difficulty:** Easy
- **Description:** Write two functions `celsiusToFahrenheit(double c)` and `fahrenheitToCelsius(double f)` using `F = C * 9.0/5.0 + 32`. In `main`, print a table from 0°C to 40°C in steps of 10 using `std::cout`.
- **Concepts:** functions, `double` arithmetic, `std::cout`, `for` loop, `std::fixed`/`std::setprecision`
- **Starter code:**
```cpp
#include <iostream>

double celsiusToFahrenheit(double c) {
    return c * 9.0 / 5.0 + 32.0;
}

double fahrenheitToCelsius(double f) {
    // TODO: return (f - 32.0) * 5.0 / 9.0;
    return 0;
}

int main() {
    for (int c = 0; c <= 40; c += 10) {
        // TODO: std::cout << c << "C = " << celsiusToFahrenheit(c) << "F\n";
    }
    return 0;
}
```

## Exercise 2: Count Vowels
- **Difficulty:** Easy
- **Description:** Write a function `int countVowels(const std::string& s)` that counts vowels (a, e, i, o, u) case-insensitively using `std::tolower` and a range-based `for` loop.
- **Concepts:** `std::string`, range-based `for`, `std::tolower` (`<cctype>`), comparison
- **Starter code:**
```cpp
#include <iostream>
#include <string>
#include <cctype>

int countVowels(const std::string& s) {
    int count = 0;
    for (char ch : s) {
        ch = std::tolower(ch);
        // TODO: increment count if ch is a vowel
    }
    return count;
}

int main() {
    std::cout << countVowels("Hello World") << "\n"; // 3
    return 0;
}
```

## Exercise 3: Fibonacci Sequence
- **Difficulty:** Easy
- **Description:** Print the first N terms of the Fibonacci sequence using a loop. Then write a recursive `long long fib(int n)` that returns the nth term (0-indexed). Test `fib(10)` → 55.
- **Concepts:** loops, recursion, `long long`, functions
- **Starter code:**
```cpp
#include <iostream>

long long fib(int n) {
    if (n <= 1) return n;
    // TODO: return fib(n - 1) + fib(n - 2);
    return 0;
}

int main() {
    int n = 10;
    long long a = 0, b = 1;
    for (int i = 0; i < n; i++) {
        std::cout << a << " ";
        // TODO: update a and b
    }
    std::cout << "\n10th term: " << fib(10) << "\n"; // 55
    return 0;
}
```

## Exercise 4: Largest Element in a Vector
- **Difficulty:** Easy
- **Description:** Write a function `int largest(const std::vector<int>& v)` that returns the biggest value in a vector without using `std::max_element`. Return `0` for an empty vector.
- **Concepts:** `std::vector`, range-based `for`, comparison, edge cases
- **Starter code:**
```cpp
#include <iostream>
#include <vector>

int largest(const std::vector<int>& v) {
    if (v.empty()) return 0;
    int biggest = v[0];
    for (int n : v) {
        // TODO: update biggest if n is larger
    }
    return biggest;
}

int main() {
    std::vector<int> nums = {3, 41, 12, 9, 74, 15};
    std::cout << largest(nums) << "\n"; // 74
    return 0;
}
```

## Exercise 5: Simple Calculator
- **Difficulty:** Easy
- **Description:** Build a calculator that reads two `double` values and an operator character (`+`, `-`, `*`, `/`) and prints the result using a `switch` statement. Check for division by zero.
- **Concepts:** `std::cin`, `switch`, `double` arithmetic, division by zero handling
- **Starter code:**
```cpp
#include <iostream>

int main() {
    double a, b;
    char op;

    std::cout << "Enter expression (e.g. 10 + 5): ";
    std::cin >> a >> op >> b;

    switch (op) {
        case '+':
            std::cout << a + b << "\n";
            break;
        // TODO: add cases for '-', '*', '/'
        // TODO: for '/', check b == 0 and print an error
        default:
            std::cout << "Invalid operator\n";
    }
    return 0;
}
```
