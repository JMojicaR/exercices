# C++ Basic Coding Exercises - 2026-07-25

## Exercise 1: Hello World & Basic Output
- **Description**: Write a program that prints "Hello, World!" to the console, then prints your name on a new line. This classic first program introduces the basic structure of a C++ program including the `#include` preprocessor directive, the `main()` function, and the `std::cout` output stream.
- **Difficulty**: Beginner
- **Concepts**: Preprocessor directives, main function, std::cout, standard output, namespace
- **Example/Starter Code**:
```cpp
#include <iostream>
using namespace std;

int main() {
    // Print Hello World and your name here

    return 0;
}
```

---

## Exercise 2: Simple Calculator with User Input and Switch
- **Description**: Create a calculator that takes two numbers and an operator (+, -, *, /) from the user, then performs the selected operation and displays the result. Use `std::cin` to read input, a `switch` statement to handle the operator selection, and handle division by zero with an error message.
- **Difficulty**: Beginner
- **Concepts**: std::cin, input/output, switch statement, arithmetic operators, error handling
- **Example/Starter Code**:
```cpp
#include <iostream>
using namespace std;

int main() {
    double num1, num2;
    char op;

    cout << "Enter first number: ";
    cin >> num1;

    cout << "Enter operator (+, -, *, /): ";
    cin >> op;

    cout << "Enter second number: ";
    cin >> num2;

    // Use a switch statement to perform the operation
    // Handle division by zero!

    return 0;
}
```

---

## Exercise 3: Factorial Using Recursion
- **Description**: Write a recursive function `factorial(int n)` that returns the factorial of a non-negative integer `n` (where n! = n × (n-1) × ... × 1, and 0! = 1). The program should prompt the user for a number, call the recursive function, and display the result. This exercise builds understanding of function definitions, base cases, and the call stack.
- **Difficulty**: Beginner
- **Concepts**: Functions, recursion, base case, return values, function declaration
- **Example/Starter Code**:
```cpp
#include <iostream>
using namespace std;

// Write the factorial function here
int factorial(int n) {
    // Base case: factorial of 0 is 1

    // Recursive case: n * factorial(n-1)
}

int main() {
    int num;

    cout << "Enter a non-negative integer: ";
    cin >> num;

    if (num < 0) {
        cout << "Factorial is not defined for negative numbers." << endl;
    } else {
        cout << num << "! = " << factorial(num) << endl;
    }

    return 0;
}
```

---

## Exercise 4: Working with Vectors — Sum and Average
- **Description**: Write a program that creates a `std::vector<int>` and populates it with 5 integers entered by the user. Then calculate and display the sum and average of the numbers using a range-based `for` loop. This exercise introduces the C++ Standard Template Library (STL) vector container and common iteration patterns.
- **Difficulty**: Beginner
- **Concepts**: std::vector, range-based for loop, vector operations, STL containers, type casting (average)
- **Example/Starter Code**:
```cpp
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> numbers;
    int input;

    cout << "Enter 5 integers:" << endl;
    for (int i = 0; i < 5; i++) {
        cout << "Number " << (i + 1) << ": ";
        cin >> input;
        // Add input to the vector
    }

    // Calculate sum using a range-based for loop

    // Calculate and display the average

    return 0;
}
```

---

## Exercise 5: Basic OOP — Rectangle Class
- **Description**: Define a `Rectangle` class with private member variables `width` and `height`, a parameterized constructor to initialize them, and public member functions `area()` and `perimeter()` that return the computed values. In `main()`, create a `Rectangle` object, call its methods, and display the results. This exercise covers the fundamentals of object-oriented programming in C++.
- **Difficulty**: Beginner
- **Concepts**: Classes, objects, constructors, member functions, encapsulation (private/public), OOP
- **Example/Starter Code**:
```cpp
#include <iostream>
using namespace std;

class Rectangle {
private:
    double width;
    double height;

public:
    // Constructor to initialize width and height


    // Member function to calculate area (width * height)


    // Member function to calculate perimeter (2 * (width + height))

};

int main() {
    // Create a Rectangle object (e.g., width=5.0, height=3.0)

    // Display its area and perimeter

    return 0;
}
```
