# C++ — Basic Exercises

Date: 2026-09-13 | Level: Beginner | Sources: GeeksforGeeks, Programiz, W3Schools (adapted)

## Exercise 1: Even or Odd
- **Difficulty:** Easy
- **Description:** Read an integer with `cin` and print whether it is "Even" or "Odd" using the modulo operator `%`.
- **Concepts:** `cin`/`cout`, `if`/`else`, modulo `%`, `iostream`
- **Starter code:**
```cpp
#include <iostream>
using namespace std;

int main() {
    int num;
    cout << "Enter a number: ";
    cin >> num;
    // TODO: print "Even" or "Odd"
    return 0;
}
```

## Exercise 2: Factorial
- **Difficulty:** Easy
- **Description:** Compute the factorial of `n` using a `for` loop and a `long long` accumulator.
- **Concepts:** `for` loop, `long long`, `cin`/`cout`
- **Starter code:**
```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    long long fact = 1;
    cout << "Enter n: ";
    cin >> n;
    for (int i = 1; i <= n; i++) {
        // TODO: fact *= i;
    }
    cout << fact << endl;
    return 0;
}
```

## Exercise 3: Palindrome Check
- **Difficulty:** Easy
- **Description:** Write `is_palindrome(string s)` that returns `true` if a string reads the same forwards and backwards. Compare the string with its reverse using `std::reverse` or a manual loop.
- **Concepts:** `std::string`, `std::reverse`, `algorithm` header, functions
- **Starter code:**
```cpp
#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

bool is_palindrome(string s) {
    // TODO: compare s with its reverse and return the result
    return false;
}

int main() {
    cout << is_palindrome("racecar") << endl; // 1
    cout << is_palindrome("hello") << endl;   // 0
    return 0;
}
```

## Exercise 4: Sum of Digits
- **Difficulty:** Easy
- **Description:** Sum the digits of an integer using a `while` loop with `% 10` and `/ 10` (e.g. `1234` → `10`).
- **Concepts:** `while` loop, modulo `%`, integer division `/`
- **Starter code:**
```cpp
#include <iostream>
using namespace std;

int main() {
    int num, sum = 0;
    cin >> num;
    while (num > 0) {
        // TODO: sum += num % 10; num /= 10;
    }
    cout << sum << endl;
    return 0;
}
```

## Exercise 5: Multiplication Table
- **Difficulty:** Easy
- **Description:** Print the multiplication table for a number from 1 to 10 using a `for` loop.
- **Concepts:** `for` loop, `cout` formatting, `cin`
- **Starter code:**
```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    for (int i = 1; i <= 10; i++) {
        // TODO: print "n x i = n*i" (e.g. cout << n << " x " << i << " = " << n * i << endl;)
    }
    return 0;
}
```
