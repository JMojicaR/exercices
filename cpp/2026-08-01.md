# C++ Coding Exercises - 2026-08-01

---

## Exercise 1: Hello, World!

**Description:** Write a program that prints "Hello, World!" followed by a newline to the console using C++ streams. This introduces the `iostream` library, the `std::cout` output stream, and the basic structure of a C++ program.

**Difficulty:** Basic

**Concepts:** `std::cout`, `main` function, `#include`, `std::endl`, namespace

**Starter Code:**

```cpp
#include <iostream>

int main() {
    // TODO: Print "Hello, World!" to the console using std::cout

    return 0;
}
```

---

## Exercise 2: Simple Calculator

**Description:** Write a program that reads two numbers (decimals allowed) from the user and prints their sum, difference, product, and quotient. Use `std::cin` for input and `std::cout` for output. If the second number is zero, print an error message instead of performing division.

**Difficulty:** Basic

**Concepts:** `std::cin`, `std::cout`, variables (`double`), arithmetic operators, `if/else`

**Starter Code:**

```cpp
#include <iostream>

int main() {
    double num1, num2;

    std::cout << "Enter first number: ";
    std::cin >> num1;
    std::cout << "Enter second number: ";
    std::cin >> num2;

    // TODO: Calculate and print sum, difference, product, and quotient
    // TODO: Handle division by zero

    return 0;
}
```

---

## Exercise 3: Factorial Calculator

**Description:** Write a program that reads a non-negative integer from the user and calculates its factorial using a function. The factorial of n (n!) is the product of all positive integers from 1 to n, with 0! defined as 1. Use a `long long` return type to handle larger values and print the result.

**Difficulty:** Basic

**Concepts:** Functions, `for` loops, `std::cin`, `std::cout`, `long long`, recursion alternative

**Starter Code:**

```cpp
#include <iostream>

// TODO: Write a function factorial(int n) that returns n!

int main() {
    int n;

    std::cout << "Enter a non-negative integer: ";
    std::cin >> n;

    // TODO: Call the factorial function
    // TODO: Handle negative input with an error message
    // TODO: Print the result

    return 0;
}
```

---

## Exercise 4: Vector Average

**Description:** Write a program that uses a `std::vector<int>` to store numbers entered by the user. The user enters numbers one at a time and types -1 to stop. The program then calculates and prints the count, sum, and average of the entered numbers (excluding -1). This introduces the `std::vector` container and its `push_back` method.

**Difficulty:** Basic

**Concepts:** `std::vector`, `push_back`, `while` loop, `std::cin`, `std::cout`, range-based `for` loop

**Starter Code:**

```cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> numbers;
    int input;

    std::cout << "Enter numbers (-1 to stop):" << std::endl;

    // TODO: Read numbers in a loop, push into vector, stop on -1
    // TODO: Calculate sum using a range-based for loop
    // TODO: Calculate and print count, sum, and average
    // TODO: Handle the case where no numbers were entered

    return 0;
}
```

---

## Exercise 5: Simple Student Class

**Description:** Define a `Student` class with private data members for `name` (string) and `grade` (int). Provide a constructor to initialize both fields and public getter/setter methods. In `main`, create two `Student` objects, display their information, modify a grade using the setter, and print the updated details.

**Difficulty:** Basic

**Concepts:** Classes, constructors, private/public access specifiers, getters/setters, `std::string`, objects

**Starter Code:**

```cpp
#include <iostream>
#include <string>

// TODO: Define the Student class with private name and grade
// TODO: Add a constructor that takes name and grade
// TODO: Add getter and setter methods

int main() {
    // TODO: Create two Student objects
    // TODO: Print their information
    // TODO: Change one student's grade using the setter
    // TODO: Print updated information

    return 0;
}
```
