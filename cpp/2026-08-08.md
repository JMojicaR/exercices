# C++ Basic Exercises — 2026-08-08

> **Level:** Beginner (Basic)  
> **Sources:** PYnative, W3Resource, GeeksforGeeks, W3Schools  
> **Topics:** iostream, Functions, Conditionals, Loops, vector, Classes

---

## Exercise 1: Hello, C++! — cout & cin

| Field | Detail |
|-------|--------|
| **Difficulty** | ★☆☆☆☆ |
| **Concepts** | `#include <iostream>`, `std::cout`, `std::cin`, `std::string`, `using namespace std` |

**Description:**  
Write a C++ program that reads the user's name and age, then prints `"Hello <name>, you are <age> years old!"`

**Starter Code:**
```cpp
#include <iostream>
#include <string>

int main() {
    std::string name;
    int age;

    std::cout << "Enter your name: ";
    std::getline(std::cin, name);

    std::cout << "Enter your age: ";
    std::cin >> age;

    // TODO: Print greeting

    return 0;
}
```

---

## Exercise 2: Temperature Converter — Functions & Arithmetic

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | Functions, `double`, formula: °F = °C × 9/5 + 32, `if/else` or menu loop |

**Description:**  
Write a program with two functions: `celsiusToFahrenheit` and `fahrenheitToCelsius`.  
Present a menu so the user can choose the conversion direction.

**Starter Code:**
```cpp
#include <iostream>

double celsiusToFahrenheit(double c) {
    // TODO: return (c * 9.0/5.0) + 32
}

double fahrenheitToCelsius(double f) {
    // TODO: return (f - 32) * 5.0/9.0
}

int main() {
    int choice;
    double temp;

    std::cout << "1. Celsius → Fahrenheit\n2. Fahrenheit → Celsius\nChoice: ";
    std::cin >> choice;

    // TODO: Read temperature, call the right function, print result

    return 0;
}
```

---

## Exercise 3: Prime Number Checker — Loops & Conditionals

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `bool`, `for` loop, `if`, `sqrt()`, `<cmath>` |

**Description:**  
Write a function `bool isPrime(int n)` that returns `true` if `n` is prime.  
A number is prime if it is > 1 and has no divisors other than 1 and itself.

**Starter Code:**
```cpp
#include <iostream>
#include <cmath>

bool isPrime(int n) {
    // TODO: Check if n is prime
}

int main() {
    for (int i = 1; i <= 20; i++) {
        std::cout << i << " is " << (isPrime(i) ? "prime" : "not prime") << "\n";
    }
    return 0;
}
```

---

## Exercise 4: Vector Statistics — std::vector & Range-for

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `std::vector<int>`, range-based `for`, `push_back()`, `size()`, algorithms |

**Description:**  
Create a `std::vector<int>` with several numbers. Print the sum, average, and the largest element using a loop.

**Starter Code:**
```cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> numbers = {15, 8, 23, 42, 4, 16};

    int sum = 0;
    int maxVal = numbers[0];

    // TODO: Loop through the vector, compute sum and maxVal
    // TODO: Print sum, average (as double), and maxVal

    return 0;
}
```

---

## Exercise 5: Simple BankAccount Class — Basic OOP

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★★☆☆ |
| **Concepts** | `class`, constructor, public/private, methods, `std::string` |

**Description:**  
Create a `BankAccount` class with:
- Private fields: `owner` (string) and `balance` (double)
- Constructor
- Methods: `deposit(amount)`, `withdraw(amount)`, `display()`
- Withdraw should reject if insufficient funds.

**Starter Code:**
```cpp
#include <iostream>
#include <string>

class BankAccount {
private:
    std::string owner;
    double balance;

public:
    BankAccount(std::string name, double initial)
        : owner(name), balance(initial) {}

    // TODO: deposit(double amount)
    // TODO: withdraw(double amount) — check for sufficient funds
    // TODO: display() — print owner and balance
};

int main() {
    BankAccount acc("Alice", 1000.0);
    acc.deposit(500);
    acc.withdraw(200);
    acc.withdraw(2000);  // Should print "Insufficient funds"
    acc.display();
    return 0;
}
```
