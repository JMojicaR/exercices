# Programming Exercises — Summary 2026-09-11

## Languages (5 exercises each, basic level)

| Language | Exercises | Theme / Topic |
|----------|-----------|---------------|
| Python | 5 | Variables, even/odd, FizzBuzz, factorial (recursion), palindrome checker |
| JavaScript | 5 | Variables & `typeof`, even/odd, FizzBuzz, string reversal, array sum |
| Java | 5 | Primitives, even/odd (`Scanner`), factorial (recursion), FizzBuzz, string reverse |
| C | 5 | `printf`/format specifiers, even/odd (`scanf`), factorial (loop), Fibonacci, array sum |
| C++ | 5 | `std::cout`, even/odd (`std::cin`), factorial (recursion), FizzBuzz, vector average |
| C# | 5 | `Console` I/O, even/odd, factorial (recursion), FizzBuzz, string reverse |
| Rust | 5 | `let`/`mut`, even/odd, factorial (recursion), FizzBuzz, vector sum |
| Go | 5 | `:=`/`var`/`const`, even/odd, factorial (recursion), FizzBuzz, slice sum |
| Erlang | 5 | Module/export, even/odd (guards), tail-recursive factorial, FizzBuzz (list comprehension), list sum |

## API Frameworks (3 exercises each)

| Framework | Exercises | Theme / Topic |
|-----------|-----------|---------------|
| Flask | 3 | GET greeting + path params, POST JSON echo, in-memory CRUD (GET/POST/PUT/DELETE) |
| FastAPI | 3 | GET + typed path params, POST with Pydantic model, in-memory CRUD with HTTPException |
| Node.js/Express | 3 | GET greeting + route params, POST with `express.json()` middleware, in-memory CRUD |

## Issues / Missing Content
- None. All requested languages and frameworks delivered at basic/beginner level.
- API exercises cover GET/POST/PUT/DELETE, request/response handling, middleware (`express.json()`), error handling (`HTTPException`, 400/404), and basic CRUD.

## Totals
- 9 languages × 5 exercises = **45 exercises**
- 3 frameworks × 3 exercises = **9 exercises**
- **Total: 54 exercises**
