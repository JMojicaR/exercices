# FastAPI — Basic API Exercises

Date: 2026-09-11 | Level: Beginner | Sources: FastAPI docs, GeeksforGeeks, Real Python (adapted)

## Exercise 1: Hello, World! GET Endpoint
- **Difficulty:** Easy
- **Description:** Create a FastAPI app with a `GET /` route returning a JSON greeting, and a `GET /hello/{name}` route with a typed path parameter.
- **Concepts:** `FastAPI()` instantiation, decorators, type hints, Pydantic response models
- **Starter code:**
```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def home():
    return {"message": "Hello, World!"}

@app.get("/hello/{name}")
def hello(name: str):
    # TODO: return a personalized greeting
    return {}

# Run with: uvicorn main:app --reload
```

## Exercise 2: POST with a Pydantic Model
- **Difficulty:** Easy
- **Description:** Define a Pydantic `Item` model (`id: int`, `name: str`, `price: float`) and a `POST /items` endpoint that accepts and returns it.
- **Concepts:** `BaseModel`, request body validation, automatic 422 responses
- **Starter code:**
```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Item(BaseModel):
    id: int
    name: str
    price: float

@app.post("/items")
def create_item(item: Item):
    # TODO: return the item with a confirmation message
    return {}

# Run with: uvicorn main:app --reload
```

## Exercise 3: In-Memory CRUD (GET/POST/PUT/DELETE)
- **Difficulty:** Medium
- **Description:** Build a full CRUD API over an in-memory dictionary. Implement `GET /items`, `POST /items`, `PUT /items/{item_id}`, and `DELETE /items/{item_id}` with proper status codes and `HTTPException` for missing items.
- **Concepts:** all HTTP verbs, path parameters, `HTTPException`, status codes, `Response`/204 handling
- **Starter code:**
```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel

app = FastAPI()
items = {}  # id -> name

class Item(BaseModel):
    name: str

@app.get("/items")
def list_items():
    return items

@app.post("/items", status_code=201)
def create_item(item_id: int, item: Item):
    # TODO: add the item, return it
    return {}

@app.put("/items/{item_id}")
def update_item(item_id: int, item: Item):
    if item_id not in items:
        raise HTTPException(status_code=404, detail="Item not found")
    items[item_id] = item.name
    return {"id": item_id, "name": item.name}

@app.delete("/items/{item_id}", status_code=204)
def delete_item(item_id: int):
    # TODO: remove the item, raise 404 if missing, return no body
    pass

# Run with: uvicorn main:app --reload
```
