# FastAPI (Python) Basic API Exercises - 2026-07-25

> Sources: [FastAPI Official Tutorial](https://fastapi.tiangolo.com/tutorial/), [FastAPI Path Parameters](https://fastapi.tiangolo.com/tutorial/path-params/), [FastAPI Query Parameters](https://fastapi.tiangolo.com/tutorial/query-params/), [FastAPI Request Body](https://fastapi.tiangolo.com/tutorial/body/)

---

## Exercise 1: Hello World API with Path & Query Parameters
- **Description**: Create a FastAPI application with a `GET /hello/{name}` endpoint that accepts a **path parameter** `name` and an optional **query parameter** `greeting` (default: `"Hello"`). The endpoint should return a JSON response like `{"message": "Hello, Alice!"}`. Add a second endpoint `GET /status` that returns `{"status": "running", "version": "1.0.0"}`. Test your endpoints using the auto-generated Swagger UI at `/docs`.
- **Difficulty**: Beginner
- **Concepts**: FastAPI app creation, `@app.get()` decorator, path parameters (`{name}`), query parameters with defaults, JSON responses, automatic Swagger docs
- **Example/Starter Code**:
```python
from fastapi import FastAPI

app = FastAPI(title="Hello API", version="1.0.0")

# TODO: Create GET /hello/{name} endpoint
#   - name: str (path parameter)
#   - greeting: str = "Hello" (query parameter, optional, default "Hello")
#   - Return {"message": f"{greeting}, {name}!"}


# TODO: Create GET /status endpoint
#   - Return {"status": "running", "version": "1.0.0"}
```

---

## Exercise 2: Basic CRUD API with Pydantic Models
- **Description**: Build a simple in-memory CRUD API to manage a list of "tasks". Define a **Pydantic model** `Task` with fields: `id` (int), `title` (str, min 3 chars), `description` (str, optional), and `completed` (bool, default `False`). Implement four endpoints:
  - `POST /tasks/` — create a new task (auto-increment `id`), return the created task
  - `GET /tasks/` — return the full list of all tasks
  - `GET /tasks/{task_id}` — return a single task by ID, or a 404 error if not found
  - `PUT /tasks/{task_id}` — update a task's `title`, `description`, and/or `completed` fields
  - `DELETE /tasks/{task_id}` — delete a task by ID, return a success message

  Store tasks in a Python list (in-memory). Use HTTP status codes and proper response models.
- **Difficulty**: Beginner
- **Concepts**: Pydantic `BaseModel`, `Field` validators (`min_length`), `Optional` fields, `@app.post()`, `@app.get()`, `@app.put()`, `@app.delete()`, HTTP status codes (`HTTPException`), `response_model`, path parameters
- **Example/Starter Code**:
```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel, Field
from typing import Optional

app = FastAPI(title="Task Manager API")

# TODO: Define the Task model
#   - id: int (auto-assigned, not required on creation)
#   - title: str (required, min_length=3)
#   - description: Optional[str] = None
#   - completed: bool = False

# TODO: Create a separate model for task creation (without id)


# In-memory task store
tasks_db = []
next_id = 1

# TODO: POST /tasks/ — create a new task, auto-assign id
# TODO: GET /tasks/ — return all tasks
# TODO: GET /tasks/{task_id} — return task by id or 404
# TODO: PUT /tasks/{task_id} — update a task
# TODO: DELETE /tasks/{task_id} — delete a task
```

---

## Exercise 3: User Search API with Query Parameters & Validation
- **Description**: Build a `GET /users/search` endpoint that filters a pre-defined list of users based on multiple **query parameters** with validation. Define a `User` Pydantic model with `id`, `name`, `age`, and `email`. Seed a list of 6-8 users. The search endpoint should accept these optional query parameters:
  - `min_age` (int, optional, ≥ 0) — filter users with age ≥ this value
  - `max_age` (int, optional, ≤ 120) — filter users with age ≤ this value
  - `name_contains` (str, optional, min 1 char) — filter users whose name contains this substring (case-insensitive)
  - `limit` (int, optional, default 10, range 1-50) — max results to return

  Also add a `GET /users/count` endpoint that returns the total number of users and the average age. Use FastAPI's built-in `Query` parameter validation and return the filtered list as JSON.
- **Difficulty**: Beginner
- **Concepts**: Query parameters with `Query()` validation (`ge`, `le`, `min_length`), Pydantic models for response, list filtering in Python, `response_model`, endpoint organization
- **Example/Starter Code**:
```python
from fastapi import FastAPI, Query
from pydantic import BaseModel, EmailStr

app = FastAPI(title="User Search API")

# TODO: Define the User model (id, name, age, email)

# Pre-seeded user data
users_db = [
    {"id": 1, "name": "Alice", "age": 28, "email": "alice@example.com"},
    {"id": 2, "name": "Bob", "age": 35, "email": "bob@example.com"},
    {"id": 3, "name": "Charlie", "age": 22, "email": "charlie@example.com"},
    {"id": 4, "name": "Diana", "age": 41, "email": "diana@example.com"},
    {"id": 5, "name": "Eve", "age": 30, "email": "eve@example.com"},
    {"id": 6, "name": "Frank", "age": 19, "email": "frank@example.com"},
]

# TODO: GET /users/search — filter users by min_age, max_age, name_contains, limit
#   Use Query() to add validation: min_age ≥ 0, max_age ≤ 120, limit in 1-50

# TODO: GET /users/count — return {"total": N, "average_age": X.X}
```
