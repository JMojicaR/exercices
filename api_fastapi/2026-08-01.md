# FastAPI API Coding Exercises - 2026-08-01

---

## Exercise 1: Item Catalog API — Basic CRUD Endpoints

**Title:** Build a Simple Item Catalog with GET, POST, PUT, and DELETE

**Difficulty:** Basic

**Description:**  
Create a RESTful API for managing an item catalog. You'll implement all four core CRUD operations (Create, Read, Update, Delete) against an in-memory store. This exercise covers defining a FastAPI app, creating Pydantic models for request/response validation, writing path operation decorators, and handling errors when items are not found.

**Concepts Covered:**
- `FastAPI()` app initialization
- Pydantic `BaseModel` for request/response schemas
- `@app.get`, `@app.post`, `@app.put`, `@app.delete` decorators
- Path parameters (`/{item_id}`)
- `HTTPException` with status codes (404, 400)
- In-memory storage with a dictionary
- Automatic OpenAPI documentation at `/docs` and `/redoc`

**Starter Code:**

```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel, Field
from typing import List

# TODO: Initialize the FastAPI app

# TODO: Define a Pydantic model named 'Item'
#       Fields: id (int), name (str), price (float), in_stock (bool, default True)
#       Use Field() to add descriptions and validation (e.g., price must be > 0)

# TODO: Define a Pydantic model named 'ItemCreate' (for POST requests)
#       Fields: name (str), price (float), in_stock (bool, default True)
#       (No id — the server assigns it)

# TODO: Create an in-memory storage dictionary (e.g., items_db = {})
#       and a counter for auto-incrementing IDs (e.g., next_id = 1)


# TODO: GET /items — return the list of all items
#       Return type: List[Item]

# TODO: GET /items/{item_id} — return a single item by ID
#       Raise HTTPException(404) if not found

# TODO: POST /items — create a new item from ItemCreate body
#       Assign the next available ID, store it, return the new Item
#       Status code should be 201 (use status_code parameter)

# TODO: PUT /items/{item_id} — update an existing item
#       Accept an ItemCreate body, update the stored item, return the updated Item
#       Raise HTTPException(404) if not found

# TODO: DELETE /items/{item_id} — delete an item by ID
#       Return {"message": "Item deleted"}
#       Raise HTTPException(404) if not found
```

---

## Exercise 2: Task Manager API — Query Parameters & Status Filtering

**Title:** Build a Task Manager with Query Parameter Filtering

**Difficulty:** Basic

**Description:**  
Create a task management API that supports listing tasks with optional status filtering via query parameters, creating new tasks, and marking tasks as complete. This exercise focuses on query parameters, optional fields in Pydantic models, and updating specific fields of a resource without requiring the full object.

**Concepts Covered:**
- Query parameters with default values and `Optional` types
- `@app.get` with query parameters (e.g., `?status=done`)
- Pydantic `Optional` fields and `Field(default=...)`
- Partial updates (marking a task as done without resending the full body)
- `Enum` for constrained status values
- Response filtering based on query parameters

**Starter Code:**

```python
from fastapi import FastAPI, HTTPException, Query
from pydantic import BaseModel, Field
from enum import Enum
from typing import List, Optional

# TODO: Initialize the FastAPI app

# TODO: Define a Status enum with values: "todo", "in_progress", "done"

# TODO: Define a Pydantic model named 'Task'
#       Fields: id (int), title (str), description (Optional[str], default None),
#               status (Status, default Status.todo)

# TODO: Define a Pydantic model named 'TaskCreate' (for POST requests)
#       Fields: title (str), description (Optional[str], default None)

# TODO: Create in-memory storage for tasks (dict + id counter)


# TODO: GET /tasks — return all tasks
#       Add an optional query parameter 'status' (type: Optional[Status])
#       If status is provided, filter tasks to only return those matching
#       Return type: List[Task]

# TODO: GET /tasks/{task_id} — return a single task
#       Raise HTTPException(404) if not found

# TODO: POST /tasks — create a new task
#       Auto-assign id, status defaults to "todo", status_code=201

# TODO: PATCH /tasks/{task_id}/complete — mark a task as done
#       Set the task's status to Status.done, return the updated task
#       Raise HTTPException(404) if not found
#       Raise HTTPException(400) if the task is already done

# TODO: DELETE /tasks/{task_id} — delete a task
#       Return {"message": "Task deleted"}
#       Raise HTTPException(404) if not found
```

---

## Exercise 3: Student Records API — Request Body Validation & Nested Models

**Title:** Build a Student Records API with Nested Pydantic Models

**Difficulty:** Basic

**Description:**  
Create a student records API that manages student profiles and their course enrollments. This exercise introduces nested Pydantic models (a student containing a list of course records), field-level validation with `Field()` constraints, and custom error handling for duplicate entries and validation failures.

**Concepts Covered:**
- Nested Pydantic models (one model inside another)
- `Field()` validators: `min_length`, `max_length`, `ge`, `le`, `gt`
- List fields with type hints (`List[Course]`)
- Custom validation logic in the route handler (e.g., preventing duplicate enrollments)
- `HTTPException` with detailed error messages (400, 404, 409 Conflict)
- Multiple path operations on the same resource (`/students/{student_id}/courses`)

**Starter Code:**

```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel, Field
from typing import List, Optional
from datetime import date

# TODO: Initialize the FastAPI app

# TODO: Define a Pydantic model named 'Course'
#       Fields: code (str, min_length=3, max_length=10),
#               name (str), grade (Optional[float], ge=0.0, le=4.0, default None)
#       This represents a course a student is enrolled in

# TODO: Define a Pydantic model named 'Student'
#       Fields: id (int), first_name (str), last_name (str),
#               email (str — should contain '@' but we'll handle that in a future exercise),
#               enrollment_date (Optional[date], default None),
#               courses (List[Course], default [])

# TODO: Define a Pydantic model named 'StudentCreate' (for POST)
#       Fields: first_name (str), last_name (str), email (str)

# TODO: Define a Pydantic model named 'EnrollCourse' (for POST to courses)
#       Fields: code (str), name (str)

# TODO: Create in-memory storage for students (dict + id counter)


# TODO: GET /students — return all students
#       Return type: List[Student]

# TODO: GET /students/{student_id} — return a single student
#       Raise HTTPException(404) if not found

# TODO: POST /students — create a new student
#       Validate that email contains '@', raise HTTPException(400) if invalid
#       Auto-assign id, empty courses list, status_code=201

# TODO: POST /students/{student_id}/courses — enroll a student in a course
#       Accept an EnrollCourse body
#       Prevent duplicate enrollment: if a course with the same code already exists,
#       raise HTTPException(409, detail="Student already enrolled in this course")
#       Append the new Course to the student's courses list
#       Raise HTTPException(404) if student not found

# TODO: DELETE /students/{student_id}/courses/{course_code} — drop a course
#       Remove the course with matching code from the student's courses
#       Raise HTTPException(404) if student not found
#       Raise HTTPException(404) if course not found in student's enrollments
#       Return {"message": f"Dropped course {course_code}"}

# TODO: GET /students/search — search students by last name
#       Add a required query parameter 'last_name' (str)
#       Return all students whose last_name matches (case-insensitive)
#       Return an empty list if no matches found (not an error)
```
