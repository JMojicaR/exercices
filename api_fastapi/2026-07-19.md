# FastAPI API Exercises
**Date:** 2026-07-19
**Level:** Basic
**Source References:** FastAPI Documentation, Real Python, GeeksforGeeks

---

## Exercise 1: CRUD API for Books with Pydantic Models

**Title:** Build a Books API with FastAPI and Pydantic

**Description:**  
Create a FastAPI application for managing a book collection. Use Pydantic models for request/response validation. Implement:
- `GET /books` — list all books (support optional `?author=` query param to filter)
- `GET /books/{book_id}` — get a single book
- `POST /books` — create a new book (validate with Pydantic)
- `PUT /books/{book_id}` — update a book
- `DELETE /books/{book_id}` — delete a book

Book model fields: `id` (int, auto-generated), `title` (str, required), `author` (str, required), `year` (int, optional), `isbn` (str, optional).

**Concepts Practiced:** FastAPI routing, Pydantic models, path/query parameters, HTTP status codes, type hints

**Difficulty:** Basic

**Starter Code:**
```python
from fastapi import FastAPI, HTTPException, status, Query
from pydantic import BaseModel, Field
from typing import Optional

app = FastAPI(title="Books API", version="1.0.0")

# In-memory storage
books: dict[int, dict] = {}
next_id = 1

# --- Pydantic Models ---

class BookCreate(BaseModel):
    title: str = Field(..., min_length=1, max_length=200, description="Book title")
    author: str = Field(..., min_length=1, max_length=100, description="Author name")
    year: Optional[int] = Field(None, ge=1000, le=2100, description="Publication year")
    isbn: Optional[str] = Field(None, description="ISBN-13 or ISBN-10")

class BookUpdate(BaseModel):
    title: Optional[str] = Field(None, min_length=1, max_length=200)
    author: Optional[str] = Field(None, min_length=1, max_length=100)
    year: Optional[int] = Field(None, ge=1000, le=2100)
    isbn: Optional[str] = None

class BookResponse(BaseModel):
    id: int
    title: str
    author: str
    year: Optional[int] = None
    isbn: Optional[str] = None

# --- Routes ---

@app.get("/books", response_model=list[BookResponse])
async def get_books(author: Optional[str] = Query(None, description="Filter by author")):
    # Return all books, optionally filtered by author
    pass

@app.get("/books/{book_id}", response_model=BookResponse)
async def get_book(book_id: int):
    # Return a single book or raise 404
    pass

@app.post("/books", response_model=BookResponse, status_code=status.HTTP_201_CREATED)
async def create_book(book: BookCreate):
    # Create a new book, auto-generate ID
    pass

@app.put("/books/{book_id}", response_model=BookResponse)
async def update_book(book_id: int, book: BookUpdate):
    # Update an existing book (partial update — only provided fields)
    # Raise 404 if not found
    pass

@app.delete("/books/{book_id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_book(book_id: int):
    # Delete a book, raise 404 if not found
    pass

# Run with: uvicorn main:app --reload
# Interactive docs at: http://localhost:8000/docs
```

---

## Exercise 2: File Upload and Streaming API

**Title:** Build a File Upload and Download API

**Description:**  
Create a FastAPI application that handles file uploads and downloads:
- `POST /upload` — accept file uploads (single file via `UploadFile`), save to `./uploads/` directory, return file metadata (name, size, type, saved path)
- `GET /files` — list all uploaded files with their metadata
- `GET /files/{filename}` — download a file using `FileResponse`
- `DELETE /files/{filename}` — delete a file

Store file metadata in memory. Use FastAPI's built-in `UploadFile` for handling multipart form data.

**Concepts Practiced:** File upload (`UploadFile`, `File`), `FileResponse`, multipart form data, file I/O, metadata management

**Difficulty:** Basic

**Starter Code:**
```python
from fastapi import FastAPI, UploadFile, File, HTTPException, status
from fastapi.responses import FileResponse
import os
import shutil
from datetime import datetime

app = FastAPI(title="File Upload API", version="1.0.0")

UPLOAD_DIR = "./uploads"
os.makedirs(UPLOAD_DIR, exist_ok=True)

# In-memory metadata store
files_metadata: dict[str, dict] = {}

@app.post("/upload")
async def upload_file(file: UploadFile = File(...)):
    # Save file to ./uploads/ directory
    # Store metadata (filename, original name, size, content_type, upload_time)
    # Return metadata
    pass

@app.get("/files")
async def list_files():
    # Return list of all file metadata
    pass

@app.get("/files/{filename}")
async def download_file(filename: str):
    # Return the file as a download using FileResponse
    # Check file exists, raise 404 if not
    pass

@app.delete("/files/{filename}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_file(filename: str):
    # Delete file from disk and remove from metadata
    # Raise 404 if not found
    pass

# Test with:
# curl -X POST http://localhost:8000/upload -F "file=@myfile.txt"
# curl http://localhost:8000/files
# curl http://localhost:8000/files/myfile.txt --output downloaded.txt
```

---

## Exercise 3: Dependency Injection and Rate Limiting

**Title:** Add API Key Authentication and Rate Limiting with Dependencies

**Description:**  
Build a FastAPI application with:
- An API key-based authentication system using FastAPI dependencies
- A `get_api_key()` dependency that extracts and validates an API key from the `X-API-Key` header
- A simple in-memory rate limiter dependency that limits requests to 5 per minute per API key
- A protected endpoint `GET /data` that returns mock data
- An admin endpoint `GET /admin/stats` that requires a specific admin API key and returns usage stats

Use `HTTPException` for unauthorized responses. Store API keys and rate limit counters in memory.

**Concepts Practiced:** FastAPI dependencies (`Depends`), dependency injection, authentication, rate limiting, header extraction, `HTTPException`

**Difficulty:** Basic

**Starter Code:**
```python
from fastapi import FastAPI, Depends, HTTPException, status, Header
from datetime import datetime, timedelta
from collections import defaultdict

app = FastAPI(title="Secure API", version="1.0.0")

# Simulated API key store (in production, use a database)
VALID_API_KEYS = {
    "user-key-abc123": {"tier": "basic", "name": "Alice"},
    "user-key-def456": {"tier": "basic", "name": "Bob"},
    "admin-key-secret": {"tier": "admin", "name": "Admin"},
}

# Rate limit tracking: api_key -> [(timestamp, endpoint), ...]
rate_limit_log: dict[str, list] = defaultdict(list)
RATE_LIMIT = 5          # max requests
RATE_WINDOW = 60        # seconds

def get_api_key(x_api_key: str = Header(..., alias="X-API-Key")):
    # Validate the API key exists in VALID_API_KEYS
    # Return the user info dict if valid
    # Raise 401 if missing or invalid
    pass

def check_rate_limit(api_key: str):
    # Check how many requests this key has made in the last RATE_WINDOW seconds
    # Raise 429 if limit exceeded
    # Record this request in rate_limit_log
    pass

@app.get("/data")
async def get_data(
    user: dict = Depends(get_api_key),
    _rate_check: None = Depends(lambda: None)  # placeholder for rate limiting
):
    # Apply rate limiting check
    # Return mock data with user info
    pass

@app.get("/admin/stats")
async def admin_stats(user: dict = Depends(get_api_key)):
    # Check if user is admin tier
    # Return rate_limit_log stats (total requests, per-key counts)
    pass

# Test with:
# curl http://localhost:8000/data -H "X-API-Key: user-key-abc123"
# curl http://localhost:8000/admin/stats -H "X-API-Key: admin-key-secret"
```

---

*Generated by Hermes Agent — Cron Job: Programming Exercise Collection*
