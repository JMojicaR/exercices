# FastAPI API Exercises — 2026-08-08

> **Level:** Beginner (Basic) — focused on API creation  
> **Sources:** FastAPI docs, Real Python, APXML tutorials  
> **Topics:** Path/Query params, Pydantic models, CRUD, HTTPException, Background tasks

---

## Exercise 1: Hello World & Path Parameters — Basic FastAPI App

| Field | Detail |
|-------|--------|
| **Difficulty** | ★☆☆☆☆ |
| **Concepts** | `FastAPI()`, `@app.get("/")`, path parameters, `uvicorn` |

**Description:**  
Create a FastAPI app with two endpoints:
- `GET /` — returns `{"message": "Hello, World!"}`
- `GET /greet/{name}` — returns `{"message": f"Hello, {name}!"}`

**Starter Code:**
```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def home():
    # TODO: Return greeting dict
    pass

@app.get("/greet/{name}")
def greet(name: str):
    # TODO: Return personalized greeting dict
    pass
```

**Run & test:**
```bash
uvicorn main:app --reload
# Open http://localhost:8000/docs for Swagger UI
```

---

## Exercise 2: Items CRUD API — Pydantic Models & Full CRUD

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `BaseModel`, `Optional`, `List`, `HTTPException`, `status`, in-memory dict |

**Description:**  
Build a complete CRUD API for "items" with in-memory storage:
- `POST /items` — create item (status 201)
- `GET /items` — list all items
- `GET /items/{item_id}` — get one item
- `PUT /items/{item_id}` — update item
- `DELETE /items/{item_id}` — delete item

**Starter Code:**
```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel
from typing import Optional

app = FastAPI()

class Item(BaseModel):
    name: str
    price: float
    description: Optional[str] = None

class ItemResponse(Item):
    id: int

items_db = {}
next_id = 1

@app.post("/items", response_model=ItemResponse, status_code=201)
def create_item(item: Item):
    global next_id
    # TODO: Store item, return with id
    pass

@app.get("/items", response_model=list[ItemResponse])
def list_items():
    # TODO: Return list of all items
    pass

@app.get("/items/{item_id}", response_model=ItemResponse)
def get_item(item_id: int):
    # TODO: Return item or raise 404
    pass

@app.put("/items/{item_id}", response_model=ItemResponse)
def update_item(item_id: int, item: Item):
    # TODO: Update and return item or 404
    pass

@app.delete("/items/{item_id}")
def delete_item(item_id: int):
    # TODO: Delete and return success message or 404
    pass
```

**Test using Swagger UI at `/docs` or curl:**
```bash
curl -X POST http://localhost:8000/items -H "Content-Type: application/json" -d '{"name":"Widget","price":9.99}'
curl http://localhost:8000/items
```

---

## Exercise 3: Error Handling & Middleware — Custom Error Responses

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★★☆☆ |
| **Concepts** | `HTTPException`, custom exception handlers, middleware, `Request`, timing, input validation |

**Description:**  
Extend Exercise 2 with:
1. A custom exception handler for `ValueError` that returns a 400 response
2. A simple timing middleware that logs the duration of each request to the console
3. Input validation: reject item creation if `price < 0`

**Starter Code:**
```python
import time
from fastapi import FastAPI, HTTPException, Request
from fastapi.responses import JSONResponse

app = FastAPI()

# 1. Custom exception handler
@app.exception_handler(ValueError)
def value_error_handler(request: Request, exc: ValueError):
    # TODO: Return 400 JSON with detail=str(exc)
    pass

# 2. Timing middleware
@app.middleware("http")
async def timing_middleware(request: Request, call_next):
    # TODO: Record start time, call_next, compute duration, print log
    pass

# 3. Price validation (add to the POST /items endpoint)
# TODO: In the create_item function, raise ValueError if price < 0

# ... Include Item model and CRUD endpoints from Exercise 2 ...
```

**Test:**
```bash
curl -X POST http://localhost:8000/items -d '{"name":"Bad","price":-5}'  # Should get 400
curl http://localhost:8000/items  # Check console for timing log
```
