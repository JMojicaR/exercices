# FastAPI — Basic API Exercises

Date: 2026-09-12 | Level: Beginner | Sources: FastAPI docs, Real Python, Programiz (adapted)

## Exercise 1: GET with Path and Query Parameters
- **Difficulty:** Easy
- **Description:** Create a `GET /greet/{name}` endpoint that returns a greeting, plus a `GET /add` endpoint that sums two query parameters `a` and `b` (typed as `int`).
- **Concepts:** path parameters, query parameters, type hints, automatic validation
- **Starter code:**
```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/greet/{name}")
def greet(name: str):
    return {"message": f"Hello, {name}!"}

@app.get("/add")
def add(a: int, b: int):
    # TODO: return {"result": a + b}
    return {"result": 0}
```

## Exercise 2: POST with a Pydantic Model
- **Difficulty:** Easy
- **Description:** Define a Pydantic `Item` model with `name` (str) and `price` (float). Create a `POST /items` endpoint that accepts and echoes the item. FastAPI's validation should reject bad types with a 422 automatically.
- **Concepts:** Pydantic `BaseModel`, request body, automatic validation, type hints
- **Starter code:**
```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Item(BaseModel):
    name: str
    price: float

@app.post("/items")
def create_item(item: Item):
    # TODO: return the created item with a status code
    return item
```

## Exercise 3: In-Memory CRUD with HTTPException
- **Difficulty:** Medium
- **Description:** Build a CRUD API for tasks stored in a Python list using Pydantic models. Implement `GET /tasks`, `POST /tasks`, `PUT /tasks/{task_id}`, and `DELETE /tasks/{task_id}`, raising `HTTPException(404)` when an id is not found.
- **Concepts:** CRUD, `HTTPException`, `Response(status_code=...)`, path parameters
- **Starter code:**
```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel

app = FastAPI()

class Task(BaseModel):
    id: int
    title: str
    done: bool = False

tasks: list[Task] = []

@app.get("/tasks")
def get_tasks():
    return tasks

@app.post("/tasks", status_code=201)
def create_task(task: Task):
    # TODO: append task and return it

@app.put("/tasks/{task_id}")
def update_task(task_id: int, task: Task):
    # TODO: update matching task or raise HTTPException(404)

@app.delete("/tasks/{task_id}", status_code=204)
def delete_task(task_id: int):
    # TODO: remove matching task or raise HTTPException(404)
```
