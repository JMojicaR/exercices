# Rust Basic Exercises — 2026-08-09

> **Level:** Beginner (Basic)  
> **Sources:** Rustlings (rust-lang/rustlings), Mainmatter (100 Exercises to Learn Rust), Exercism  
> **Topics:** Variables, Functions, Conditionals, Primitive Types, Ownership Basics

---

## Exercise 1: Variables and Mutability

### Problem
Create a Rust program that declares an immutable variable `x` with a value, then creates a mutable variable `y`, modifies it, and prints both values. Rust variables are immutable by default — use `mut` when mutation is needed.

### Concepts
- `let` bindings
- Immutability by default
- `mut` keyword
- `println!` macro

### Example/Starter Code
```rust
fn main() {
    let x = 10;
    // TODO: Declare a mutable variable y, assign it a value, then change it
    // TODO: Print both x and y

}
```

---

## Exercise 2: Functions with Return Values

### Problem
Write a Rust program with a function `add(a: i32, b: i32) -> i32` that returns the sum of two integers. Call it from `main()` and print the result. Note that the last expression in a Rust function (without a semicolon) is the return value.

### Concepts
- Function definitions (`fn`)
- Parameter type annotations
- Return types (`-> i32`)
- Implicit returns (no semicolon on last expression)
- Explicit `return` statement (optional)

### Example/Starter Code
```rust
// TODO: Define the add function that takes two i32 parameters and returns their sum

fn main() {
    let result = add(5, 7);
    println!("5 + 7 = {}", result);
}
```

---

## Exercise 3: If/Else — Even or Odd

### Problem
Write a Rust program that checks whether a given integer is even or odd. Use an `if-else` expression — in Rust, `if` is an expression that can return a value.

### Concepts
- `if` / `else`
- Modulo operator (`%`)
- `if` as an expression (can bind to a variable)
- Boolean conditions

### Example/Starter Code
```rust
fn main() {
    let number = 42;

    // TODO: Use if-else to determine if number is even or odd
    // Hint: number % 2 == 0 means even
    // Print the result

}
```

---

## Exercise 4: Primitive Types — Temperature Converter

### Problem
Write a Rust program that converts Celsius to Fahrenheit. Declare a `f64` variable for the Celsius temperature, compute the Fahrenheit equivalent using the formula `F = C × 9/5 + 32`, and print the result with one decimal place.

### Concepts
- Primitive types: `f64` (floating-point)
- Arithmetic with floats
- Type annotations
- Formatted printing (`{:.1}`)

### Example/Starter Code
```rust
fn main() {
    let celsius: f64 = 25.0;

    // TODO: Convert celsius to fahrenheit
    // Formula: fahrenheit = celsius * 9.0 / 5.0 + 32.0
    // Print with one decimal place: println!("{:.1}", value);

}
```

---

## Exercise 5: Ownership Basics — String Clone vs Move

### Problem
Rust's ownership system ensures memory safety. Create a `String`, try assigning it to another variable, and observe the move. Then use `.clone()` to create a deep copy. Print both the original and the clone to verify they both work.

### Concepts
- `String` type (heap-allocated)
- Ownership and move semantics
- `.clone()` for deep copying
- Stack vs heap allocation

### Example/Starter Code
```rust
fn main() {
    let s1 = String::from("hello");

    // TODO: Try let s2 = s1; — this moves ownership. Can you still use s1?
    // Instead, clone s1 and print both s1 and the clone

}
```
