# Node.js / Express API Exercises — 2026-08-08

> **Level:** Beginner (Basic) — focused on API creation  
> **Sources:** freeCodeCamp, DEV Community, Express docs, Postman Blog  
> **Topics:** Express server, Routes, Middleware, JSON body, In-memory CRUD, Error handling

---

## Exercise 1: Hello World API — Basic Express Server

| Field | Detail |
|-------|--------|
| **Difficulty** | ★☆☆☆☆ |
| **Concepts** | `require('express')`, `app.get()`, `app.listen()`, `res.json()`, `req.params` |

**Description:**  
Create a basic Express server with two endpoints:
- `GET /` — returns `{ message: "Hello, World!" }`
- `GET /greet/:name` — returns `{ message: "Hello, <name>!" }`

**Starter Code:**
```javascript
const express = require('express');
const app = express();
const PORT = 3000;

app.get('/', (req, res) => {
  // TODO: Return { message: "Hello, World!" }
});

app.get('/greet/:name', (req, res) => {
  // TODO: Return { message: `Hello, ${req.params.name}!` }
});

app.listen(PORT, () => {
  console.log(`Server running on http://localhost:${PORT}`);
});
```

**Test:**
```bash
curl http://localhost:3000/
curl http://localhost:3000/greet/Alice
```

---

## Exercise 2: Users CRUD API — GET & POST with In-Memory Storage

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★☆☆☆ |
| **Concepts** | `express.json()` middleware, `req.body`, `app.post()`, array storage, status codes |

**Description:**  
Build a simple Users API with in-memory array:
- `GET /users` — returns all users
- `POST /users` — creates a user `{ id, name, email }`, returns it with status 201
- `GET /users/:id` — returns a single user by id, or 404

**Starter Code:**
```javascript
const express = require('express');
const app = express();
const PORT = 3000;

app.use(express.json());

let users = [];
let nextId = 1;

app.get('/users', (req, res) => {
  res.json(users);
});

app.post('/users', (req, res) => {
  const { name, email } = req.body;
  // TODO: Validate input, create user object, push, return 201
});

app.get('/users/:id', (req, res) => {
  const id = parseInt(req.params.id);
  // TODO: Find user by id or return 404
});

app.listen(PORT, () => {
  console.log(`Server on http://localhost:${PORT}`);
});
```

**Test:**
```bash
curl -X POST http://localhost:3000/users -H "Content-Type: application/json" -d '{"name":"Alice","email":"alice@example.com"}'
curl http://localhost:3000/users
curl http://localhost:3000/users/1
```

---

## Exercise 3: Full CRUD + Middleware — PUT, DELETE & Error Handling

| Field | Detail |
|-------|--------|
| **Difficulty** | ★★★☆☆ |
| **Concepts** | `app.put()`, `app.delete()`, custom middleware, `app.use()` for 404/500, validation |

**Description:**  
Extend Exercise 2 with:
- `PUT /users/:id` — update name/email, return updated user or 404
- `DELETE /users/:id` — delete user, return success or 404
- A custom request-logger middleware: logs `[METHOD] /path` for every request
- A global 404 handler middleware
- A global error handler middleware

**Starter Code:**
```javascript
const express = require('express');
const app = express();
const PORT = 3000;

app.use(express.json());

// 1. Logger middleware
app.use((req, res, next) => {
  // TODO: console.log `[${req.method}] ${req.path}`
  next();
});

let users = [];
let nextId = 1;

app.get('/users', (req, res) => { res.json(users); });

app.post('/users', (req, res) => {
  const { name, email } = req.body;
  if (!name || !email) return res.status(400).json({ error: 'name and email required' });
  const user = { id: nextId++, name, email };
  users.push(user);
  res.status(201).json(user);
});

app.put('/users/:id', (req, res) => {
  const id = parseInt(req.params.id);
  // TODO: Find user, update fields, return updated user or 404
});

app.delete('/users/:id', (req, res) => {
  const id = parseInt(req.params.id);
  // TODO: Find and remove user, return { message: 'User deleted' } or 404
});

// 2. 404 handler
app.use((req, res) => {
  // TODO: Return status 404, { error: 'Not found' }
});

// 3. Error handler (4 params)
app.use((err, req, res, next) => {
  // TODO: Log error, return 500 JSON
});

app.listen(PORT, () => {
  console.log(`Server on http://localhost:${PORT}`);
});
```

**Test:**
```bash
curl -X PUT http://localhost:3000/users/1 -H "Content-Type: application/json" -d '{"name":"Alice Updated","email":"alice@new.com"}'
curl -X DELETE http://localhost:3000/users/1
curl http://localhost:3000/notfound  # Should get 404 JSON
```
